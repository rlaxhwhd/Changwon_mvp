# 수정.md — v2 화면 디자인 정비 규칙

이번 작업은 거의 전부 **디자인 변경**이었다. 다른 컴퓨터·다른 사람이 이어받아도 같은 기준으로
판단할 수 있도록 **규칙과 근거**를 남긴다.

> 사실(토큰 값·파일 위치)은 `DESIGN.md`·`src_v2/index.css`가 소유한다.
> 이 문서는 **판단 규칙**만 담는다. 값을 여기에 복사하지 말 것.

---

## 0. 기준 화면 — 고치지 않는다

| 대상 | 이유 |
|---|---|
| `/v2/main` (`Main.css`) | 디자인 기준점 |
| `/v2/lounge` (`AiLounge.css`) | 디자인 기준점 |
| `StudentStatCards.css` | 라운지 카드 그 자체 (교직원 상세와 공용) |

나머지 서브페이지는 **이 두 화면의 말투에 맞춘다.** 반대 방향으로 맞추지 않는다.

---

## 1. 타이포

### 1-1 크기 — 시안 9단만 쓴다

```
12 · 13 · 14 · 16 · 18 · 20 · 24 · 28 · 32     (+ 10 · 11 은 마이크로 라벨로 허용)
```

- **홀수·소수점 금지** — 15 · 17 · 19 · 21 · 22 · 23 · 25 · 13.5 · 14.5 는 시안에 없다.
- **33px 초과는 스냅 대상이 아니다** — 히어로 숫자·장식 글리프다. 줄이면 깨진다.
- 근거: 라운지 실사용 분포 — 12px 51회 · 13px 15회 · 14px 12회 · 18px 6회 · 20px 4회.
  15/17/19/21/22/23/25px 은 **0회**.

### 1-2 굵기 — 900 은 아껴 쓴다

라운지도 900을 쓴다(14회). 다만 **12~13px 라벨과 24px 이상 숫자에만** 쓴다.

> **규칙: 같은 블록의 `font-size` 가 14~23px 인데 `font-weight: 900` 이면 700 으로 내린다.**

본문 크기 글자에 900이 붙으면 화면 전체가 굵어져 위계가 사라진다.

---

## 2. 색

### 2-1 원칙

1. **새 팔레트·새 hex 를 만들지 않는다.** `:root` 토큰에서만 고른다.
2. **한 화면이 전부 보라면 항목이 구분되지 않는다.** 갈래·카테고리·카드마다 휴를 나눈다.
3. **나눌 기준이 이미 데이터에 있으면 그것을 쓴다** — `category` · `tone` · 퀘스트 종류 등.
   화면에 `if` 분기를 두지 말고 `data-*` 속성이나 `:nth-child()` 로 **값에서 색을 고른다.**
4. **점과 면은 다른 문제다.** 범례 점(6px)에서 잘 읽히는 색이라도 화면 전체 면으로 깔면
   잠긴다. 앰버가 대표적이다 — 라운지 범례에서는 좋지만 상담 신청 화면 배경으로는 못 쓴다.
5. **한 지표의 순위를 그리는 막대는 단색 농도차가 맞다.** (예: 취업예측분석 기업별 합격률)
   갈래가 다를 때만 색을 나눈다.

### 2-2 잉크 규칙 ★ 가장 자주 걸린다

휴 **본색**은 흰 배경에서 글자로 못 쓰는 것이 많다. 면·점에는 본색, **글자·아이콘에는 잉크**.

| 토큰 | 흰 배경 대비 | 글자로 쓸 때 |
|---|---|---|
| `--mint` #20b486 | **2.65:1** ✗ | `--mint-ink` #148262 (4.85:1) |
| `--amber` #f5a524 | **1.9:1** ✗ | `--amber-ink` #9a6300 (5.5:1) |
| `--sky` #17a9e6 | **2.67:1** ✗ | 잉크가 없다 → `--blue` 를 쓴다 |
| `--coral` #ff6b6b | ✗ | `--coral-ink` #dc4444 |
| `--violet` #7c5cfc | 4.38:1 | 그대로 (앱의 바닥값) |
| `--blue` #356dff | 4.39:1 | 그대로 |
| `--violet-hover` #6848ec | 5.63:1 | 옅은 면 위 글자에 쓴다 |

`--color-success`(=`--mint`) · `--color-warning`(=`--amber`) 를 **글자색으로 쓰면 안 된다.**
이 실수가 여러 화면에 있었다.

### 2-3 버튼

- **실행 버튼(제출·예약·신청·저장)은 보라 하나로 간다.**
  같은 역할의 버튼이 화면마다 색이 다르면 다른 버튼으로 읽힌다.
- **선택·필터·보조 동작은 갈래 색**을 쓴다 (탭 · 카테고리 칩 · 온라인/오프라인 토글 등).
- ★ **보라 채움 + 흰 글자는 어떤 농도에서도 4.5:1 을 못 넘긴다** (본색조차 4.38:1).
  그래서 이 앱의 보라 버튼은 **큰 글씨 기준(18.66px 이상 굵게 → 3:1)** 에 기대고 있다.
  → **그런 버튼의 글씨를 20px/700 아래로 내리면 대비 근거가 깨진다.**
- 작은 버튼이 필요하면 **옅은 면 + 진한 글자** 꼴로 간다:
  `--violet-soft` 면 + `--violet-hover` 글자 = **4.9:1**, 크기와 무관하게 통과한다.
- 비활성·이용 불가는 **중립 회색**이다. 갈래 색을 쓰면 강조로 읽힌다.

### 2-4 확정된 갈래 색 사전

| 갈래 | 색 |
|---|---|
| **상담** 진로·취업 / 심리 / 지도교수 | 파랑+초록 / 보라 / 민트 |
| **진단** C-CORE / C-1·2 / C-3 / C-4·6 / C-5 | 보라 / 파랑 / 민트 / 앰버 / 핑크 (`--diagnosis-N`) |
| **비교과 카테고리** 진로 / 취업 / 어학 / 창업 / 자격증 / 기타 | 파랑 / 민트 / 앰버 / 코랄 / 보라 / 중립 |
| **일일퀘스트** TOEIC / 전공 / NCS·GSAT | 파랑 / 보라 / 앰버 (`--daily-N`) |
| **상태** 완료 / 진행중 / 신청완료·예정 / 잠금·불가 | 민트 / 앰버 / 파랑 / 회색 |

같은 갈래는 **앱 어디서나 같은 색**이어야 한다. 라운지 범례 · 상담 현황 유형별 분포 ·
상담 신청 화면이 서로 맞는지 확인하고 바꾼다.

---

## 3. 구조

- **제목·설명은 공용 머리글 한 곳** — 학생 `usePageHead`, 교직원 `usePageCrumbLeaf`.
  페이지가 `<h1>` 을 다시 두면 제목이 두 번 나온다.
- **빈 히어로 띠 금지.** 제목이 머리글로 옮겨간 뒤 배경·`min-height` 만 남은 띠가
  여러 화면에 있었다(178px). 버튼 한 줄로 접는다. 그라데이션·테두리를 되살리지 말 것.
- **같은 페이지 안에서 카드 열 비율을 통일한다.** (예: 8/4 · 7/5 가 섞여 좌우가 어긋났다)
- **좌우 여백·최대폭은 `.v2-main` / `.admin-page` 가 정한다.** 페이지가 컨테이너를 또 만들지 않는다.

---

## 4. 구현 패턴

```css
/* 부모에 변수를 두고 자식은 그것만 본다 — 갈래가 늘어도 규칙이 한 곳이다. */
.card            { --x-ink: var(--violet); --x-soft: var(--violet-soft); --x-line: var(--violet-border); }
.card.is-career  { --x-ink: var(--blue);   --x-soft: var(--blue-soft);   --x-line: var(--blue-border); }
```

- `--x-ink` 글자·테두리 / `--x-soft` 옅은 면 / `--x-line` 옅은 테두리
  (필요하면 `--x-ok` 처럼 뜻이 다른 자리를 하나 더 나눈다)
- **공용 컴포넌트**는 두 SPA 의 토큰 이름이 달라(`--color-navy` vs `--color-text`)
  파일 맨 위에서 한 번만 별칭으로 잇는다 → `JobBoard.css` · `JobDetailView.css` 참고.
- **집계·판정은 데이터층**이 한다. 화면은 코드만 넘긴다(`CLAUDE.md` 규칙 10·13).

---

## 5. 검증

```bash
npx tsc -b            # 빌드 게이트. tsc --noEmit 는 아무것도 검사하지 않는다
```

- 렌더 확인은 **gstack `/browse`** 로만 한다. `mcp__claude-in-chrome__*` 는 쓰지 않는다.
- **대비는 눈으로 판단하지 말고 계산한다.** 브라우저 콘솔에 붙여 쓰는 스니펫:

```js
const lum = c => { const [r,g,b] = c.map(v => { v/=255; return v<=0.03928 ? v/12.92 : Math.pow((v+0.055)/1.055, 2.4) }); return 0.2126*r + 0.7152*g + 0.0722*b }
const ratio = (a,b) => { const L1=lum(a), L2=lum(b), [h,l] = L1>L2 ? [L1,L2] : [L2,L1]; return +((h+0.05)/(l+0.05)).toFixed(2) }
const hex = h => [1,3,5].map(i => parseInt(h.slice(i,i+2),16))
ratio(hex('#20b486'), [255,255,255])   // 2.65 → 글자로 못 쓴다
```

기준: 본문 **4.5:1** / 큰 글씨(18.66px 이상 굵게 또는 24px 이상) **3:1**.

---

## 6. 이 규칙이 나온 커밋

| 커밋 | 내용 |
|---|---|
| `ad6ec1e` | 서브페이지 37개 타이포 정비(크기 322건·굵기 138건), 진단센터·상담센터 갈래색, C-CORE 차트 그라데이션 |
| `7bbe528` · `ecb3a4b` · `6a52dd3` | 진단센터 버튼 꼴 확정 — 옅은 면 + 테두리, 라운지 C-3 배합 |
| `b3cd0a4` | 상단 네비게이션이 이동 후에도 펼쳐져 있던 문제 (`:hover` + `:focus-within` 두 원인) |
| `da70c96` | 홈대시보드 카드별 색, 열 폭 정렬 |
| `7d0dcd3` | 비교과 카테고리색 · 일일퀘스트 3종색 · 프로그램 현황 지표색 · 성장일지 필터 칩 |

---

## 7. 아직 안 본 것

색을 아직 손대지 않은 화면: `SkillTree` · `RoadmapStatus` · `Portfolio` · `AiResume` ·
`AiConsulting` · `Attendance` · `Prediction`.
조사해 보니 이들은 **이미 3~4색을 쓰고 있어 급하지 않다.** 손댈 때도 위 규칙을 그대로 적용한다.

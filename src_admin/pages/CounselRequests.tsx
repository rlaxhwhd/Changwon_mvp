import { useMemo, useState } from 'react'
import { Link } from 'react-router-dom'
import AdminModal from '../components/AdminModal'
import { getActiveCounselor, getCounselorById, getReassignableCounselors } from '../data/counselors'
import { confirmRequest, formatRelativeTime, getCounselStudentProfile, getRequestsByAssignee, reassignRequest, rejectRequest, rescheduleRequest } from '../data/counselRequests'
import type { CounselRequest, CounselRequestStatus, CounselSlot } from '../data/counselRequests'
import type { CounselRequestType } from '../data/schema/counselRequest'
import { enrollStatusClass } from '../data/studentRoster'

type RequestTab = '전체' | CounselRequestStatus
type ModalTab = 'schedule' | 'reassign'
const TABS: RequestTab[] = ['전체', '대기', '확정', '완료', '취소']
const WEEKDAYS = ['일', '월', '화', '수', '목', '금', '토']
const ROADMAP_STATUS: Record<string, string> = { done: '완료', active: '진행 중', upcoming: '예정' }

const pad = (value: number) => String(value).padStart(2, '0')
const dateKey = (date: Date) => `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())}`
function requestDate(req: CounselRequest) { if (req.slot?.date) return req.slot.date; const date = new Date(req.requestedAt); return Number.isNaN(date.getTime()) ? req.requestedAt.slice(0, 10) : dateKey(date) }
function requestTime(req: CounselRequest) { if (req.slot?.start) return req.slot.start; const date = new Date(req.requestedAt); return Number.isNaN(date.getTime()) ? req.requestedAt.slice(11, 16) : `${pad(date.getHours())}:${pad(date.getMinutes())}` }
function formatSelectedDate(value: string) { const [year, month, day] = value.split('-').map(Number); return `${year}.${pad(month)}.${pad(day)} (${WEEKDAYS[new Date(year, month - 1, day).getDay()]})` }
function statusClass(status: CounselRequestStatus) { return status === '대기' ? 'is-waiting' : status === '확정' ? 'is-confirmed' : status === '완료' ? 'is-complete' : 'is-cancelled' }
function requestTypeLabel(req: CounselRequest) { return req.type === '심리' ? '심리상담' : '진로취업 상담' }
function calendarDays(month: Date) { const first = new Date(month.getFullYear(), month.getMonth(), 1); const start = new Date(month.getFullYear(), month.getMonth(), 1 - first.getDay()); return Array.from({ length: 42 }, (_, index) => { const current = new Date(start); current.setDate(start.getDate() + index); return { key: dateKey(current), day: current.getDate(), inMonth: current.getMonth() === month.getMonth() } }) }

function ScheduleAction({ request, onClose }: { request: CounselRequest; onClose: () => void }) {
  const [date, setDate] = useState(request.slot?.date ?? requestDate(request)); const [start, setStart] = useState(request.slot?.start ?? requestTime(request)); const [end, setEnd] = useState(request.slot?.end ?? ''); const [place, setPlace] = useState(request.slot?.place ?? '')
  const valid = Boolean(date && start && end && start < end)
  const save = () => { if (!valid) return; const slot: CounselSlot = { date, start, end, place: place.trim() || undefined }; request.status === '확정' ? rescheduleRequest(request.id, slot) : confirmRequest(request.id, slot); window.location.reload() }
  return <div className="counsel-modal-action"><div className="counsel-request-detail-grid"><label><span>날짜</span><input type="date" value={date} onChange={e => setDate(e.target.value)} /></label><label><span>시작</span><input type="time" value={start} onChange={e => setStart(e.target.value)} /></label><label><span>종료</span><input type="time" value={end} onChange={e => setEnd(e.target.value)} /></label><label className="is-wide"><span>장소 / 링크</span><input value={place} onChange={e => setPlace(e.target.value)} /></label></div><div className="counsel-request-detail-actions">{request.status === '대기' && <button type="button" className="counsel-outline-btn is-danger" onClick={() => { rejectRequest(request.id); window.location.reload() }}>신청 취소</button>}<button type="button" className="counsel-outline-btn" onClick={onClose}>닫기</button><button type="button" className="counsel-primary-btn" disabled={!valid} onClick={save}>{request.status === '확정' ? '일정 변경' : '상담 확정'}</button></div></div>
}

function ReassignAction({ request, onClose }: { request: CounselRequest; onClose: () => void }) {
  const candidates = getReassignableCounselors(request.assignedCounselorId ?? ''); const [selectedId, setSelectedId] = useState(candidates[0]?.id ?? '')
  if (!candidates.length) return <div className="counsel-modal-action"><p className="counsel-request-detail-empty">재배정할 다른 상담사가 없습니다.</p></div>
  return <div className="counsel-modal-action"><div className="counsel-request-detail-grid"><label className="is-wide"><span>재배정할 상담사</span><select value={selectedId} onChange={e => setSelectedId(e.target.value)}>{candidates.map(c => <option key={c.id} value={c.id}>{c.name} · {c.roleLabel}</option>)}</select></label></div><div className="counsel-request-detail-actions"><button type="button" className="counsel-outline-btn" onClick={onClose}>닫기</button><button type="button" className="counsel-primary-btn" disabled={!selectedId} onClick={() => { reassignRequest(request.id, selectedId); window.location.reload() }}>재배정</button></div></div>
}

function RequestModal({ request, initialTab, onClose }: { request: CounselRequest; initialTab: ModalTab; onClose: () => void }) {
  const [actionTab, setActionTab] = useState<ModalTab>(initialTab); const profile = getCounselStudentProfile(request.studentId); if (!profile) return null
  const canAct = request.status === '대기' || request.status === '확정'; const canReassign = request.status === '확정'; const slot = request.slot; const assignee = getCounselorById(request.assignedCounselorId)
  return <AdminModal title="학생 상세 정보" onClose={onClose} size="lg"><div className="counsel-profile-heading"><div><strong>{profile.name}</strong><span>{profile.studentNo}</span></div><span className={enrollStatusClass(profile.enrollmentStatus)}>{profile.enrollmentStatus}</span></div><section className="counsel-modal-section"><h3>학생 기본 정보</h3><dl className="counsel-profile-grid"><div><dt>학과 · 학년</dt><dd>{profile.major}</dd></div><div><dt>휴대폰</dt><dd>{profile.phone}</dd></div><div><dt>성적</dt><dd>{profile.gpa}</dd></div><div><dt>어학</dt><dd>{profile.language}</dd></div><div className="is-wide"><dt>목표 기업</dt><dd>{profile.targetCompanySummary}</dd></div></dl></section><section className="counsel-modal-section"><h3>진단 유형 · IAP</h3><dl className="counsel-profile-grid"><div><dt>학생 유형</dt><dd>{profile.studentType}</dd></div><div><dt>IAP</dt><dd>{profile.iap.iapType} · {profile.iap.label}</dd></div></dl></section><section className="counsel-modal-section"><h3>이번 상담 요청</h3><dl className="counsel-profile-grid"><div><dt>상담 유형</dt><dd><span className={`counsel-type-badge ${request.type === '심리' ? 'is-psych' : ''}`}>{requestTypeLabel(request)}</span></dd></div><div><dt>상태</dt><dd><span className={`counsel-status-badge ${statusClass(request.status)}`}>{request.status}</span></dd></div><div className="is-wide"><dt>주제</dt><dd>{request.topic}</dd></div><div><dt>신청 일시</dt><dd>{formatRelativeTime(request.requestedAt)} 신청</dd></div><div><dt>일정</dt><dd>{slot ? `${slot.date} ${slot.start}–${slot.end}${slot.place ? ` · ${slot.place}` : ''}` : '미확정'}</dd></div><div className="is-wide"><dt>담당 상담사</dt><dd>{assignee?.name ?? '미배정'}</dd></div></dl></section><section className="counsel-modal-section"><h3>로드맵</h3>{profile.detailed ? <ol className="counsel-roadmap-phases">{profile.detailed.phases.map(phase => <li key={phase.num}><strong>{phase.num}단계 {phase.title}</strong><span>{ROADMAP_STATUS[phase.status] ?? phase.status} · {phase.period}</span></li>)}</ol> : <p className="counsel-roadmap-summary">{profile.roadmapSummary}</p>}</section>{canAct && <section className="counsel-modal-actions">{canReassign && <div className="counsel-modal-action-tabs" role="tablist"><button type="button" role="tab" aria-selected={actionTab === 'schedule'} className={actionTab === 'schedule' ? 'active' : ''} onClick={() => setActionTab('schedule')}>일정 변경</button><button type="button" role="tab" aria-selected={actionTab === 'reassign'} className={actionTab === 'reassign' ? 'active' : ''} onClick={() => setActionTab('reassign')}>재배정</button></div>}{actionTab === 'reassign' && canReassign ? <ReassignAction request={request} onClose={onClose} /> : <ScheduleAction request={request} onClose={onClose} />}</section>}{request.status === '완료' && <Link to="/counsel/records" className="counsel-record-link">기록 보기</Link>}</AdminModal>
}

export default function CounselRequests() {
  const counselor = getActiveCounselor(); const all = useMemo(() => getRequestsByAssignee(counselor.id), [counselor.id]); const initialKey = useMemo(() => { const frequency = new Map<string, number>(); all.forEach(req => frequency.set(requestDate(req), (frequency.get(requestDate(req)) ?? 0) + 1)); return [...frequency.entries()].sort((a,b) => b[1] - a[1] || b[0].localeCompare(a[0]))[0]?.[0] ?? dateKey(new Date()) }, [all])
  const [tab, setTab] = useState<RequestTab>('전체'); const [query, setQuery] = useState(''); const [typeFilter, setTypeFilter] = useState<'전체' | CounselRequestType>('전체'); const [selectedDate, setSelectedDate] = useState(initialKey); const [month, setMonth] = useState(() => { const [year, value] = initialKey.split('-').map(Number); return new Date(year, value - 1, 1) }); const [expandedTopics, setExpandedTopics] = useState<Set<string>>(() => new Set()); const [modalRequest, setModalRequest] = useState<CounselRequest | null>(null); const [modalTab, setModalTab] = useState<ModalTab>('schedule')
  const counts = useMemo(() => { const result: Record<RequestTab, number> = { 전체: all.length, 대기: 0, 확정: 0, 완료: 0, 취소: 0 }; all.forEach(req => result[req.status] += 1); return result }, [all]); const dateCounts = useMemo(() => { const result = new Map<string, number>(); all.forEach(req => result.set(requestDate(req), (result.get(requestDate(req)) ?? 0) + 1)); return result }, [all]); const days = useMemo(() => calendarDays(month), [month]); const list = useMemo(() => all.filter(req => requestDate(req) === selectedDate).filter(req => tab === '전체' || req.status === tab).filter(req => typeFilter === '전체' || req.type === typeFilter).filter(req => !query.trim() || req.studentName.toLowerCase().includes(query.trim().toLowerCase()) || req.studentNo.toLowerCase().includes(query.trim().toLowerCase())).sort((a,b) => requestTime(a).localeCompare(requestTime(b))), [all, selectedDate, tab, typeFilter, query])
  const updateSelectedDate = (value: string) => { setSelectedDate(value); const [year, valueMonth] = value.split('-').map(Number); setMonth(new Date(year, valueMonth - 1, 1)) }; const openModal = (request: CounselRequest, nextTab: ModalTab = 'schedule') => { setModalRequest(request); setModalTab(nextTab) }; const toggleTopic = (id: string) => setExpandedTopics(current => { const next = new Set(current); next.has(id) ? next.delete(id) : next.add(id); return next })
  const downloadCsv = () => { const header = ['상담시간','학생명','학과','상담유형','상담주제','상태']; const rows = list.map(req => [requestTime(req), req.studentName, req.studentMajor, requestTypeLabel(req), req.topic, req.status]); const csv = [header, ...rows].map(row => row.map(value => `"${String(value).replaceAll('"', '""')}"`).join(',')).join('\n'); const url = URL.createObjectURL(new Blob([`\uFEFF${csv}`], { type: 'text/csv;charset=utf-8' })); const anchor = document.createElement('a'); anchor.href = url; anchor.download = `상담신청_${selectedDate}.csv`; anchor.click(); URL.revokeObjectURL(url) }
  return <div className="admin-page counsel-requests-page"><header className="admin-page-head"><div><h1 className="admin-page-title">신청 접수함</h1><p className="admin-page-desc">학생들이 신청한 상담 요청을 확인하고 관리할 수 있습니다.</p></div></header><form className="counsel-filter-bar" onSubmit={event => event.preventDefault()}><label className="counsel-filter-search"><i className="fa-solid fa-magnifying-glass" aria-hidden="true" /><input value={query} onChange={event => setQuery(event.target.value)} placeholder="이름, 학번 검색" aria-label="이름 또는 학번 검색" /></label><select className="counsel-filter-select" value={typeFilter} onChange={event => setTypeFilter(event.target.value as '전체' | CounselRequestType)} aria-label="상담 유형"><option value="전체">전체 유형</option><option value="진로취업">진로취업 상담</option><option value="심리">심리상담</option></select><input type="date" className="counsel-filter-date" value={selectedDate} onChange={event => updateSelectedDate(event.target.value)} aria-label="상담 신청 날짜" /><button type="submit" className="counsel-primary-btn"><i className="fa-solid fa-magnifying-glass" /> 검색</button><button type="button" className="counsel-outline-btn" onClick={() => { setQuery(''); setTypeFilter('전체'); setTab('전체'); updateSelectedDate(initialKey) }}><i className="fa-solid fa-rotate-left" /> 초기화</button></form><div className="counsel-status-tabs" role="tablist" aria-label="상담 신청 상태">{TABS.map(item => <button key={item} type="button" role="tab" aria-selected={tab === item} className={tab === item ? 'active' : ''} onClick={() => setTab(item)}><span>{item}</span><strong>{counts[item]}</strong></button>)}</div><div className="counsel-requests-layout"><section className="counsel-calendar-card" aria-label="상담 신청 달력"><h2>상담 신청 캘린더</h2><div className="counsel-calendar-toolbar"><div className="counsel-calendar-nav"><button type="button" aria-label="이전 달" onClick={() => setMonth(current => new Date(current.getFullYear(), current.getMonth() - 1, 1))}><i className="fa-solid fa-chevron-left" /></button><button type="button" aria-label="다음 달" onClick={() => setMonth(current => new Date(current.getFullYear(), current.getMonth() + 1, 1))}><i className="fa-solid fa-chevron-right" /></button></div><strong>{month.getFullYear()}년 {month.getMonth() + 1}월</strong><button type="button" className="counsel-today-btn" onClick={() => { const today = new Date(); setMonth(new Date(today.getFullYear(), today.getMonth(), 1)); setSelectedDate(dateKey(today)) }}>오늘</button></div><div className="counsel-calendar-weekdays">{WEEKDAYS.map(day => <span key={day}>{day}</span>)}</div><div className="counsel-calendar-grid">{days.map(day => { const count = dateCounts.get(day.key) ?? 0; return <button key={day.key} type="button" className={`${day.inMonth ? '' : 'is-muted'}${day.key === selectedDate ? ' is-selected' : ''}`} onClick={() => updateSelectedDate(day.key)}><span>{day.day}</span>{count > 0 && <em>{count}</em>}</button> })}</div><p className="counsel-calendar-legend"><span /> 해당 날짜의 상담 신청 건수</p></section><section className="counsel-request-table-card"><header className="counsel-request-table-head"><h2>{formatSelectedDate(selectedDate)} <span>상담 신청 목록</span> <em>{list.length}건</em></h2><div><button type="button" className="counsel-outline-btn"><i className="fa-solid fa-filter" /> 필터</button><button type="button" className="counsel-outline-btn" onClick={downloadCsv}><i className="fa-solid fa-download" /> 엑셀 다운로드</button></div></header><div className="counsel-request-table-scroll"><div className="counsel-request-table"><div className="counsel-request-columns" aria-hidden="true"><span>신청 시간</span><span>학생 정보</span><span>학번</span><span>상담 유형</span><span>상담 주제</span><span>상태/담당</span><span>관리</span></div>{list.length === 0 ? <div className="counsel-request-empty"><i className="fa-regular fa-calendar-xmark" /><strong>선택한 날짜에 상담 신청이 없습니다.</strong><span>다른 날짜 또는 상태를 선택해 주세요.</span></div> : list.map(req => { const expandable = req.topic.length > 15; const expanded = expandedTopics.has(req.id); return <div className="counsel-request-row-wrap" key={req.id}><article className="counsel-request-row"><div className="counsel-request-time"><strong>{requestTime(req)}</strong><small>{formatRelativeTime(req.requestedAt)} 신청</small></div><div className="counsel-request-person"><span><strong>{req.studentName}</strong><span className={enrollStatusClass(req.studentEnrollmentStatus)}>{req.studentEnrollmentStatus}</span><small>{req.studentMajor}</small></span></div><div className="counsel-request-studentid">{req.studentNo}</div><div><span className={`counsel-type-badge ${req.type === '심리' ? 'is-psych' : ''}`}>{requestTypeLabel(req)}</span></div>{expandable ? <button type="button" className="counsel-request-topic-cell is-expandable" aria-expanded={expanded} onClick={() => toggleTopic(req.id)}>{expanded ? req.topic : `${req.topic.slice(0, 15)}…`}<i className={`fa-solid fa-chevron-${expanded ? 'up' : 'down'}`} aria-hidden="true" /></button> : <p className="counsel-request-topic-cell">{req.topic}</p>}<div className="counsel-request-status-cell"><span className={`counsel-status-badge ${statusClass(req.status)}`}>{req.status}</span><span className="counsel-assignee-tag"><i className="fa-solid fa-user-tie" /> {getCounselorById(req.assignedCounselorId)?.name ?? '미배정'}</span></div><div className="counsel-request-manage"><button type="button" className="counsel-detail-btn" onClick={() => openModal(req)}>상세 보기</button>{(req.status === '대기' || req.status === '확정') && <button type="button" className="counsel-detail-btn" onClick={() => openModal(req, 'reassign')}>재배정</button>}<button type="button" className="counsel-more-btn" aria-label={`${req.studentName} 상담 더보기`}><i className="fa-solid fa-ellipsis-vertical" /></button></div></article>{expandable && expanded && <div className="counsel-topic-full">{req.topic}</div>}</div> })}</div></div><footer className="counsel-request-table-footer"><span>총 {list.length}건</span><button type="button">10개씩 보기 <i className="fa-solid fa-chevron-down" /></button><nav aria-label="상담 신청 페이지"><button type="button" disabled><i className="fa-solid fa-chevron-left" /></button><button type="button" className="active">1</button><button type="button" disabled><i className="fa-solid fa-chevron-right" /></button></nav></footer></section></div>{modalRequest && <RequestModal request={modalRequest} initialTab={modalTab} onClose={() => setModalRequest(null)} />}</div>
}

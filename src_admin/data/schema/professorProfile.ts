/**
 * ---------------------------------------------------------------------------
 * 단일소스: 교수 상담 노출 프로필 스키마 → 향후 교수상담 프로필 테이블.
 * DB 전환 시 필드 매핑만 교체한다. 화면 로컬 상태를 저장하지 말 것.
 * ---------------------------------------------------------------------------
 */
export interface ProfessorCounselProfile {
  /** 로그인 신원과 공유하는 교수 pool 식별자(INTG_UID). */
  professorId: string
  /** true일 때만 학생 교수 목록에 노출된다. */
  accept: boolean
  /** 학생에게 노출할 면담 가능 시간 요약. */
  officeHours: string
  /** 학생에게 노출할 상담 소개문. */
  intro: string
  /** override가 마지막으로 갱신된 ISO 시각. */
  updatedAt?: string
}

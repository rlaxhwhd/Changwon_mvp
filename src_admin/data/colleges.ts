// ─────────────────────────────────────────────────────────────────────────
// (호환 재노출) 학과 → 소속 대학 매핑은 학과 트리 단일소스로 통합됐다.
// 실제 데이터는 departments.seed.json(V_DEP_INF_ALL 미러), 로더는 departments.ts.
// 신규 코드는 './departments' 에서 직접 import 할 것 — 이 파일은 기존 호출부 유지용이다.
// ─────────────────────────────────────────────────────────────────────────
export { collegeOf } from './departments'

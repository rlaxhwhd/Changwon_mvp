# SSO 도입 전 학생 로그인

## 명시적 로컬 테스트 학생 (2026-09-11 추가)

- 김채원: `20990001` / `!`, 기존 내부 ID와 alias `chaewon` 유지.
- 김창원: `20990002` / `!`, 기존 내부 ID와 alias `changwon` 유지.
- 063·064에서 `dc.fixture_student_login`에 두 계정만 등록했다. 기존 상담·진단·로드맵 이력의 연결 키를 바꾸지 않고 서비스 학번만 변경했다.
- 원본 학사 미러에 추가하지 않는다. 학사 로그인 조회에 없는 학번일 때만 명시적으로 등록된 fixture 계정을 확인한다. 향후 학사 미러에 같은 학번이 들어오면 fixture 경로는 차단된다.
- 이지우 학번 `20261137`의 실제 학사 인원 충돌 문제는 이 두 계정 등록으로 해결되지 않으며 별도 정리가 필요하다.

2026-09-11 사용자 요청으로 학사 복제본의 학생 학번과 공통 비밀번호 `!`를 사용하는 임시 로그인을 연결했다. 기존 교직원 개발 로그인과 구분하며 운영 SSO 구현 완료를 뜻하지 않는다.

- 로그인 화면: `/login`, 성공하면 `/v2/main`.
- 요청 학생: **20180001 / 홍길동 / 공과대학 / 전자공학과**, 조직 코드 **5 / 714**.
- 원본에 이미 존재하는 학번이므로 원본 이름·소속을 수정하지 않고 `dc.student_login_override`에 요청한 로컬 정보를 명시했다. `dc.person`·`dc.student`에도 저장되어 있다. 원본 Oracle과 `academic.v_usr_inf`에는 쓰지 않았다.
- `dc.student_login_source`는 `academic.v_usr_inf`의 학생 신분 코드 `1101`, `1102`, `1201`, `1202`만 포함한다. 사용자 요청대로 재학뿐 아니라 이 학번 집합 전체를 허용한다. 교수·직원·조교 코드는 학생 로그인에서 제외한다.
- 입력 학번을 `LOGIN_ID`(없으면 `INTG_UID`) 또는 `INTG_UID`로 확인한다. 일치하는 원본이 없거나 중복·모호하면 로그인하지 않는다. 원본에 없는 학번은 프론트에 추가해도 로그인할 수 없다.
- 첫 로그인 시 서비스용 학생 행을 생성한다. 기존 학생 프로필·상담·진단 이력은 덮어쓰지 않으며 진단 유형·GPA·로드맵 결과를 만들어 넣지 않는다. 조직 코드는 서비스에 존재하는 정확한 단대/학과 쌍만 연결한다.
- `POST /api/v1/auth/student/login` 입력: `{ "studentNo": "20180001", "password": "!" }`. 서버가 비밀번호와 원본 학생 여부를 확인한다. 개발 환경과 기존 개발 프록시 토큰이 필요하다.
- 로그인하면 8시간 HttpOnly·SameSite=Lax 쿠키를 발급하고 DB에는 무작위 토큰의 SHA-256 해시만 저장한다. HTTPS에서는 Secure 쿠키를 사용한다. `GET /auth/student/me`로 본인 정보를 복구하고 `POST /auth/student/logout`으로 세션을 폐기한다.
- 학생 포털은 `X-DC-Portal: student`와 쿠키를 사용한다. localStorage의 학생 ID는 화면 선택값일 뿐 인증 근거가 아니다. 학사/로컬 학생은 개발 ID 헤더만으로 접근할 수 없다. 교직원 포털은 기존 개발 인증을 유지한다.
- 학생 세션이 없거나 만료되면 `/v2` 진입은 로그인 화면으로 돌아간다. 원본 DB 연결 없이 로컬 복제본만 사용한다. 학사 복제 자체의 주기 실행과 SSO는 별도 작업이다.
- 마이그레이션 059(조회 뷰·세션), 060(요청 학생 등록), 061(첫 로그인 등록 권한). 적용한 파일은 수정하지 않는다.
- 검증: 별도 테스트 DB의 잘못된 자격증명·헤더 위조·학생 범위·세션 만료·로그아웃·최초 등록 테스트 4개와 기존 API·교직원·심리상담 회귀 검증을 포함해 17개 통과. 빈 DB의 마이그레이션 61개 및 재실행 통과. Chrome MCP에서 실제 로그인과 학생 메인 화면 확인.

향후 SSO 전환 시 학번 식별과 학생 프로필을 재사용하고 임시 공통 비밀번호 로그인 발급 경로를 비활성화한다.

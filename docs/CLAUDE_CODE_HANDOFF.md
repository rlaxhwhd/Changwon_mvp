# dreamcatch_pr — Claude Code 작업 인수인계

작성일: 2026-09-15 (KST)

## 범위와 근거

사용자가 지정한 `C:\Users\desig\Desktop\dreamcatch_pr`의 로컬 Claude Code 기록만 읽어 정리했다. Codex 사이드바에 과거 대화방을 생성하거나 전체 계정 기록을 가져온 것은 아니다.

원본 기록 위치: `C:\Users\desig\.claude\projects\C--Users-desig-Desktop-dreamcatch-pr`

- `433945f1-319d-4deb-8c75-1ee551a07a63.jsonl`: 사용자·어시스턴트 텍스트 메시지 150개. 확인된 대화 기간 2026-09-08~09-09. 파일 수정일은 09-15이나 마지막 텍스트 대화는 09-09이다.
- `d4a113ba-5bce-44d7-8291-2ebed4916960.jsonl`: 텍스트 메시지 106개. 앞 기록과 대화가 중복되며, 마지막 작업은 프로그램 상세의 중간 폭 레이아웃 수정이다.
- `memory/MEMORY.md`, `memory/dreamcatch-project-setup.md`: 프로젝트 위치·브랜치·실행 방법.

사용자의 요청 내용과 최근 완료 보고를 읽고, 주요 결과를 현재 파일·Git 기록과 대조했다. 과거 어시스턴트의 검증 보고는 이번에 새로 실행한 검증과 구분한다. 과거 대화의 실행 명령은 현재의 새 작업 지시로 취급하지 않는다.

## 현재 위치

- 국립창원대학교 드림캐치 리뉴얼 프론트엔드. React 19 / TypeScript 5.9 / Vite 8.
- 작업 브랜치: `design`.
- HEAD: `2d1ec37fcc9d4dc852f14cde5ebd42706e78726a`.
- 마지막 커밋: `design: 학생 포털 반응형 정비 + AI 맞춤채용·비교과 현황 리디자인`.
- 실제 Git 통계: 39개 파일, 2,177줄 추가 / 1,492줄 삭제. 과거 완료 보고의 숫자 대신 Git 결과를 기준으로 한다.
- 인수인계 문서 작성 전 추적 파일 변경 없음. 로컬 `origin/design` 참조와 HEAD가 같음. 원격 서버를 새로 조회한 것은 아니다.
- 마지막 사용자 요청은 권한을 받은 뒤 푸시 재시도. 09-09 대화에서 푸시 성공으로 마무리됐다. 미완료 상태로 남은 마지막 수정 요청은 발견하지 못했다.

## 진입점과 문서

| 대상 | 코드 / 문서 |
|---|---|
| 랜딩 `/` | `src_landing/` |
| 학생 `/v2/main`, `/v2/lounge` 등 | `src_v2/` |
| 교직원 `/admin` | `src_admin/` |
| 실행 / 빌드 | `npm run dev` / `npm run build` (`tsc -b && vite build`) |
| 공통 개발 규칙 | `CLAUDE.md`, `src_v2/CLAUDE.MD` |
| 현행 운영 사실 | `CURRENT.md` |
| 확정 프로세스 | `PROCESS.md` |
| 화면·필드·코드 명세 | `SPEC.md` |
| DB 설계·정책 미결 | `DB.md` §9 |
| 디자인 | `DESIGN.md`, `src_v2/DESIGN.md`, `수정.md` 및 실제 공용 CSS |

본 문서는 작업 기록의 길잡이다. 명세의 정본을 대체하지 않는다.

## 최근 완료한 작업

### 학생 포털 반응형

- `/v2/main`: 원본 HTML 시안의 반응형 이식, career-hero 폭·장식 크기 보정, 핵심역량 정렬.
- GNB: 모바일 서브링크 디자인, 점 마커 제거, 드롭다운 잘림·중간 폭 배치 수정. 사용자가 직접 수정한 프로필 버튼도 최종 커밋에 포함됨.
- 진로 여정 단계 연결선·폭·스크롤 수정.
- `/v2/lounge`: 시안 반응형 적용, 지표 카드 간격 및 성장 활동 기록 타이포 정리. 사용자의 `AiLounge.tsx` 변경도 포함됨.
- 공통 페이지 머리글 `.v2-page-head` 아래 여백 28px로 통일. 바로 다음 요소의 상단 여백 중복 제거.
- 진단·상담 페이지의 중복 컨테이너 패딩 제거 및 좁은 화면 탭·상담 현황 정비.
- 상담 예약: 640px 이하에서 요일 칩 → 시간 칩 선택 UI. 진로취업·심리·교수 상담에 적용.
- 상담 일정표 머리는 768px 이하에서 제목과 주 이동 버튼을 두 줄로 배치. 선택한 일정 제목 앞 아이콘 제거.
- 비교과 목록 카드의 모집기간·진행기간·정원 아이콘을 동일 규격 SVG 라인 아이콘으로 통일.
- 프로그램 상세 배너의 1180~640px 구간 및 모바일 배치 수정.
- 채용공고·외부채용공고 목록의 검색 필터, 정렬 선택, 모바일 카드 배치 정비.

### AI 맞춤채용 `/v2/jobs/joblist`

파일: `src_v2/pages/roadmap/AiJobs.tsx`, `AiJobs.css`.

- 기존 카드·버튼·색상 토큰에 맞춰 리디자인하고 반응형 적용.
- 이 페이지의 공통 카드 머리글 최소 높이를 0으로 재정의.
- 반복되는 카드별 매칭률 링은 배지로 변경. 회사명 옆에 매칭률 배치.
- 최종 순서: 회사명·매칭률 → 직무명 → 기업종류·지역·경력 등 태그 → 상세 정보 → 지원하기.
- 하트는 카드 우측 상단, 지원하기는 본문 아래. 요약 카드의 준비율 링은 유지.

### 비교과 현황 `/v2/mypage/programs`

파일: `src_v2/pages/mypage/MyPrograms.tsx`, `MyPrograms.css`.

- 공용 카드·버튼·배지와 토큰 색상에 맞춰 요약, 필터, 목록, 모달 리디자인.
- 좁은 화면에서 목록 1열, 요약 3열 또는 2열, 신청 버튼 전폭 배치.
- 상태 표시의 점 제거. 수료증·다운로드 아이콘은 SVG 라인 아이콘.
- 분류 배지: 투명 배경, 회색 테두리·글자. 카드와 모달에만 적용.
- 카드 수료증 버튼의 아이콘·글자 gap은 4px. 공용 버튼 전체를 바꾸지 않음.

## 이어받을 작업 원칙

- 시안과 현재 디자인 언어를 존중하고, 요청한 페이지나 요소에만 스타일 변경을 한정한다.
- 사용자가 직접 수정한 파일 내용을 보존한다. 변경 직전 현재 파일과 Git 차이를 다시 확인한다.
- 원본 시안으로 사용한 파일은 `C:\Users\desig\Desktop\김주영_작업폴더\창원대학교_2차\main.html`, `stu_dash.html`. 이번 인수인계에서 이 외부 시안 파일을 다시 읽지는 않았다.
- SVG 라인 아이콘 전환은 요청된 요소에 적용한 결정이며 모든 Font Awesome을 전면 교체하라는 지시는 아니다.
- 복구 가능한 상담 UI 스위치: `src_v2/hooks/useSlotPicker.ts`의 `MOBILE_SLOT_PICKER`. 현재 `true`; `false`면 기존 표로 돌아간다.
- 데이터 원칙은 seed JSON + localStorage 오버레이. 상태 변경과 이력은 정해진 데이터 계층을 사용한다.
- 소스 변경 시 타입 게이트는 `npx tsc -b`. 루트 설정상 `tsc --noEmit`만으로는 충분하지 않다.

## 남은 사항과 주의할 불일치

1. **비교과 현황 데이터 연결**: `MyPrograms.tsx`에 `const PROGRAMS` 하드코딩 데이터가 현재도 존재한다. 과거 작업은 디자인 범위였으며 학생 JSON·`dc_program_apply` 연동은 별도 과제로 남겼다. 이번에 구현하지 않았다.
2. **상담 예약 반응형 경계 확인**: 과거 보고에서 동일 창을 640px 경계 너머로 리사이즈할 때 실시간 전환은 미검증이라고 명시했다. 현재 코드는 `matchMedia` 변경 리스너를 사용한다. 이번에도 브라우저 재검증은 하지 않았다.
3. **프로그램 상세 모바일 고정 신청 버튼**: 과거 어시스턴트가 제안만 했고 사용자 채택 기록은 없다. 확정 요구사항으로 간주하지 않는다.
4. **문서의 데스크톱 전용 표기는 오래됨**: 루트 `CLAUDE.md`와 Claude 메모리에는 데스크톱 전용이라고 쓰였으나, 09-08~09-09 사용자 요청과 현재 코드에는 모바일·태블릿 대응이 분명히 있다. 반응형을 제거하지 않는다.
5. **로드맵 완료 조건 문서 충돌**: `CLAUDE.md` 이벤트 표에는 프로그램 선발 시 자동 완료라고 남아 있으나, `PROCESS.md` §6-4와 `SPEC.md`의 후속 확정안은 수료 시 완료다. 관련 구현을 할 때 정본을 기준으로 대조한다.
6. **참조 문서 일부 없음**: 현재 체크아웃에 `AGENTS.md`, `.ai/interop.md`, 루트 `UI.md`가 없다. `CLAUDE.md`에서 참조한다고 해서 실제 파일·도구가 있다고 가정하지 않는다.
7. **정책 미결**: 진단 문항·판정식, STAR 진단 재응시/연계, 로드맵 재생성 정책 등은 `DB.md` §9에서 확인한다. 미결 번호 일부가 중복돼 있어 번호와 항목명을 함께 확인한다. 임의로 확정하지 않는다.
8. 과거 개발 서버 종료 설명은 당시 Claude 미리보기 환경에 대한 보고다. 현재 서버 실행 여부·포트 점유·종료 원인은 이번에 확인하지 않았다.

## 이번 확인의 한계

- 인수인계 문서만 작성했다. 앱 소스·기존 명세·Claude 원본 로그·설정은 수정하지 않았다.
- 과거 기록에는 TypeScript·ESLint 및 여러 폭의 브라우저 검증 성공이 보고돼 있다. 이번에는 코드와 Git의 정적 대조만 했으며 빌드·린트·브라우저 검증을 새로 실행하지 않았다.
- 원격 푸시·배포·설정 이관·다른 프로젝트 대화 가져오기는 수행하지 않았다.

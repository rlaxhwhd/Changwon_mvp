# SPEC.md — 화면·기능 명세 <span>(무엇을 만들 것인가 + 지금 어디까지 됐나)</span>

> **이 문서가 답하는 것**
>
> | 질문 | 절 |
> |---|---|
> | 어떤 화면·기능이 필요한가, 지금 상태는 | **§3** |
> | 누가 어떤 학생 데이터를 볼 수 있는가 | §2 |
> | 어떻게 만드는가 (React 규약) | §4 · §5 |
> | 필드명·상태값을 무엇으로 쓰는가 | §6 필드 사전 · §7 코드 체계 ← **일관성의 핵심** |
> | 대학원생을 어떻게 여는가 | §8 |
> | 아직 못 정한 것 | §9 → 대장은 `DB.md` §9 |
>
> **이 문서에 없는 것**
>
> | 내용 | 문서 |
> |---|---|
> | 현행 시스템의 사이트맵·기능·권한·DB구조 | **`CURRENT.md`** |
> | 진단→상담→로드맵 프로세스·게이팅·6유형·24주제 | **`PROCESS.md`** |
> | 우리 DB 설계·이관 전제·미결 대장 | **`DB.md`** |
> | 매번 지켜야 할 코드 작성 규칙 | **`CLAUDE.md`** |
>
> ⚠️ **현행은 JSP다. 화면 레이아웃을 베끼지 않는다.** 가져오는 것은 **기능 목록·데이터 항목·상태 전이**이고, UI는 `DESIGN.md` 토큰으로 새로 만든다.

---

## 1. 로그인 유형 (역할)

현행 `SY_AUTH` 13개 중 우리가 구현할 것. (현행 역할 전수·배정 실태는 `CURRENT.md` §3-2)

| 역할 | 현행 코드 | 판정 방식 | 우리 구현 위치 | 우선순위 |
|---|---|---|---|---|
| **학생** | `AUTH0001` | **`USER_TY_CD`로 자동** — 권한 부여 안 함 | `src_v2/` | 구현 중 |
| **상담사** | `AUTH0012` | 명시 부여 + `COM_CON_INF` 등록 | `src_admin/` | 구현 중 |
| **상담센터 총괄** | `AUTH0005` | 명시 부여 | `src_admin/` | 2순위 |
| **조교** | `AUTH0002` | **`USER_TY_CD`로 자동** + 학과 배정 | `src_admin/` | **1순위** |
| **교수(교원)** | `AUTH0003` | **`USER_TY_CD`로 자동** | `src_admin/` | 2순위 |
| **슈퍼관리자** | `AUTH0006` | 명시 부여 | `src_admin/` | 2순위 |
| 교무과 학사관리과 | `AUTH0011` | 명시 부여 | 보류 | — |
| 기업 담당자 | `AUTH0008` | SSO 밖 자체 인증 | 보류 | — |

**핵심 규칙 — 역할 판정이 두 갈래다.** (현행 `BASEGRUP_YN` 체계 계승)

```
학사 신분 기반 (자동)   학생 · 조교 · 교수 · 직원
  → USER_TY_CD 로 판정. 권한 테이블에 등록하지 않는다.

명시 부여 (수동)        상담사 · 관리자 · 기타 백오피스
  → 권한 테이블에 등록해야 화면에 들어온다.
```

> **상담사 화면을 두 단계로 나눈다** — 실무 상담사(`AUTH0012`)와 회원·시스템 관리까지 하는 총괄(`AUTH0005`). 현행이 이 두 역할로 갈려 있다.

---

## 2. 데이터 접근 범위 (행 수준) — 구현 필수

현행은 이 규칙이 SQL `<if>`에 흩어져 있고 파라미터 신뢰 지점이 있다(`CURRENT.md` §3-5).
**우리는 한 곳에서 판정한다.**

| 역할 | 볼 수 있는 학생 | 범위 단위 |
|---|---|---|
| 학생 | **본인만** | 개인 |
| 조교 | 담당 **학과 + 전공** 학생 | 학과·전공 |
| 교수 | 담당 학과 학생 + **지도학생** | 학과 / 개인 |
| 상담사 | 담당 **단과대** 학생 | 단과대 |
| 상담센터 총괄 · 슈퍼관리자 | 전체 | 전체 |

### 구현 규약

```ts
// src_admin/data/accessScope.ts (신설)
export interface AccessScope {
  kind: 'self' | 'dept' | 'college' | 'advisee' | 'all'
  deptCodes?: string[]      // 조교·교수: (deptCode, majorCode) 쌍
  collegeCodes?: string[]   // 상담사: 단과대
  studentIds?: string[]     // 교수: 지도학생
}

/** 로그인 사용자 → 접근 범위. 모든 목록·상세 조회가 이 함수를 거친다. */
export function getAccessScope(user: SessionUser): AccessScope

/** 범위 적용된 학생 목록. 화면은 이 함수만 호출한다. */
export function queryStudents(scope: AccessScope, params: ListParams): Promise<Paginated<Student>>
```

**금지** — 화면 컴포넌트에서 전체 학생 배열을 받아 `filter`로 범위를 좁히는 것. 범위 판정은 반드시 데이터 층에서 끝낸다(DB 전환 시 `WHERE` 절이 되어야 함).

**필수** — 모든 학생 조회에 **학적상태 필터**를 건다. 현행 `V_USR_INF`는 113,733행이고 졸업생·퇴직자가 누적돼 있다. 필터 없으면 학과당 수십 년치 졸업생이 나온다.

---

## 3. 화면 명세 <span>— 필요한 기능 + 현재 구현 상태</span>

각 화면: **목적 → 표시 데이터 → 액션 → 데이터 소스**. 현행 경로는 근거 추적용이며 구현 시 참조하지 않는다.
현행 화면 수의 출처는 `CURRENT.md` §1-2 (전수 대조 847화면).

**상태 표기** — ✅ 구현 완료 / ⚠️ 부분·재설계 필요 / ❌ 미구현 / 🔲 결정 필요 / `—` 스코프 밖
우리 쪽은 `src_v2/App.tsx` · `src_admin/App.tsx`에 **라우팅된 것만** 구현으로 센다.

| 역할 | 현행 화면 | 우리 라우트 | 커버율 | 절 |
|---|---|---|---|---|
| **상담사** | 29 | 13 | ~48% | §3-1 |
| **조교** | 7 | 3 | ~55% | §3-2 |
| 상담센터 총괄 | (상담사 + α) | 0 | 0% | §3-3 |
| **슈퍼관리자** | 315 | 0 | **0%** | §3-4 |
| **교수** | 33 | 5 | ~15% | §3-5 |
| **학생** | 약 200 | 27 | ~15% | §3-6 |
| 비교과 운영자 | 30 | (진로상담사 겸함) | 🔲 | §3-7 |
| 직원 · 기업 · 멘토 · 지역청년 | 약 87 | 0 | 🔲 | §3-8 |

> 이 문서는 *무엇이 필요한가*와 *지금 어디까지 됐나*만 담는다.

---

### 3-1. 상담사 <span>(현행 29 / 우리 13)</span>

| # | 화면 | 우리 구현 | 상태 |
|---|---|---|---|
| 1 | 마이홈 (대시보드) | `Home.tsx` | ✅ |
| 2 | 상담 현황 (접수·진행·완료) | `CounselRequests` + `CounselSession` + `CounselRecords` | ✅ |
| 3 | 상담 스케줄 (캘린더) | `CounselSchedule` | ✅ |
| 4 | **검사 현황** | `DiagnosisStatus` · `/students/diagnostics` | ✅ |
| 5 | **학생 포트폴리오 관리(첨삭)** | — | ❌ **3역할 공용 · 현행 `SS_JOB_RES` 계승** |
| 6 | **상담 통계** | `CounselStats` · `/counsel/stats` | ✅ |
| 7 | **상담 제한일정 관리** | `SettingsAvailability` | ⚠️ **재설계 필요** (가능 → 제한 등록) |
| 8 | 학생 관리 (목록·상세) | `StudentList`(`/students` 담당) + `StudentAll`(`/students/all` 전체) — 표는 `StudentChargeTable` 공유, 범위만 다름 · + `StudentDetail` | ✅ |
| 9 | 프로필 설정 | `SettingsProfile` | ✅ |
| 10 | **재배정 + 처리 이력** | `counselEvents` · 접수함 모달 | ✅ |
| 11 | **집단상담 / 집단심리검사** | `GroupCounsels` · `/counsel/groups` | ✅ |
| 12 | **심리검사 결과 작성** | `PsychTests` · `/counsel/psych-tests` <span>(psych 전용)</span> | ✅ |
| 13 | **학생 검색 피커** | `StudentPicker` (모달) | ✅ |
| 14 | **상담일지 인쇄** | `CounselRecordPrint` · `/counsel/records/:id/print` | ✅ |
| 15 | 기록지 잔여 필드 | `schema/counselRecord.ts` | ⚠️ **⑧ 참조** |
| 16 | **로드맵 생성** | `RoadmapCreate` · `/roadmap/create` <span>(career 전용)</span> | ✅ **⑯ 참조** |

#### ⑯ 로드맵 생성 <span>(2026-09-07 신설)</span>

| 항목 | 내용 |
|---|---|
| 목적 | **아직 로드맵이 없는 학생을 찾아 만든다** — 작업 대기열. 「로드맵 이행률 현황」(`/roadmap/progress`)은 통계 화면이라 별개다 |
| 표시 | 학생·학번 · 학과·학년 · 6유형 · **로드맵 칸** — 없으면 `생성` 버튼, 있으면 이행률 막대. 생성 대상이 맨 위 |
| 액션 | `생성` → 학생 상세를 **로드맵 탭으로 열고**, 「목표 달성 계획」 카드 안에서 생성 |
| 권한 | **진로취업 상담사 전용.** 학생은 생성·재생성할 수 없다 (`PROCESS.md` §6-6) |
| 소스 | `getRoadmapRoster()` (`studentRoster.ts`) · `dc_roadmap` · `dc_roadmap_snapshots` |

**「목표 달성 계획」 카드가 세 얼굴을 갖는다** — 상담 중에 화면을 옮기지 않고 여기서 끝낸다.

| 상태 | 카드 우측 | 카드 본문 |
|---|---|---|
| 로드맵 없음 | — | 가운데 `로드맵 생성` → 누르면 생성 화면이 펼쳐진다 |
| 보기 | `로드맵 편집` · `재생성` | 3축 보드(`RoadmapAxisBoard`) |
| 편집 | `편집 닫기` | `RoadmapEditorPanel` — 편집 페이지(`/roadmap/:studentId`)와 **같은 본문** |
| 재생성 | — | 생성 화면 + **완료 칸 미이월 경고**. `재생성 취소`로 되돌린다 |

> **생성이 일어나는 자리는 한 곳이다** — 학생 상세의 「목표 달성 계획」 카드. 목록의 버튼도 거기로 보낸다.
> **편집 로직은 한 벌이다** — 편집 페이지는 껍데기(헤더·링크)만 갖고 본문은 `RoadmapEditorPanel` 하나다. 변경 요청함·이행률 현황이 그 경로로 링크를 걸고 있어 페이지 자체는 남는다.
> 상담 중에는 상세를 열어 둔 채로 만들 수 있어야 하므로, 생성 화면으로 나갔다가 학생을 다시 찾는 동선을 만들지 않는다.
> 생성 UI(`RoadmapCreatePanel`)는 **공용 컴포넌트가 아니다.** 학생 화면 `/v2/roadmap/skill-tree`와 흐름은 같지만 잠금·동선이 달라 코드를 공유하지 않는다. 결과를 그리는 `RoadmapAxisBoard`만 공용이다.

#### ① 마이홈 (대시보드)

| 항목 | 내용 |
|---|---|
| 목적 | 오늘 할 일과 담당 학생 요약을 한 화면에 |
| 표시 | 오늘 상담 일정 · 대기 중 신청 건수 · 담당 학생 수 · 트랙별 분포(집중관리/위험/표준/우수) · 미작성 기록지 수 |
| 액션 | 각 카드 → 해당 목록으로 이동 |
| 소스 | `counselRequests` · `counselRecords` · `queryStudents(scope)` 집계 |

#### ② 상담 현황

| 항목 | 내용 |
|---|---|
| 목적 | 신청 접수 → 확정 → 진행 → 기록 완료까지 한 흐름 |
| 표시 | 학번 · 이름 · 학과 · 학년 · **학적상태 배지** · **과정 배지(학부/석사/박사)** · 상담유형 · 방식(대면/비대면) · 주제 · 신청일시 · 상태 · 담당상담사 |
| 액션 | 확정(슬롯 지정) · **재배정**(다른 상담사 이관) · 취소(**사유 필수**) · 기록지 작성 · 완료 |
| 필터 | 상태 · 상담유형 · 학과 · 학년 · 학적상태 · 기간 · 검색(이름/학번) |
| 소스 | `dc_counsel_requests` · `dc_counsel_records` |

> **현행에서 가져올 누락 기능** — 현행 `COUNSEL_MASTER`에는 우리 화면에 없는 항목이 있다.
> - **취소 사유·취소자·취소일시** (`CANCELREASON`/`CANCELUSERID`/`CANCELDATE`) — 취소 시 사유 입력 필수
> - **학생 공개 여부** (`STUD_OTP_YN`) — 기록지의 어느 부분을 학생에게 보일지 토글
> - **재배정 이력** (`ASSIGN_YN`/`ASSIGN_ID`) — 현행은 최종값만 있고 이력이 없다 → **우리는 이력 테이블로**
> - **연계 발송/접수** (`LINK_YN`/`LINK_TYPE`/`LINK_RCT_YN`/`LINK_RCT_TYPE`) — 진로↔심리 상담 간 이송
> - **상담 전 진단 4문항** (`MAJOR_YN` 전공만족 / `COURS_YN` 진로목표 / `EMPLO_YN` 취업역량 / `REASON_YN` 취업동기) — 상담 시작 시 상담사가 체크
> - **첨부 작성 여부 플래그** (`CON_ADD1~4`: 첨부파일·이력서·희망사항·가족관계) — 학생이 무엇을 제출했는지 표시
> - **가족관계**(`COUNSEL_FAM`) · **문제유형 다중선택**(`COUNSEL_PROBLEM`) — 심리상담에서 사용

#### ③ 상담 스케줄

| 항목 | 내용 |
|---|---|
| 목적 | 본인 상담 일정 확인 + 예약 가능 시간 관리 |
| 표시 | 월/주 캘린더 · 확정 상담 · 제한(불가) 시간 · 집단상담 |
| 액션 | 슬롯 확정 · 일정 메모 · 제한일정 등록 |
| 소스 | `dc_counsel_requests.slot` · `dc_counsel_excluded` (신설, §3-1-⑦) |

#### ④ 검사 현황 <span>✅ `/students/diagnostics`</span>

| 항목 | 내용 |
|---|---|
| 목적 | 담당 학생의 진단검사 응시 현황과 결과를 상담 준비용으로 열람 |
| 표시 | C-CORE·유형별 후속진단 응시율 카드 + 학생×검사 표(학번·이름·학과·학년·유형·검사·상태·응시일·결과 요약·재검사) |
| 액션 | 미응시 → **검사 권유 발송** · 완료 → **결과 코멘트 작성**(append-only) |
| 소스 | 검사 목록 `careerProcess.DIAGNOSIS_MODULES` + `dc_diag_attempts` · `dc_diag_comments` · `dc_diag_nudges` |
| ★ | **'미응시'는 저장하지 않는다.** 담당 학생별 필수 `CCORE`와 C-CORE 결과 유형의 후속진단 1종의 응시 레코드를 비교해 로더가 파생한다. |
| 참고 | **심리검사(§⑩)와 다른 도메인이다.** C-CORE·후속진단은 학생 단독 응시, 심리검사는 상담 건에 종속 |

#### ⑤ 학생 포트폴리오 관리 <span>(신규 · 3역할 공용)</span>

| 항목 | 내용 |
|---|---|
| 목적 | 학생의 이력서·자소서·자격증·어학·프로젝트를 상담사가 열람하고 첨삭 |
| 표시 | 학생 · 제출 항목 목록 · 상태(미신청/신청/완료) · 요청사항 · 상담사 의견 · 답변일 |
| 액션 | **첨삭 의견 작성** · 상태 변경 · 반환 |
| 소스 | `dc_portfolio` · `dc_portfolio_item` |
| 참고 | 현행 `SS_JOB_RES`에 `STATUS`(미신청/신청/완료) · `CON_ID`(담당상담사) · `CONCONT`(상담사 의견) · `REQCONT`(학생 요청사항) · `RE_DATE`(답변일)로 **첨삭 워크플로가 이미 구현돼 있다.** 그대로 계승 |
| ★ | **현행은 같은 화면을 `TYPE` 파라미터로 3역할이 공유한다** (`TYPE=T` 상담사 / `TYPE=A` 조교 / `TYPE=M` 프로그램담당자). 우리도 한 컴포넌트 + 역할 prop으로 만든다 (§4-3) |

#### ⑥ 상담 통계 <span>✅ `/counsel/stats`</span>

| 항목 | 내용 |
|---|---|
| 목적 | 본인 상담 실적 집계 (범위 전환 시 전체) |
| 표시 | 요약 5지표(신청·완료·취소·평균 소요일·기록지 작성률) · 월별 추이 · 유형/방식/학년/학과별 |
| 액션 | 범위(내 상담·전체) · 기간(3·6·12개월·전체) 필터 · CSV 내려받기 |
| 소스 | `counselStats.getCounselStats()` — **집계는 전부 이 층에서. 화면은 그리기만 한다** |
| ★ | **완료율·취소율의 모수는 "기간 내 신청 건"이다.** 완료 기록(`dc_counsel_records`)만 보면 취소가 안 보여 분모가 왜곡된다. 월별 축도 상담일이 아니라 **신청일** |

#### ⑦ 상담 제한일정 관리 <span>(설계 변경)</span>

**입력 방식은 "제한(불가) 등록"이다.** 기본값은 전 시간대 가능이고 불가능한 시간만 등록한다 — 상담사·교수 공통. 매주 반복 등록이 필요한 "가능 시간 등록" 방식을 쓰지 않는다.

| 항목 | 내용 |
|---|---|
| 표시 | 기본 상담 가능 요일·시간대(규칙) · 제한 일정 목록(날짜/시간/사유) |
| 액션 | 기본 규칙 설정 · 제한 일정 추가·삭제 · 일괄(휴가 기간) |

> ⚠️ **저장 구조는 별개 결정이며 뒤집혔다.** 현행 `BASICSETTING` 57만 행을 이관해야 하므로 **슬롯 저장 구조는 현행을 승계하고 `is_use`로 가능/불가를 표현**한다(`dc_counsel_slot`). 행 수 감축은 이관 후 과제다. → `DB.md` §9
> 즉 **UI는 "제한 등록", 저장은 "슬롯 + 사용여부"** 다. 이 둘을 혼동하지 말 것.

#### ⑧ 재배정 + 처리 이력 <span>✅</span>

| 항목 | 내용 |
|---|---|
| 목적 | 담당 상담사 이관과 **모든 상태 변화의 추적** |
| 액션 | 접수함 드롭다운 즉시 이관 · 상세 모달 탭(사유 입력) · **취소는 사유 필수** |
| 소스 | `dc_counsel_events` — 확정·일정변경·재배정·취소·완료 5개 전이가 모두 **append-only** 이벤트를 남긴다 |
| ★ | 현행 `CoMcReAssing`은 `CONSULTID`만 바꾸고 **이력이 남지 않는다**. 취소도 `CANCELREASON` 3컬럼에 마지막 1건만 덮어쓴다. 우리는 이벤트 로그로 대체 — CLAUDE.md "신설" 항목의 **상태 변경 이력** |
| ⚠️ 잔여 | 상담 기록지에 **학생 공개 여부**(`STUD_OTP_YN`) · **진로↔심리 연계**(`LINK_*`) · 상담 전 진단 4문항 · 가족관계 · 문제유형 다중선택 미적용 |

#### ⑨ 집단상담 / 집단심리검사 <span>✅ `/counsel/groups`</span>

| 항목 | 내용 |
|---|---|
| 목적 | 상담사 1명 : 학생 N명 회차 운영 (현행 `CoMc050L` 일반 / `CoMc060L` 심리검사) |
| 표시 | 회차 목록(실시일·시간·회차명·장소·인원·상태) · 상세(참여자·출석·기록) |
| 액션 | 회차 개설 → 학생 추가(**§⑬ 피커**) → 출석 체크 → 완료 처리 / 회차 취소(**사유 필수**) |
| 소스 | `dc_group_counsels` |
| ★ | **1:1 상담과 별도 도메인이다.** `CounselRequest`에 넣으면 접수함·통계 모수에 섞여 **실적이 이중 계상**된다 |
| ★ | 역할이 유형을 결정한다 — 진로 → 집단상담 / 심리 → 집단심리검사. 정원 초과·중복·상태 위반은 **로더에서 거부**(FK 없는 DB 전제) |

#### ⑩ 심리검사 결과 작성 <span>✅ `/counsel/psych-tests` · psych 전용</span>

| 항목 | 내용 |
|---|---|
| 목적 | 외부 검사도구(MMPI·TCI 등) 결과를 상담사가 입력·열람 |
| 표시 | 심리상담 건별 작성 여부 · 검사 종류 · 실시일 · 상태 · 학생 공개 여부 |
| 액션 | 척도 행 추가·삭제 · 결과 해석 · 상담사 소견 · **학생 공개 토글**(`STUD_OTP_YN` 계승) · 임시저장/완료 |
| 소스 | 대상 = 심리상담 신청(확정·완료)에서 파생 / 결과 = `dc_psych_tests` |
| ★ | **문항·채점 로직이 시스템에 없다.** 현행도 외부 결과를 입력하는 구조이고 **심리검사는 상담 신청 절차 안에서만** 이뤄진다(`docs/_analysis/05_answers.md` Q9). 그래서 척도를 고정 컬럼이 아니라 **자유 배열**로 받는다 |
| ⚠️ | 검사 종류 코드의 현행 값(`BASICSETTING.TRIALTYPE`)이 **미확인**이라 `legacy: null`. 확인 후 채울 것 |

#### ⑪ 상담일지 인쇄 <span>✅ `/counsel/records/:id/print`</span>

| 항목 | 내용 |
|---|---|
| 목적 | 완료 상담 기록을 A4 문서로 출력 (현행 `CoMs010Print`) |
| 표시 | 학생·상담 정보 표 · 소견 · 학생 공개 코멘트 · 후속 조치 · **처리 이력(§⑧)** · 서명란 |
| ★ | 레이아웃(GNB·사이드바) **밖** 라우트라 화면 그대로가 인쇄물이 된다. `@media print`로 툴바를 숨기고 A4 여백 지정 |

#### ⑫ 학생 검색 피커 <span>✅ `StudentPicker`</span>

현행 `CoStuInfoPop`(학생검색 팝업) 대응. **단독 화면인 `CoSs010L`은 §3-1-⑧ 학생 관리(`/students`)가 이미 대체**하므로, 다른 화면에서 학생을 고를 때 쓰는 **모달만** 만든다. 조회는 학생 로스터 단일소스(`queryStudentRoster`)를 그대로 쓴다.

---

### 3-2. 조교 <span>(현행 7 / 우리 3)</span>

**조교의 본질: 담당 학과 학생 현황 조회 + 전담교수 배정 보조.** 기능이 적으므로 **기존 컴포넌트 재사용으로 대부분 해결한다.**

| # | 화면 | 현행 | 우리 구현 | 상태 |
|---|---|---|---|---|
| 1 | 담당 학과 학생 현황 | `CoAs*` | `AssistantStudents` | ✅ |
| 2 | 전담교수 배정 | `CoAs010L`·`Advis` | `AssistantAdvisor` | ✅ |
| 3 | 전담교수 상담 실적 | `CoAs020L`·`020DL` | `AssistantAdvisorRecords` | ✅ |
| 4 | **학생별 역량현황** | `CoAs030L`·`CoAs040L` | — | ❌ **현행 조교 5화면 중 2개가 이것** |
| 5 | 학생 포트폴리오 (`TYPE=A`) | — | — | ❌ **§3-1-⑤와 같은 컴포넌트 + 역할 prop** |
| 6 | 학과 추천기업 관리 | (현행 없음) | `NotReady` | 🔲 우리 신설 항목 |
| 7 | 진로목표 현황 · 취업통계 | `Fu`·`St` 계열 | — | 🔲 보류 |

> **④ 학생별 역량현황은 학생(§3-6 S5)·교수(§3-5 P8)와 동일한 데이터다.** 5역량 축 화면 하나를 만들어 3역할이 공유한다(§4-3).

#### ① 담당 학과 학생 현황

| 항목 | 내용 |
|---|---|
| 목적 | 본인이 배정받은 학과·전공의 학생을 조회 |
| 표시 | 학번 · 이름 · 학과 · 전공 · 학년 · **과정(학부/석사/박사)** · 학적상태 · 진단 완료 여부 · 상담 이력 수 · 비교과 참여 수 |
| 액션 | 학생 상세 열기 (**읽기 전용** — 조교는 로드맵·IAP 수정 불가) |
| 필터 | 학과 · 전공 · 학년 · 학적상태 · 검색 |
| 소스 | `queryStudents(getAccessScope(user))` — 범위는 `FU_ASS_DEPT` 배정에서 파생 |
| ⚠️ | **배정이 삭제되면 즉시 안 보여야 한다.** 화면 진입 시마다 배정을 다시 읽고, 캐시하지 않는다 |

#### ② 전담교수 배정 (조회)

| 항목 | 내용 |
|---|---|
| 표시 | 학생 ↔ 지도교수 매핑 · 배정일 · 미배정 학생 목록 (탭: 전체/미배정/배정) |
| 액션 | **조교가 직접 확정 배정** — 미배정 행의 `[교수 배정]` 버튼 → 학생 학과의 교수 선택 |
| 소스 | `dc_advisor_assign` (현행 `CO_ADVISER`: `STU_NO`+`ADV_NO`) |
| ⚠️ | 확정형이므로 §3-4-① 감사 규칙을 **데이터층에서 강제**한다 — 학생당 active 1건 유일성 · append-only(해제는 `status` 전이) · `by`+`assignedAt` 기록 |

#### ③ 전담교수 상담 실적

| 항목 | 내용 |
|---|---|
| 표시 | 교수별 상담 건수 · 학생별 최근 상담일 · 미상담 학생 |
| 액션 | 독려 발송 (`dc_advisor_nudges` append) |
| 소스 | **`dc_prof_counsel_records`** — 교수상담 전용 단일소스 (현행 `CON_PROF_INFO` 대응) |
| ⚠️ | 상담사 상담(`dc_counsel_records`)과 **다른 도메인**이다. 현행도 `COUNSEL_MASTER`와 `CON_PROF_INFO`가 별도 테이블이므로 `CounselRequestType`(`진로취업`\|`심리`)에 '교수'를 추가하지 않는다 — 상담사 화면 필터·통계가 오염된다. 상담구분은 `SY_CODE` GRP `0131` 6종을 코드+라벨로 미러 |

---

### 3-3. 상담센터 총괄 <span>(상담사 화면 + 아래 추가)</span>

| 화면 | 목적 | 신규 |
|---|---|---|
| **상담사 관리** | 상담사 등록·수정·비활성. **내외부 구분** · 상담분야(진로/취업/클리닉) · 담당 단과대 · 상담 요일 · 장소 · 온라인 제한수 | 신규 |
| **조교학과 배정** | 학과 트리에서 조교 지정·해제 (§3-4-① 참조) | 신규 |
| **교수학과 배정** | 학과 트리에서 교수 지정·해제 | 신규 |
| 심리검사 현황 (전체) | 전 학생 검사 현황 | 신규 |
| 집단상담 / 집단심리검사 | 현행은 비활성(`X-`)이나 데이터 292건 존재 | 보류 |
| 상담 통계 (전체) | 전체 범위 집계 | 신규 |

> **상담사 관리 화면의 필드는 현행 `COM_CON_INF`(51컬럼)에서 가져온다.**
> `INOUT_GB`(내외부) · `CON_AREA1/2/3`(진로/취업/이력서클리닉) · `CON_TYPE1/2/3`(방문/온라인/클리닉) · `CON_W1~5`(월~금 가능) · `PLACE`+`PLACE_NUM`(건물/호수) · `LIMIT_GB`+`LIMIT_NUM`(온라인 제한) · `TARGET`(신청대상) · `STATUS`
> ⚠️ **`CONPWD`(자체 비밀번호)가 있다** — 외부 위촉 상담사는 SSO 밖 자체 인증을 쓴다. 신규 설계도 이 경로를 전제한다

---

### 3-4. 슈퍼관리자

현행 315화면 중 우리가 만들 영역만. **전용 SPA가 아직 없다 — 커버율 0%.**

| 영역 | 화면 | 현행 | 상태 | 우선순위 |
|---|---|---|---|---|
| **회원관리** | **조교/교수 학과 배정**(①) · 학생 포트폴리오 · 일반회원 | `Fu/sys_assDept`·`sys_profDept` | ❌ | **1순위** |
| **권한관리** | 역할 목록 · 사용자별 역할 부여 · 권한 변경 이력 | `SyAm010M`·`SyAm020M` | ❌ | **1순위** |
| **상담 총괄** | 상담사 관리 · 상담 현황(전체) · 전담교수 매칭 · 상담 제한일정 | `CoCt010L`·`CoCm010L`·`CoAm010L`·`CoAm020L` | ❌ | 2순위 |
| **비교과** | 프로그램 개설현황 · 분류 관리 · 블랙리스트 · 참여후기 | `Ep010L`·`Ep040L`·`EpBlackList`·`/admin/poss` | ⚠️ 진로상담사 화면으로 일부 구현 | 구현 중 |
| **채용** | 공고 관리 · 지원 현황 · 기업회원 관리 | `/Sa/Re` 계열 | ⚠️ 공고 관리만 | 구현 중 |
| **시스템** | **공통코드**(`SY_CODE`, §7 운영 화면) · 메뉴 · 게시판 · 배너·팝업 · 접속이력 · **열람 감사로그(신설)** | `SyCm010M`·`SyMn010M`·`SyBd010L` | ❌ | 3순위 |
| **데이터 분석** | 상담 · 비교과 · 마일리지 구간·순위 6종 | `/Sa/Dt` | ❌ | 3순위 |
| **취업통계** | 취업조사 · 예비조사 · 조사 차수 관리 | `/Sa/St` | ❌ | 3순위 |
| 포인트·마일리지 | 지급 기준 · 승인 · 학생별 현황 · 자격증 | `/Sa/Ex` | 🔲 **이관 제외 도메인** — 화면 존치 여부 미정 | — |

#### ① 조교/교수 학과 배정 <span>★ 구현 시 반드시 아래를 따를 것</span>

| 항목 | 내용 |
|---|---|
| 목적 | 학과 트리에서 특정 학과·전공에 조교(또는 교수)를 지정/해제 |
| 표시 | **단대 → 학과 → 전공** 3단 트리 · 학과코드 · 전공코드 · **과정 배지(학부/석사/박사)** · 배정된 담당자(이름·연락처) · 해당 학과 학생 수 |
| 액션 | 담당자 등록(사용자 검색) · 해제 · 일괄 SMS |
| 소스 | 학과 트리 = `V_DEP_INF_ALL` 미러 / 배정 = `dc_dept_assign` |

> 🔴 **현행 화면(`관리자 > 회원관리 > 조교학과 배정` = `/Sa/Fu/sys_assDept.do` = `Fu.assDeptList2`)의 버그를 반복하지 말 것.**
> **학사DB에 있는 학과가 이 화면에만 없는 일**이 상시 발생해 왔다 — 운영자가 매번 전산원에 요청하고 코드에 매핑을 박아야 했다.
> 학과 목록을 떨어뜨리는 필터가 **4개**다. 어느 하나에만 걸려도 학과가 통째로 사라진다.
>
> | # | 현행 조건 | 걸리는 학과 | 우리 구현 |
> |---|---|---|---|
> | (0) | `V_DEP_INF`(1,679) 사용 — `_ALL`(1,921)보다 242개 적음 | **대학원 전부** | **`V_DEP_INF_ALL` 하나만 쓴다** |
> | (3) | `LEFT JOIN` 전공에 **`WHERE C.USE_YN='Y'`** → **outer join이 inner join으로 붕괴** | **전공이 없는 학과** ← 신설 학과의 전형 | 조건을 **`ON` 절로 이동**. 전공 컬럼은 빈값으로 두고 학과는 반드시 노출 |
> | (1) | 상위조직을 `LVL='1' AND GRP_CD='0001'`(단대)로 한정 | 기관(`0004`) 밑에 달린 학과 | 상위 조직 종류를 **한정하지 않는다** |
> | (2) | 학과 자신이 `GRP_CD IN ('0002','0003')` | 그룹코드가 다르게 부여된 신설 학과 | 코드 상수 대신 **트리 레벨로 판정** |
>
> **더미 전공 행 관행을 없앤다.** 현행은 (3)을 우회하려고 전공이 없는 학부를 **자기 자신을 전공으로 한 번 더 등록**해 둔다(예: `건축학부 / 건축학부 / 436 / 436`). 학과 트리에 실재하지 않는 노드를 만드는 것이므로 우리는 만들지 않는다 — 대신 전공 없는 학과를 그대로 노출한다.
>
> **★ 누락 감지를 화면에 넣는다.** 학사DB 원본과 우리 트리를 대조해 **"학사DB에는 있는데 우리 목록에 없는 학과 N건"** 을 상단에 띄운다. 현행은 사람이 발견할 때까지 아무도 모른다. → `DB.md` §3-5

> ⚠️ **배정 저장 규칙** — 현행 `FU_ASS_DEPT`는 **PK가 없어** 중복 등록이 가능하고, 삭제 시 **이력이 남지 않는다.**
> 우리는 ① `(deptCode, majorCode, userId)` 유일성을 애플리케이션에서 보장하고, ② **배정/해제 이력을 남긴다**(학생 개인정보 접근 범위를 바꾸는 일이므로 감사 대상).
>
> ⚠️ **`majorCode`를 NULL 허용으로 두고 "학부 전체"로 정의한다.** 현행은 `MAJOR_CD`가 NULL인 배정에 학생이 0명으로 나온다 — **"배정은 했는데 학생 목록이 빈다"**. 학생 조회가 `deptCode` 기준으로도 매칭되게 한다.
>
> ⚠️ **같은 이름의 화면이 현행에 두 개 있다.** `/Sa/Co/sys_assDept.do`(상담관리)는 읽기는 `FU_ASS_DEPT`, 쓰기는 `COM_ASS_DEPT`(읽는 곳 0건인 고아 테이블)라 **저장해도 아무 일이 일어나지 않는다.** 이관 시 `COM_ASS_DEPT`는 폐기하되 **잔여 배정을 먼저 diff** 한다 (`DB.md` §9 #9).

---

### 3-5. 교수 (교원) <span>(현행 33 / 우리 5)</span>

> **현행 교수상담은 `CON_PROF_INFO` 184,126건 — 이 시스템에서 가장 많이 쓰이는 기능이다.**
> 상담구분은 `SY_CODE` 그룹 `0131`(§7-7): 전공 및 학업 · 진로 · 취업 · 봉사 및 실습 · 사제동행프로그램 · 기타.

| # | 화면 | 현행 | 우리 구현 | 상태 |
|---|---|---|---|---|
| P1 | 지도학생 목록 | `CaMpSs010L` | `ProfessorAdvisees` | ✅ |
| P2 | 상담 신청 접수 | — | `ProfessorCounselRequests` | ✅ |
| P3 | 상담 기록 작성 | `PcMp010M`·`PcMa010M` | `ProfessorCounselRecords` | ✅ |
| P4 | 상담 제한일정 | `PcMp030L`·`040L`·`CoMp040MonthL` | `ProfessorSchedule` | ✅ |
| P5 | **상담 노출 설정** | 신설(`CO_PROF` 0행) | `ProfessorProfile` | ✅ |
| P6 | **온라인 상담**(게시판형) | `PcMpOnlineL`·`onlineConProf` | — | ❌ 학생 §3-6 S4와 짝 |
| P7 | **오프라인 상담 일괄등록** | `pop_offLineProfContDirNew` | — | ❌ 2025-04 신설된 현행 최신 기능 |
| P8 | **학생별 역량현황** | `CaMpSs010L`·`010D` | — | ❌ 조교 §3-2-④ · 학생 S8과 동일 데이터 |
| P9 | 집단상담 작성 | `pop_PcMp010M` | — | ❌ |
| P10 | 상담내역 목록·상세·인쇄 | `PcMp020L`·`020D`·`010D_Print` | — | ❌ |
| P11 | 역량진단 관리(교수) | `CaMp010L`·`CaMpDm010P` | — | ❌ |
| P12 | 학생별 진로목표 현황·상세 | `FuAp050A`·`050D` | — | ❌ |
| P13 | 푸시 전송 | `PcPushSend` | — | ❌ |
| P14 | `PcMa*` vs `PcMp*` 2계열 | — | — | 🔲 **담당교수/지도교수 구분 여부 확인 필요** |

**상담 제한일정(P4) 규칙** — **기본값은 모든 시간대가 가능**이고, **불가능한 시간대만 등록**한다. 상담사(§3-1-⑦)와 같은 방식이다.

> ⚠️ **오프라인 교수상담 슬롯은 상담사와 같은 `BASICSETTING` 테이블을 쓴다**(`CONSULTANTID`에 교수 `INTG_UID`). 슬롯 모델을 상담사 전용으로 설계하지 말 것.

---

### 3-6. 학생 <span>(현행 약 200 / 우리 27)</span>

우리 쪽 단일소스는 `src_v2/`. 사이트맵 컨셉·게임화 요소는 `STU_README.md`.

**A. 상담**

| # | 기능 | 현행 | 상태 |
|---|---|---|---|
| S1 | 상담 신청 · 내역 | `CoVi010M`·`CoMs010L` | ✅ `/counsel/career`·`/psych`·`/record` — **게이트 밖이다.** 일반 진로·취업, 심리, 교수 상담은 진단 없이 바로 신청한다. 진단·유형 확정을 요구하는 것은 **CARE 7+ 연계 상담 하나뿐**이다 → `PROCESS.md` §2-1 |
| S2 | **상담 취소 · 취소사유** | `CoMsCancelPop` | ❌ 상담사측은 구현됨(§3-1-⑧), **학생측 경로 없음** |
| S3 | **검사 신청** | `CoVi020M` | ❌ 상담과 별개 트랙인 심리검사 단독 신청 |
| S4 | 지도교수 **온라인 상담**(게시판형) | `CoMs020L`·`030L`·`040L` | ⚠️ `/counsel/professor`는 신청만. 현행은 온·오프라인 2트랙 |
| S5 | 상담내역 인쇄 | `CoMs010Print` | ❌ |

**B. 진단 · 역량**

| # | 기능 | 현행 | 상태 |
|---|---|---|---|
| S6 | 진단 결과 열람 | `CaMsDm010R` | ✅ `/diagnosis/employment` |
| S7 | **진단검사 응시** | `CaMsDm010D` | ❌ **`DiagnosisCenter.tsx`·`PersonalityTest.tsx`가 라우팅 안 됨** — "검사시작"이 갈 곳이 없다 |
| S8 | **학생별 역량현황** | `CaMsSs010D` | ❌ **`CompetencyCenter.tsx`가 라우팅 안 됨.** 조교 §3-2-④ · 교수 P8과 동일 데이터 |
| S9 | **핵심역량 시뮬레이션** | `CaMsEp010D`·`010V` | ❌ "이 프로그램 이수 시 역량 +N" — 우리 로드맵과 직결 |

**C. 비교과**

| # | 기능 | 현행 | 상태 |
|---|---|---|---|
| S10 | 신청 · 내역 · 찜 | `EpMs010L`·`020L` | ✅ `/growth/program`·`/mypage/programs` — **C-CORE·상담·로드맵 선행 완료 후에만 신청 가능**. 중간계층은 진로탐색·진로설정 카테고리만 신청 |
| S11 | **그룹형(팀) 신청** | `extMyGroup`·`group_app_popup` | ❌ 현행은 개인형/그룹형 2종, 우리는 개인형만 |
| S12 | **차수(회차) 선택** | `extStep` (`EP_PRM_STEP`) | ❌ |
| S13 | **만족도조사 응답** | `EpMs040L`·`040D` | ❌ |
| S14 | **참여후기 게시판** | `/user/poss/pgmPoss*` | ❌ |

**D. 취업**

| # | 기능 | 현행 | 상태 |
|---|---|---|---|
| S15 | 채용공고 열람 | `ReMs020L` 외 | ✅ `/jobs` |
| S16 | **채용 지원 프로세스** | `ReAppD`·`ReAgree`(개인정보동의) | ❌ `APPLICATIONMASTER` **1.7만건 이관 대상인데 지원 경로가 없다** |
| S17 | **MY추천채용** | `ReMs010D`·`MsRd010L` | ❌ |
| S18 | 채용 카테고리 8종 분화 | 추천/일반/공공기관/인턴십/공모전/교육·박람회/알바/해외 | ⚠️ 우리는 단일 목록 |
| S19 | **이력서·자소서 첨삭 요청** | `portfolio_resume`·`portfolio_selfHistory` | ❌ `SS_JOB_RES` 워크플로 — 상담사 §3-1-⑤와 짝 |
| S20 | 진로취업카드 · 취업수기 | `SsMsRs010M` · `IpBd010L` | ❌ |

**E. 포트폴리오 세부** <span>(현행 18화면 → 우리 1화면)</span>

자기성찰(`introspection`) · 교과 이수·선후수(`study_courses`·`coursesSunAfterSearch`) · 장학·수상(`study_scholarship`) · 상담이력 연동(`activity_counsel`) · 비교과 참여이력+성찰(`extState`·`extStateThkl`) — **전부 ❌**

**F. 진로설계** <span>(이관 대상 1.4만건인데 화면이 없다)</span>

진로목표 설정(`FuAp050L`) · 진로설계서 3단계(`MyCuStep3_Pop`) · 워크넷 검사결과(`FuAp0*`) — **전부 ❌** (`STU_COURSE_*` 계열)

**G. 공통 인프라 · 결정 필요**

공지·게시판(`Bd`) · 센터안내(`Ki`) · 알림 수신함 · 약관 동의(`SY_AGREE`) — **전부 ❌**
🔲 **마일리지**(`Ex` 11화면) — 이관 제외 확정. 퀘스트·레벨로 대체할지 없앨지 미정
🔲 **멘토·멘티**(`Mt` 8화면) — 스코프 포함 여부

---

### 3-7. 비교과 운영자 <span>(현행 30 / 역할 미신설)</span> 🔲

현재 **진로상담사(`career`)가 겸한다.** 현행은 별도 역할(`Mn`)이고 `EP_PRM.REGID = 세션ID`로 **본인 등록 프로그램만** 본다.

- 🔲 **역할 신설 여부** — 겸한다면 "본인 등록 프로그램만" 범위 규칙을 어떻게 할지
- ❌ 그룹형 등록 · 차수 관리(`extStep*`) · 선발그룹 관리(`extGroupBindMng`) · 외부인 관리(`EpTb050D`)
- ❌ 만족도조사 관리(`EpMn050*`, 학생 S13과 짝) · 결과보고서(`EpTb060D`)
- ❌ 참가대상 설정(부서 `EpMnselectPrmTrgtDept` / 국적 `EpMnsearchNat`) · 달력·월력 뷰 · 푸시 발송

---

### 3-8. 스코프 미정 🔲

| 역할 | 현행 | 비고 |
|---|---|---|
| 직원(교무·학사) | 23 | 장기결석·학사경고 명단(`Wa010L`) · 취업통계 18화면 · 직원 상담(`CoEm*`) |
| 기업 | 13 | `SESSION_USER_TY_CD='C'` · SSO 밖 자체 인증(`TB_CARR_USER_LOGIN`) |
| 멘토 | 6 | `TB_MENTEE_INFO.MENTO_ID` |
| 지역청년 | 45 | `RegLoginSession` — **세션 체계가 분리돼 사실상 별도 사이트** |

---

## 4. React 구현 규약

### 4-1. 라우팅

**React Router** `createBrowserRouter`. basename은 `src_v2/` = `/v2`, `src_admin/` = `/admin`.
라우팅된 것만 구현으로 센다 — 컴포넌트만 있고 라우트가 없으면 사용자가 갈 수 없다(§3-6 S7·S8이 그 상태).

### 4-2. 역할 주입

역할은 **에이전트나 별도 앱이 아니라 컨텍스트로 주입**하고, **읽기 범위를 역할별로 격리**한다.

```tsx
<SessionProvider user={{ id, name, role, scope }}>
```

화면은 `useSession()`으로 역할을 읽고, 데이터는 `queryXxx(scope, params)`로만 가져온다.

#### 백엔드 도입 시 — 요청자 판정도 단일 지점이다 <span>(2026-09-08 결정)</span>

SSO는 `DB.md` §8-3 **5단계**이고 선행조건이 "학교 협의"다. 그 전까지 인증은 **스텁**으로 둔다 — 요청 헤더에 실린 사용자 ID를 서버가 그대로 믿는다.

**다만 서버는 "요청자가 누구인가"를 한 군데에서만 판정한다.** 나중에 그 함수 하나만 SSO로 바꾸면 되도록 — 로더 스왑 seam과 같은 발상이다.

```
❌ 각 엔드포인트가 헤더를 직접 읽는다
✅ 의존성 주입 한 곳 → 모든 엔드포인트가 그 결과만 받는다
```

⚠️ 스텁이므로 **개발 환경 밖으로 내보내지 않는다.** 헤더를 믿는 서버는 누구나 임의 사용자로 위장할 수 있다.

### 4-3. 한 화면을 여러 역할이 공유하는 패턴 <span>(현행에서 배울 점)</span>

현행은 같은 JSP를 `TYPE` 파라미터로 3역할이 쓴다.

```
CoMc070L.do?TYPE=T  상담사 → 포트폴리오 관리
CoMc070L.do?TYPE=A  조교   → 학생현황
CoMc070L.do?TYPE=M  프로그램담당자
```

**우리 방식** — 컴포넌트 하나 + 역할별 설정 객체.

```tsx
// 화면은 하나, 역할별 차이는 설정으로
const VIEW_CONFIG: Record<Role, { columns: ColumnKey[]; actions: ActionKey[]; readonly: boolean }> = {
  counselor: { columns: [...], actions: ['review','comment'], readonly: false },
  assistant: { columns: [...], actions: [],                   readonly: true  },
  admin:     { columns: [...], actions: ['review','comment','delete'], readonly: false },
}
```

**조교 화면 대부분은 이 방식으로 신규 개발 없이 해결된다.**

### 4-4. 목록 화면 필수 요소

모든 학생·신청 목록에 아래를 빠뜨리지 않는다.

1. **학적상태 필터** (기본값: 재학) — 졸업생 누적 때문에 필수
2. **과정 필터** (전체/학부/석사/박사) — 대학원 지원 요건
3. **학과 트리 필터** (단대 → 학과 → 전공)
4. **서버 페이징 계약** — 화면은 전체 배열을 받지 않고 `Paginated<T>`만 받는다 (`src_admin/data/query.ts` 패턴)
5. **빈 상태** — `EmptyState` 컴포넌트
6. **스냅샷 표기** — 목록의 학과·학년은 **신청 시점 스냅샷**임을 배지나 툴팁으로 구분

### 4-5. 컴포넌트 재사용

`Modal`(sm/md/lg) · `FormField` · `ConfirmDialog` · `EmptyState` · `AdminModal` · `SectionSidebar` · `RichEditor`. **Drawer는 쓰지 않는다.**

새 화면을 만들 때 먼저 기존 컴포넌트로 되는지 확인한다 (CLAUDE.md 규칙 5: 재생성 금지).

---

## 5. 데이터 설계 원칙

**이미 관계형 DB 로 옮겨지는 중이다.** 학생·상담·진단·비교과는 PostgreSQL 이 정본이고, 나머지는 아직 JSON·localStorage 다.
어느 도메인이 어느 쪽인지는 **`DB.md` §8-3 이관 대장**이 정본이다. 아래 원칙은 **아직 안 옮긴 도메인에 그대로 적용**된다 — 여기서 원칙을 지켜야 옮길 때 로더 교체로 끝난다.

> ⚠️ **운영 DBMS는 확정되지 않았다** — 현행은 Oracle, 배선 검증은 PostgreSQL, 운영은 미정(`DB.md` §9 #39).
> 여기 원칙은 **DBMS와 무관하게** 성립하도록 쓴다. 특정 DBMS 문법에 기대는 설계를 하지 않는다.

> **→ 원칙 12개(학사데이터 읽기전용 · `INTG_UID` 단일 조인키 · 스냅샷 동반 · 파생값 분리 · 코드+라벨 · append-only 등)는 `CLAUDE.md` §코드 작성 규칙이 단일 소스다.** 여기 복사하지 않는다.
> **→ 이관 전제(도메인별 O/X · 강제사항 5가지)는 `DB.md` §8-0이 단일 소스다.**

화면·스키마를 만들 때 **매번 걸리는 것**만 여기 적는다.

| # | 이것만 기억한다 |
|---|---|
| 1 | **모든 코드 항목에 `legacy` 값을 채운다** (§7-0 규칙 2). 비면 이관 스크립트를 쓸 수 없다 |
| 2 | **신설 필드는 NULL 허용 + 화면 폴백.** NOT NULL로 잡으면 이관이 막힌다 |
| 3 | **이관 대상 도메인의 스키마를 임의로 재설계하지 않는다.** 1:1이 아닌 변환은 규칙을 함께 적는다 |
| 4 | **민감정보는 컬럼 분리 + 동의 + 감사.** 상담 소견(내부)과 학생 공개 코멘트를 분리한다 |
| 5 | **override는 버전으로.** base+오버레이 병합 구조는 DB에서 버전 테이블이 된다 — `version`·`confirmed`·`updatedBy`를 JSON에도 둔다 |
| 6 | **모든 학생 조회에 학적상태 필터.** 기본값 `재학` — 현행 사용자 테이블 11.3만 행에 졸업생이 누적돼 있다 |

### 런타임 반영 방식 <span>(아직 안 옮긴 도메인 한정)</span>

브라우저에서 JSON 파일을 쓸 수 없으므로 **base JSON(seed) + `localStorage` 오버레이 = 병합 렌더**로 처리한다. 원본 JSON은 불변, 이벤트 결과만 오버레이에 쌓고 읽을 때 병합한다.

**로더 = 단일 스왑 seam.** 화면은 로더 함수만 호출하고, DB 전환 시 그 함수 내부만 API 호출로 교체한다.

```ts
// ✅ 이렇게
const students = queryStudents(scope, params)   // 로더만 호출

// ❌ 이렇게 하지 않는다
import roster from '../data/studentsRoster.json'      // 화면에서 직접 import
const filtered = roster.filter(s => s.major === myDept)
```

#### ★ 읽기 로더는 **동기**, 쓰기와 페이징 목록은 **비동기** <span>(2026-09-09 갱신)</span>

DB로 바꿀 때 **화면을 한 줄도 고치지 않는 것**이 이 seam의 목적이다. 읽기 로더를 `async`로 만들면
그 로더를 쓰는 모든 화면에 로딩·에러 상태가 생겨 **seam이 seam이 아니게 된다** — 「JSON만 갈아끼운다」가 40화면 수정이 된다.
그래서 **읽기는 기동 시 1회 적재한 메모리 캐시를 동기로 읽는다.**

그러나 **쓰기는 처음부터 비동기다.** 서버가 정본이고 정합성을 판정하기 때문이다 —
정원 초과·중복 신청·낙관적 잠금 충돌은 **저장을 시도해야 알 수 있다.** 쓰기를 동기로 위장하면
저장되지 않은 것을 저장된 것처럼 보여 주게 된다(비교과 신청이 실제로 그랬다).

| 무엇 | 시그니처 | 이유 |
|---|---|---|
| **읽기 셀렉터** (`getPrograms`·`getStudentCounselRequests` …) | **동기** | 기동 시 적재한 스토어를 읽는다. 화면 무수정 |
| **쓰기** (신청·선발·출석·저장 …) | **비동기** | 서버가 판정한다. 실패 사유를 화면에 그대로 띄운다 |
| **페이징 목록** (`queryPenaltyList` …) | **비동기** | 전량을 메모리에 올릴 수 없는 목록. `useListData` 훅이 로딩·레이스를 흡수한다 |
| **학생 1명의 대용량 스냅샷** (`fetchAcademic`) | **비동기** | 학생 전환 시마다 받는다. 전 학생을 미리 올리지 않는다 |

**비동기 경계는 세 곳뿐이다** — 앱 기동(`initializeData`) · 쓰기 액션 · 위 두 종류의 전용 훅.
그 밖의 화면은 여전히 셀렉터만 구독한다.

> 규모가 커지면 읽기도 페이징으로 옮긴다. **도메인 단위로 옮기고, 옮길 때 전용 훅을 쓴다** —
> 셀렉터 시그니처를 바꿔 전 화면을 건드리지 않는다.
> ⚠️ 코드 항목(§7)은 예외 없이 이 캐시 대상이다 — §7-0 규칙 7이 "기동 시 1회 로드 후 메모리 캐시"를 이미 요구한다.

---

## 6. 필드 사전 — 화면에서 쓸 표준 필드명

**일관성의 핵심 섹션.** 새 화면·새 JSON을 만들 때 아래 이름을 그대로 쓴다. 현행 컬럼과의 대응을 명시해 DB 전환 시 매핑이 자동으로 되게 한다.

### 6-1. 학생 (`Student`)

| 우리 필드 | 타입 | 현행 컬럼 | 출처 | 비고 |
|---|---|---|---|---|
| `id` | string | `V_USR_INF.INTG_UID` | 학사 | **PK. 학번** |
| `name` | string | `USR_NM` | 학사 | |
| `collegeCode` / `collegeName` | string | `DAEHAK_CD` → `DEPT_NM` | 학사 | 단대 |
| `facultyCode` | string | `HAKBU_CD` | 학사 | 학부 |
| `deptCode` / `deptName` | string | `MAJOR_CD` → `DEPT_NM` | 학사 | **전공.** 화면의 "학과" |
| `grade` | number | `STU_SCHGR` / `GRADE` | 학사 | 학부 1~4, **대학원 1~3** |
| `courseType` | `CourseType` | `UNIV_CODE` | 학사 | **학부/석사/박사/전문** (§7-5) |
| `enrollStatus` | `EnrollStatus` | `HOFC_STA_CD` | 학사 | 재학/휴학/졸업/수료/제적 |
| `enterDate` / `outDate` | string | `ENTER_DT` / `OUT_DT` | 학사 | |
| `completedTerms` | number | `ISU_CNT` | 학사 | 이수학기 |
| `advisorId` | string | **`PROF_ID`** | 학사 | 지도교수 교번 |
| `phone` / `email` | string | `HP` / `EMAIL` | 학사 | |
| `photo` | string | `PHOTO` | 학사 | |
| `isForeigner` | boolean | `FOR_YN` | 학사 | |
| `minorMajors` | string[] | `MAJOR_CD2` / `MJ2_NM` | 학사 | 복수·부전공 |
| — 아래는 **우리가 만드는 값** — | | | | |
| `studentType` | `StudentType` | — | 자체 | **6유형** `T1`~`T6` (C-CORE 결과 → 상담에서 확정) → `PROCESS.md` §4 |
| `tier` | `Tier` | — | 자체 | **3계층** 하위/중간/상위. `studentType`에서 파생 — **비교과 신청 범위를 가른다** (`PROCESS.md` §7) |
| `track` | `Track` | — | 자체 | 집중관리/위험/표준/우수. **`tier`와 중복 — 폐기 검토 대상**(§9) |
| `competencyScore` | number | — | 자체 | 0~100 |
| `gpa` | string | `TB_CARR_CAPA_STTS.GRADE` | 자체* | *학사 미연계 → 자체 입력 |
| `languageScore` | string | `TB_CARR_CAPA_STTS.TOEIC` 등 | 자체 | |
| `penaltyTotal` | number | — | 자체 | 노쇼 벌점 (신설) |
| `level` / `expPoint` | number | — | 자체 | 게임화 (신설) |

### 6-2. 조직 (`Dept`)

| 우리 필드 | 타입 | 현행 컬럼 | 비고 |
|---|---|---|---|
| `code` | string | `V_DEP_INF_ALL.DEPT_CD` | PK |
| `name` | string | `DEPT_NM` | |
| `shortName` | string | `DEPT_SUB_NM` | 있으면 우선 표시 (현행 로직) |
| `parentCode` | string | **`DEPT_UP_CD`** | 계층 |
| `level` | 1\|2\|3\|4 | `LVL` | |
| `kind` | `DeptKind` | **`GRP_CD`** | `0001`단대 / `0002`학과 / `0003`전공 / `0004`기관 |
| `courseType` | `CourseType` | **`UNIV_CODE`** | 학부/석사/박사 |
| `active` | boolean | `USE_YN` | `N`=폐과 (639건 존재) |
| `sortOrder` | string | `ORDERING` | |

> ⚠️ **`(collegeCode, deptCode)` 쌍으로 식별한다.** 동명 학과가 여러 개다 — `신소재공학부`가 `505`(공과대), `501`(메카트로닉스대), `1528`(석사), `2528`(박사)로 4개. **학과명 매칭 금지.**

### 6-3. 상담 (`CounselRequest` / `CounselRecord`)

| 우리 필드 | 현행 컬럼 (`COUNSEL_MASTER`) | 비고 |
|---|---|---|
| `id` | `IDX` | |
| `studentId` | `USERID` | = `INTG_UID` |
| `counselorId` | `CONSULTID` | |
| `type` | `COUNSELTYPEIDX` | 진로/취업/심리/교수 (§7-1) |
| `careTrack` | — | **진로취업 상담의 트랙** `general` / `care7` (§7-6). 심리·교수상담에는 적용하지 않음. **NULL = `care7`** — 신설 컬럼이라 이관 데이터에 없다 |
| `status` | `COUNSELSTATUSIDX` | 신청/완료/학생취소/상담사취소 (§7-2) |
| `method` | `ON_OFF_TTYPE` | 대면/비대면 |
| `topic` | `CONSULTTITLE` | 상담 제목 |
| `careerTopicCodes` | — | 해당 유형의 4개 주제 코드 (`A01`~`A24`, §7-10). **`careTrack='care7'`에만 적용** — 일반 상담은 자유 입력(`topic`), 심리·교수상담은 해당 없음 |
| `finalStudentType` | — | C-CORE 결과와 후속진단을 바탕으로 상담 중 최종 확정한 6유형. **`care7`에만 적용** — 일반 상담은 유형을 확정하지 않는다 |
| `roadmapId` | — | 같은 오프라인 상담에서 생성한 확정 로드맵. **`care7` 완료 시 필수** — 일반 상담에서는 로드맵을 만들지 않으므로 항상 비어 있다 |
| `requestBody` | `CONSULTINFO` | 학생이 쓴 신청 내용 |
| `hopeNote` | `HOPE_INFO` | 희망사항 |
| `requestedAt` | `IDATE` | |
| `slotDate` / `slotStart` / `slotEnd` / `slotPlace` | `COUNSELSDATE` / `COUNSELEDATE` | 확정 슬롯 |
| `resultTitle` / `resultBody` | `RESULTTITLE` / `RESULTINFO` | **상담사 소견 (내부)** |
| `commentToStudent` | — | **학생 공개 코멘트** (신설 — 현행은 분리 안 됨) |
| `publicToStudent` | `STUD_OTP_YN` | 학생 공개 여부 |
| `recordedAt` | `CONSULTDATE` | 결과 입력일 |
| `reassigned` / `reassignedBy` | `ASSIGN_YN` / `ASSIGN_ID` | **이력은 별도 테이블로 신설** |
| `cancelReason` / `cancelAt` / `cancelBy` | `CANCELREASON` / `CANCELDATE` / `CANCELUSERID` | **취소 시 사유 필수** |
| `preCheck.majorSatisfied` | `MAJOR_YN` | 전공 만족/불만족 |
| `preCheck.careerGoalSet` | `COURS_YN` | 진로목표 설정/미설정 |
| `preCheck.employability` | `EMPLO_YN` | 취업역량 높음/낮음 |
| `preCheck.motivation` | `REASON_YN` | 취업동기 높음/낮음 |
| `link.sent` / `link.sentType` / `link.received` / `link.receivedType` | `LINK_YN` / `LINK_TYPE` / `LINK_RCT_YN` / `LINK_RCT_TYPE` | 진로↔심리 연계 이송 |
| `attach.file` / `attach.resume` / `attach.hope` / `attach.family` | `CON_ADD1~4` | 학생 제출물 여부 |
| `problemCodes` | `COUNSEL_PROBLEM` | 문제유형 다중선택 |
| `family` | `COUNSEL_FAM` | 가족관계 (심리상담) |
| **스냅샷** `snapName`·`snapDeptName`·`snapGrade`·`snapEnrollStatus`·`snapTrack` | — | 발생 시점 값 (원칙 4) |

### 6-4. 비교과 프로그램 (`Program` / `ProgramApply`)

| 우리 필드 | 현행 컬럼 | 비고 |
|---|---|---|
| `id` | `EP_PRM.PRM_SEQ` | |
| `title` | `PRM_NM` | |
| `groupType` | `PRM_GB` | 개인(P) / 그룹(G) |
| `year` / `term` | `PRM_YEAR` / `PRM_HAKGI` | |
| `status` | `PRM_STAT_GB` | 생성(C)/운영(O)/종료(E) |
| `openRequestStatus` | `REQ_STAT_GB` | 임시저장(T)/신청(A)/반려(R)/승인(S) — **개설 승인 워크플로** |
| `applyStart` / `applyEnd` | `APP_ST_DATE` / `APP_ED_DATE` | |
| `runStart` / `runEnd` | `PRM_ST_DATE` / `PRM_ED_DATE` | |
| `capacity` / `selectedCount` | `PRM_NUM` / `SEL_NUM` | |
| `firstComeFirstServed` | `SEL_NUM_FST_YN` | 선착순 여부 |
| `deptCode` | `DEPT_CD` | 주관부서 |
| `place` | `PLACE` | |
| `categoryCodes` | `PRM_GB_1~4` | 1~4차 분류 (`EP_CUR_GUBUN`) |
| `careTypes[]` | — (**신설**) | **CARE 7+ 분류** — 대상 6유형 `T1`~`T6` 중복 선택. 코드만 저장하고 라벨은 `typeLabel()` |
| `roadmapEntry` | — (**신설**) | **로드맵 편입** `NONE`/`RECOMMEND`/`REQUIRED`. `careTypes`에 든 유형의 **IAP 실행 축에 칸을 추가**한다 → `PROCESS.md` §6-4 |
| `competencyRatio[]` | `CAP_PER1~6` | 역량 반영 비율 6종 |
| `mileage` / `point` | `POINT1/2` · `MIN/MAX_MILEAGE` | 균등/차등 (`MILEAGE_DIV`) |
| `targets` | `TRGT_STU11~15`·`TRGT_STU21~24`·`TRGT_STU31~35`·`TRGT_EMP`·`TRGT_OUT` | **참가대상 — 학적상태/대학원학년/학부학년/교직원/외부인** |
| `rounds[]` | `EP_PRM_STEP` | **차수** (`PRM_STEP`) — 차수별 기간·정원 |
| — 신청 — | | |
| `applyId` | `EP_PRM_APP.PRM_APP_SEQ` | |
| `round` | `PRM_STEP` | |
| `selectionStatus` | `STATUS` | 신청/선발/탈락/취소/수료/미수료 (§7-3) |
| `motiveCode` / `routeCode` | `MOTIVE_CD` / `ROUTE_CD` | 지원동기·경로 |
| `reflection` | `THKL` | 성찰 |
| **스냅샷** `snapEnrollStatus`·`snapGrade`·`snapEmail`·`snapPhone` | `HOFC_STA_CD`·`STU_SCHGR`·`EMAIL`·`HP` | **현행이 이미 이 패턴** |
| `competencyScores[]` | `EP_PRM_RESULT.CAP_PNT1~6` | 참여 결과 = 역량점수 |
| `mileageScores[]` | `MILEAGE_PNT1~6` | |

### 6-5. 상담사 (`Counselor`)

| 우리 필드 | 현행 컬럼 (`COM_CON_INF`) |
|---|---|
| `id` | `CONID` |
| `name` | `CON_NM` |
| `internal` | **`INOUT_GB`** (내부/외부 위촉) |
| `areas` | `CON_AREA1`(진로) `CON_AREA2`(취업) `CON_AREA3`(이력서·자소서 클리닉) |
| `methods` | `CON_TYPE1`(방문) `CON_TYPE2`(온라인) `CON_TYPE3`(클리닉) |
| `availableWeekdays` | `CON_W1~5` (월~금) |
| `place` / `placeRoom` | `PLACE` / `PLACE_NUM` |
| `onlineLimit` | `LIMIT_GB` / `LIMIT_NUM` |
| `targetNote` | `TARGET` (신청대상 설명) |
| `collegeCodes` | `COM_CON_TAR.DAEHAK_CD` (담당 단과대) |
| `active` | `STATUS` |
| — | `CONPWD` — **외부 상담사 자체 비밀번호 (SSO 밖 인증)** |

### 6-6. 배정 (`DeptAssign`)

| 우리 필드 | 현행 컬럼 (`FU_ASS_DEPT` / `TB_CARR_PROF_ASSI_DEPT`) |
|---|---|
| `deptCode` | `DEPT_CD` (학과) |
| `majorCode` | `MAJOR_CD` (전공) |
| `userId` | `USR_ID` (= `INTG_UID`) |
| `userName` | `USR_NM` (스냅샷) |
| `role` | — (조교 / 교수 구분, 우리 추가) |
| `assignedAt` / `assignedBy` | — (**신설 — 현행에 없음**) |

---

## 7. 코드 체계 — 분류·그룹·권한 코드 <span>★ 유지보수의 핵심</span>

현행은 코드가 **세 군데에 흩어져** 있다: ① `SY_CODE` 테이블(3,874행) ② SQL의 `CASE WHEN` ③ JSP의 `<th>` 하드코딩.
그래서 역량명 하나를 바꾸려면 8곳 이상을 고쳐야 하고, 실제로 `1101`(학부 재학생) 같은 신분코드가 **sqlmap 229곳에 리터럴로 박혀** 있다. **우리는 코드를 한 곳에 모은다.**

### 7-0. 운영 규칙

| # | 규칙 |
|---|---|
| 1 | **코드는 `src_v2/data/codes/` 단일 소스에 둔다.** 화면·JSON 어디에도 한글 라벨을 값으로 쓰지 않는다 |
| 1-1 | **★ 구조 코드는 이 TypeScript가 정본이고, DB 코드 테이블은 시드로 생성한다** <span>(2026-09-08)</span> — 코드값이 **타입**이어야 컴파일 타임에 오타가 잡힌다(§7-2 `STUDENT_ENROLLED` 집합 상수가 대학원생 누락을 막는 방식). DB를 정본으로 두면 그 안전장치가 사라진다. `legacy` 열은 TS에 그대로 두고 시드가 DB로 옮긴다.<br>**⚠️ 이 규칙은 구조 코드 전용이다** — 표시·분류만 하는 **운영 코드**(24주제 · 유형 **명칭** · 진단 검사 명칭 · 학과↔조교 배정 등)는 **DB가 정본이고 관리자 화면에서 고친다.** 두 층의 경계와 테이블 설계는 `DB.md` §8-5 |
| 2 | **모든 코드 항목은 4열을 갖는다** — `code`(우리 값) · `label`(표시) · `legacy`(현행 값) · `active`. **`legacy`가 곧 이관 매핑표다** (§5 원칙 13) |
| 3 | **2단 계층을 지원한다.** 현행 `SY_CODE.UP_GRP_CODE`/`UP_CODE` 패턴을 계승 (상담분야 → 세부분야) |
| 4 | **코드값은 의미 있는 영문 상수.** `'1'`·`'0002'` 같은 현행 숫자는 `legacy` 열에만 두고 화면 로직에 노출하지 않는다 |
| 5 | **폐지 코드는 지우지 않고 `active: false`.** 과거 이벤트 레코드가 그 값을 참조한다 |
| 6 | **신분·학년·학적으로 대상을 거를 때 리터럴 금지.** 반드시 §7-2 `UserType` 집합 상수를 쓴다 — 현행이 대학원생을 잃은 원인이 이것이다 |
| 7 | 코드는 **기동 시 1회 로드 후 메모리 캐시.** 현행은 매 요청 DB를 쳐서 `SY_CODE` 참조 statement가 108개다 |

> **구현 상태 — `src_v2/data/codes/` 는 만들지 않았고, 만들 계획도 없다** <span>(2026-09-09 갱신)</span>
> 규칙 1이 지목한 디렉터리 대신 **각 도메인 파일이 자기 코드를 갖는다**(`careerProcess.ts`의 유형·주제,
> `schema/program.ts`의 비교과 코드 등). DB 코드 테이블(`dc.code_item`)의 시드도 이미 그렇게 만들어졌다
> (migration `010` 헤더 참조). **한 디렉터리로 다시 모으지 않는다** — 도메인 파일이 타입·라벨·헬퍼를
> 함께 들고 있어야 그 도메인만 읽고 작업할 수 있고, 모아 두면 전 도메인이 한 파일에 얽힌다.
>
> 규칙 1이 실제로 지키는 것은 **"한글 라벨을 값으로 쓰지 않는다"** 이며 그 부분은 유효하다.
>
> ⚠️ **필요한 코드나 테이블이 DB에 없으면 직접 SQL을 치지 않는다.** 마이그레이션 파일을 추가하는
> 경로로만 바꾼다(`DB.md` §8-4 — 스키마 정본은 손으로 쓴 SQL이다). 운영 코드 **항목**을 더하는 것은
> 관리자 화면에서 한다(`DB.md` §8-5).

---

### 7-1. 권한 코드 `AuthCode` <span>(백오피스 전용)</span>

**중요 — 부여 방식이 두 갈래다.** 역할 기본권한은 **코드가 자동 부여**하고, `SY_AUTH_USER` 테이블에서 오는 것은 추가권한뿐이다. (`PUtil.java:1002-1031`)

| 코드 | 역할 | 부여 방식 | 우리 구현 |
|---|---|---|---|
| `AUTH0000` | 비로그인 | 자동 (세션 없음) | 진입 차단 |
| `AUTH0001` | 학생 | **자동** — `UserType`이 학생군 | `src_v2/` |
| `AUTH0002` | 조교 | **자동** — `ASSISTANT` + `FU_ASS_DEPT` 배정 | `src_admin/` |
| `AUTH0003` | 교수 | **자동** — `FACULTY` | `src_admin/` |
| `AUTH0004` | 직원 | **자동** — `STAFF` | 보류 |
| `AUTH0005` | 상담센터 총괄 | 명시 부여 | `src_admin/` |
| `AUTH0006` | **슈퍼관리자** | 명시 부여 (`SY_AUTH_USER`) | `src_admin/` |
| `AUTH0012` | 상담사 | 명시 부여 + `COM_CON_INF` 등록 | `src_admin/` |
| `AUTH0008` / `AUTH0010` | 기업 / 멘토 | 자동 (외부 로그인 경로) | 보류 |
| `AUTH0011` | 교무·학사관리과 | 명시 부여 | 보류 |

> **학생은 권한 테이블에 등록하지 않는다.** 52명뿐인 `SY_AUTH_USER`는 전체 권한자 명단이 아니라 **추가권한 보유자 명단**이다.
> 통제 범위는 **메뉴까지**다. "어떤 학생 행을 볼 수 있나"는 §2 `AccessScope`가 전담한다 — 현행처럼 SQL `<if>`에 흩뿌리지 않는다.

### 7-2. 신분 코드 `UserType` <span>★ 대학원 지원의 1차 관문</span>

`V_USR_INF.USER_TY_CD` (학사DB 발급, 4자리). **집합 상수로만 쓴다.**

| 우리 코드 | 라벨 | `USER_TY_CD` | 세션 1글자 |
|---|---|---|---|
| `STU_UG` | 재학생-학부 | `1101` | `S` |
| `STU_UG_ALUM` | 졸업생-학부 | `1102` | `S` |
| `STU_GR` | **재학생-대학원** | `1201` | `S` |
| `STU_GR_ALUM` | **졸업생-대학원** | `1202` | `S` |
| `FACULTY` | 교원 | `1301` | `P` |
| `STAFF` | 직원 | `1401` | `M` |
| `ASSISTANT` | 조교 | `1501` | `A` |
| `COUNSELOR` | 상담사 | — (`COM_CON_INF` 조회 후 덮어씀) | `T` |

**필수 집합 상수** — 화면·로더는 이것만 쓴다.

```ts
export const STUDENT_ENROLLED = ['STU_UG', 'STU_GR'] as const   // 재학생 전체 ★ 기본값
export const STUDENT_ALL      = ['STU_UG','STU_UG_ALUM','STU_GR','STU_GR_ALUM'] as const
export const STUDENT_UG_ONLY  = ['STU_UG'] as const              // 학부 한정이 정책일 때만
```

> ⚠️ **현행의 `1101` 리터럴 229곳을 그대로 옮기면 대학원생이 다시 사라진다.** 이관 시 각 지점을 위 집합으로 치환하되, **`STUDENT_UG_ONLY`를 쓰는 곳은 "왜 학부만인지" 주석을 남긴다**(예: 학부 기준 취업통계).

### 7-3. 학적·재직 상태 `EnrollStatus`

`V_USR_INF.HOFC_STA_CD` + `SY_CODE` 그룹 `0034`.

| 코드 | 라벨 | 현행 | 비고 |
|---|---|---|---|
| `ENROLLED` | 재학 | `0001` | **목록 기본 필터** |
| `LEAVE` | 휴학 | | |
| `GRADUATED` | 졸업 | | `V_USR_INF`에 대량 누적 |
| `COMPLETED` | 수료 | | 대학원 수료생 — 대학원 지원 시 누락 주의 |
| `EXPELLED` | 제적·자퇴 | `OUT_STAT` | |
| `EMPLOYED` / `RETIRED` | 재직 / 퇴직 | `89` / `90` | **교직원 전용** — 같은 컬럼을 학생/교직원이 공유한다 |

### 7-4. 과정 구분 `CourseType` <span>★ 대학원 지원의 2차 관문</span>

`V_DEP_INF_ALL.UNIV_CODE`.

| 코드 | 라벨 | 현행 | 건수 |
|---|---|---|---|
| `UNDERGRAD` | 학부 | `00` | 725 |
| `MASTER` | 대학원 석사 | `01` | 364 |
| `DOCTOR` | 대학원 박사 | `02` | 69 |
| `PROFESSIONAL` | 전문·특수대학원 | `03`~`12` | 226 |
| `ETC` | 기타 | `90` | 21 |

### 7-5. 조직 종류 `DeptKind`

`V_DEP_INF_ALL.GRP_CD` + `LVL`.

| 코드 | 라벨 | 현행 `GRP_CD` | `LVL` | 건수 |
|---|---|---|---|---|
| `COLLEGE` | 단대 | `0001` | 1 | 16 |
| `DEPT` | 학과 | `0002` | 2 | 599 |
| `MAJOR` | 전공 | `0003` | 3 | 808 |
| `ORG` | 기관 | `0004` | 4 | 498 |

> 대학원 조직 자체(`대학원`·`교육대학원`·`행정대학원`…)는 **`ORG`(기관)** 로 등록돼 있고, 대학원 학과는 `DEPT`로 학부 학과와 나란히 있다. **상위조직이 `COLLEGE`인 것만 훑으면 대학원이 통째로 빠진다** — 현행 `Fu.assDeptList2`의 실패 원인.

### 7-6. 상담 `CounselType` / `CounselStatus` / `CounselLink`

**유형** — `COUNSEL_MASTER.COUNSELTYPEIDX`

| 코드 | 라벨 | 현행 |
|---|---|---|
| `CAREER` | 진로 | `1` |
| `JOB` | 취업 | `2` |
| `PSYCH` | 심리 | `3` |
| `PROF` | 교수상담 | — (`CON_PROF_INFO` 별도 테이블) |

**트랙** `CareTrack` — 진로취업 상담에만 있는 축이다. 유형(`CounselType`)과 직교한다. 단일소스 `src_v2/data/counselTrack.ts`.

| 코드 | 교직원 표기 | 학생 표기 | 선행조건 | 문진표 | 로드맵 |
|---|---|---|---|---|---|
| `general` | 일반 | 일반 진로·취업 상담 | **없음** | 없음 | 생성 안 함 |
| `care7` | CARE 7+ 연계 | CARE 7+ 연계 상담 | 진단 2종 완료 | 있음 | **생성·재생성** |

> **NULL은 `care7`이다.** 신설 필드라 이관 데이터와 기존 시드에 없는데, 없는 것을 `general`로 읽으면 이미 상담을 마친 학생의 로드맵·역량강화·취업지원이 한꺼번에 잠긴다. 판정은 `isCare7()` 한 곳에서만 한다.
> 표기가 둘인 이유는 자리마다 폭이 달라서다 — 접수함 배지에는 짧게, 학생 화면 카드 제목에는 길게. **문자열을 화면에 박지 않고 `CARE_TRACK_LABEL`에서 받는다.** 프로세스 정의는 `PROCESS.md` §2-1.

**상태** — `COUNSEL_MASTER.COUNSELSTATUSIDX`

| 코드 | 라벨 | 현행 | 일으키는 주체 |
|---|---|---|---|
| `REQ` | 신청 | `1` | 학생 (또는 상담사 대리등록) |
| `CONFIRMED` | 확정 | **없음 — 우리 추가** | 상담사 |
| `DONE` | 완료 | `2` | 상담사 (결과 작성 시) |
| `CANCEL_STU` | 학생취소 | `3` | 학생 — **상담일 3일 전까지만** |
| `CANCEL_CNS` | 상담사취소 | `4` | 상담사·관리자 |

> 현행은 **배정 대기 상태가 없다.** 학생이 슬롯을 고르는 순간 상담사가 확정된다. 재배정은 `CONSULTID`만 바꾸고 **상태도 이력도 남지 않는다** → 우리는 `CONFIRMED` 단계와 배정 이력을 신설한다.

**연계 이송** `CounselLink` — 상태가 아니라 **완료 시 부가정보**다. 라벨이 상담 유형에 따라 달라진다.

| 코드 | `JOB`(취업상담)일 때 | `CAREER`(진로상담)일 때 | 현행 |
|---|---|---|---|
| `NONE` | 미결정 | 미결정 | `0001` |
| `SWAP` | **진로** | **취업** | `0002` |
| `OTHER_DEPT` | 타부서 | 타부서 | `0003` |
| `ADVISOR` | 지도교수 | 지도교수 | `0004` |
| `ETC` | 기타 | 기타 | `0005` |

보낸 쪽 `link.sent`/`link.sentType`, 받은 쪽 `link.received`/`link.receivedType`. **현행은 연계해도 새 상담건이 생기지 않고 컬럼만 기록된다** — 접수 여부는 상담사가 수기 갱신. 우리는 연계를 **새 `CounselRequest` 생성**으로 바꾼다.

### 7-7. 교수상담 `ProfCounsel`

`CON_PROF_INFO`. **상담사 상담과 상태값 체계가 다르다 — 합치지 말 것.**

| 축 | 코드 | 현행 |
|---|---|---|
| 상태 | `REQ` 신청 / `DONE` 완료 / `CANCEL_STU` 학생취소 | `0001` / `0002` / `0003` |
| 방식 | `ONLINE` / `OFFLINE` | `CON_KIND` = `ON` / `OF` |
| 세부구분 | 전공 및 학업 · 진로 · 취업 · 봉사 및 실습 · 사제동행프로그램 · 기타 | `CON_TYPE` (`SY_CODE` 그룹 `0131`) |
| 예약 여부 | `RESERVED` / `WALK_IN` | `ISRESERVATION` |

> 오프라인 교수상담 슬롯은 **상담사와 같은 `BASICSETTING` 테이블**을 쓴다(`CONSULTANTID`에 교수 `INTG_UID`가 들어감). 슬롯 모델을 상담사 전용으로 설계하면 안 된다.
> `CON_PROF_INFO`는 신청 시점의 `USER_TY_CD`·`STU_SCHGR`를 **스냅샷으로 저장**한다 — §5 원칙 4의 현행 선례.

### 7-8. 비교과 신청 상태 `ApplyStatus`

`EP_PRM_APP.STATUS`. 현행이 **단계별로 쓸 수 있는 값이 갈린다**(`APP_GB`=A 신청자관리 / C 선발자관리).

| 코드 | 라벨 | 현행 | 단계 |
|---|---|---|---|
| `REQ` | 신청(접수대기) | `1` | 신청자관리 |
| `RECEIVED` | 접수완료(대기) | `7` | 신청자관리 |
| `SELECTED` | 선발 | `2` | 양쪽 |
| `REJECTED` | 탈락 | `3` | 신청자관리 |
| `CANCELED` | 취소 | `4` | 신청자관리 |
| `ATTENDED` | 참석 | `8` | 선발자관리 |
| `NOSHOW_1` | **불참 (벌점 1점)** | `9` | 선발자관리 |
| `NOSHOW_2` | **불참 (벌점 2점)** | — *(신설)* | 선발자관리 |
| `NOSHOW_3` | **불참 (벌점 3점)** | `10` | 선발자관리 |
| `COMPLETED` | 수료 | `5` | 선발자관리 |
| `INCOMPLETE` | 미수료 | `6` | 선발자관리 |
| `DELETED` | 삭제 | `9999` | 양쪽 |

> **노쇼 벌점은 신설이 아니라 계승**이다. 현행이 이미 2단계 벌점(1점/3점)을 상태값에 내장하고 블랙리스트 화면도 운영한다.
> ⚠️ **`NOSHOW_2`(2점)만 우리 쪽 신설**이다 — 현행 코드값이 없으므로 이관 시 `SY_CODE`에 값을 추가해야 한다.
> 집계 정의도 그대로 계승: 신청자수 = `≠CANCELED` · 수료자수 = `COMPLETED`.
> ⚠️ 현행은 그룹형 수료 판정이 두 벌이다(`EP_PRM_MAPPING.STATUS='2'` vs `EP_PRM_APP.STATUS='5'`) — **우리는 `ApplyStatus` 하나로 통일한다.**

### 7-9. 역량 `Competency` <span>(2026-07-31 확정)</span>

**현행이 쓰는 것은 아래 계열이다.** 화면 표시는 5종이고 6번째 `HUMANITY`는 전 화면에서 주석 처리되어 있다 — **스키마는 6, 화면은 5.**

| 코드 | 라벨 | 현행 | 상태 |
|---|---|---|---|
| `LOCAL_LEADER` | 지역형리더 | `PA_1` / `CAP_PER1` | 활성 |
| `CREATIVE` | 창의적사고 | `PA_2` | 활성 |
| `CONVERGENCE` | 실용적융복합 | `PA_3` | 활성 |
| `COMMUNICATION` | 의사소통 | `PA_4` | 활성 |
| `GLOBAL` | 글로벌 | `PA_5` | 활성 |
| `HUMANITY` | 인문 | `PA_6` | **비활성** — 되살릴지 정책 결정 필요 (§9) |

**"역량"이라는 축이 3개 공존한다. 셋은 서로 다른 테이블이고 라벨만 공유한다 — 화면을 만들 때 어느 축인지 명시할 것.**

| 축 | 무엇 | 현행 테이블 |
|---|---|---|
| **진단 설문** | 회차별 응시 결과 | `CA_SURVEY` · `CA_SURVEY_QUS` · `CA_SURVEY_TAR` |
| **누적 점수** | 교과(`SUB_PA_n`) + 비교과(`EXT_PA_n`) 달성률 | `CA_GOAL`(학과 목표) · `CA_GOAL_DATA`(학생별) |
| **마일리지 배분** | 항목별 역량 반영비율 | `EX_ITEM` — **컬럼명이 아직 옛 체계**(`job_rate`·`fusion_rate`…)인데 화면 라벨만 바꿔 달았다 |

하위역량 **17종**(`CAP_QUS_GB2`)도 존재한다 → 전체 목록은 `CURRENT.md` §2-3.

### 7-10. 진단·상담 코드 <span>`DiagnosisTest` · `StudentType` · `CounselTopic`</span>

> **프로세스 전체(게이팅 · 6유형 정의 · 24주제 내용 · 로드맵 3축 · 승급 규칙)는 `PROCESS.md`가 단일 소스다.**
> 여기에는 **화면·JSON에서 쓸 코드값**만 둔다.

**진단 코드** `DiagnosisTest`

| 코드 | 의미 | 비고 |
|---|---|---|
| `CCORE` | 필수 핵심진단 | 전 기능의 관문. 완료 시 6유형 산출 |
| `C1`~`C6` | 유형별 후속진단 | 유형 번호와 정렬. 상담 전 1종 응시 |

**유형 코드** `StudentType` · **계층** `Tier` · 그에 딸린 매핑

| 코드 | 라벨 | `tier` | 후속진단 | 상담주제 | 비교과 범위 |
|---|---|---|---|---|---|
| `T1` | 진로탐색형 | `MID` | `C1` | `A01`~`A04` | 진로탐색·진로설정 |
| `T2` | 진로설정형 | `MID` | `C2` | `A05`~`A08` | 진로탐색·진로설정 |
| `T3` | 역량성장형 | `HIGH` | `C3` | `A09`~`A12` | 전체 |
| `T4` | 취업준비형 | `HIGH` | `C4` | `A13`~`A16` | 전체 |
| `T5` | 취약관리형 | `LOW` | `C5` | `A17`~`A20` | 🔲 미정 |
| `T6` | 우수인재형 | `HIGH` | `C6` | `A21`~`A24` | 전체 |

**상담주제 코드** `CounselTopic` — `A01`~`A24` (4주제 × 6유형). 주제명·상담목표는 `PROCESS.md` §5.

- 이전 표기 `S01`~`S24`는 쓰지 않는다.
- `legacy`는 현행 `SY_CODE` 상담분야 2단 그룹(`0145`→`0147`)과 대조해 채운다.

**단일소스** — `src_v2/data/careerProcess.ts`에 위 코드·매핑·게이트 상태만 둔다.
`DIAGNOSIS_BY_GRADE` / `getGradeTests()` 같은 학년 기반 배정은 사용하지 않는다.

> **★ 판정식은 미확정이다.** 문항·판정 로직을 코드에 고정하지 않는다 — 유형은 **주입받는 값**으로 다룬다. → `PROCESS.md` §9
> **계승 범위** — 현행 역량진단(`CA_SURVEY`)의 **"회차 개설 → 대상자 지정 → 응시 → 결과" 4단 뼈대**와 **"진단은 상담에 종속되지 않는다"** 는 원칙만 가져온다.

### 7-11. 첨부 `FileRef`

현행 `SY_FILE`의 **다형 참조 모델을 그대로 계승한다.** 도메인마다 첨부 테이블을 만들지 않는다.

| 우리 필드 | 현행 | 의미 |
|---|---|---|
| `ownerKind` | `TAB_NM` | 어느 도메인의 첨부인가 (문자열 구분자) |
| `ownerId` | `TAB_SEQ` | 그 도메인 레코드의 PK |
| `slot` | `FILE_CD` | 같은 레코드 안 첨부 종류 (본문 / 결과보고서 …) |
| `realName` / `storedName` / `path` / `size` / `ext` | `REAL_FILE_NM` / `TRANS_FILE_NM` / `FILE_PATH` / `FILE_SIZE` / `FILE_TYPE` | |
| `deleted` | `DEL_YN` | **논리삭제** |

> 현행은 업로드 파일을 **웹 문서 루트 안**에 두고 확장자를 부분문자열로 검사한다. **이 두 가지는 계승하지 않는다** — 저장은 웹루트 밖, 확장자는 완전일치, 다운로드는 소유자 검증.

### 7-12. `SY_CODE` 그룹코드 대장 <span>(소스에서 확인된 것)</span>

이관 시 **`GRP_CODE` 단위로 우리 코드 테이블에 대응**시킨다.

| 그룹 | 용도 | 우리 대응 |
|---|---|---|
| `0024` | 상담사 상태 | `Counselor.active` |
| `0034` | 학적·재직 상태명 | §7-3 `EnrollStatus` |
| `0053` · `0054` · `0103` | 상담 세부분야 (취업전략센터 / 학생과 / 기초교육원) | `CounselTopic` (부서별) |
| `0060` | 비교과 그룹 상태 | `Program.groupStatus` |
| `0131` | **교수상담 세부구분** | §7-7 `CON_TYPE` |
| `0143` · `0146` | 상담 유형 계열 | §7-6 `CounselType` |
| `0145` → `0147` | **상담분야 → 세부분야 (2단 계층)** | `CounselTopic` 부모/자식 — §7-0 규칙 3의 실례 |
| `0150` | 역량 | §7-9 `Competency` |
| `0012` · `0037` | **현행 코드관리 화면에서 숨김 처리** | 정체 미확인 — 이관 전 확인 필요 |

> 그룹코드 전수는 `docs/dbmeta/out/E4_tables.csv`(3,874행)에 있다. **이관 전 이 CSV를 코드 테이블 초기 데이터로 변환한다.**

### 7-13. 로드맵 코드 <span>`RoadmapAxis` · `RoadmapEntry` · `CellStatus`</span>

> **축의 정의·칸 수·이행률 공식·재생성 주기는 `PROCESS.md` §6이 단일 소스다.** 여기에는 코드값만 둔다.

**축 코드** `RoadmapAxis` — 로드맵 1개는 이 3축으로만 나뉜다.

| 코드 | 라벨 | 기본 칸 수 | 학기 중 증가 |
|---|---|---|---|
| `IAP` | IAP 실행 | 5 | **가능** (프로그램 개설로 추가 — §6-4) |
| `CORE` | 핵심역량 수행 | 5 | 없음 |
| `GROWTH` | 내 성장 활동 | 5 | 없음 |

> ⚠️ `IAP`는 **축 이름일 뿐**이다. 폐기된 IAP 유형 `R1`~`R6`과 무관하다 — `PROCESS.md` §6-2.
> ⚠️ **단기·중기·장기(`TermDetail`)는 폐기됐다.** 칸에 시기 라벨을 새로 붙이지 않는다.

**로드맵 편입** `RoadmapEntry` — 비교과 프로그램 개설 시 지정(§6-4). `Program.roadmapEntry`.

| 코드 | 라벨 | 칸 수명 | 이행률 분모 |
|---|---|---|---|
| `NONE` | 미편입 | 칸을 만들지 않음 | 제외 |
| `RECOMMEND` | 추천 | **프로그램 신청 마감일시까지** (미체크 칸은 증발) | 살아 있는 동안만 |
| `REQUIRED` | 필수 | 영구 | **포함** |

**칸 상태** `CellStatus` — `TODO` / `DONE`. IAP 칸은 프로그램 **이수 결과가 `COMPLETED`(수료)일 때 자동으로** `DONE`이 된다. 선발·출석은 완료가 아니다 — `PROCESS.md` §6-4.
결과를 되돌리면 칸도 함께 `TODO` 로 열리며, 두 변경은 서버의 한 트랜잭션이다(`DB.md` §8-6).

**단일소스** — 축·편입 코드와 이행률 계산은 데이터층 한 곳에 둔다. 화면이 칸 배열을 받아 세지 않는다(CLAUDE.md 규칙 10).

---

## 8. 대학원 지원 요건 <span>(필수)</span>

**대학원 학생·조교·교수가 모두 시스템을 써야 한다.** 데이터는 학사DB에 이미 있다 — 막고 있는 것은 전부 **애플리케이션 쪽 하드코딩**이다.

### 8-1. 막혀 있는 지점 <span>(요약)</span>

관문이 **두 겹**이다. 학과 트리만 고치면 절반만 열린다.

| 관문 | 증상 | 우리 대응 |
|---|---|---|
| **① 신분코드** | 대학원생이 **학생 목록·검색·배정 대상에서 사라진다** | §7-2 `STUDENT_ENROLLED` 집합 상수로만 판정. **리터럴 금지** |
| **② 학과 트리** | 대학원 **학과 자체가 목록에 안 뜬다** | 대학원 포함 트리 사용 + `USE_YN`을 `ON`절로 + 기관 상위 허용 |

> **→ 현행에서 막히는 지점(화면·SQL·리터럴 분포)은 `CURRENT.md` §4-6·§4-7이 단일 소스다.**

### 8-2. 반드시 열어야 하는 것 <span>(요구사항 확정)</span>

| # | 요건 | 우리 구현 |
|---|---|---|
| **A** | **대학원생이 비교과에 신청할 수 있어야 한다** | 참가대상 UI에 **대학원 학년 옵션 노출** · 자격 판정에 `STUDENT_ENROLLED` 사용 · **SMS·알림 대상에서 빠지지 않게** 한다 |
| **B** | **대학원생이 교수에게 상담을 신청할 수 있어야 한다** | 교수 목록을 **대학원 포함 조직 트리 기준 소속 매칭**으로 바꾸고, **직급 화이트리스트를 코드 테이블로** 뺀다 |
| C | 학과 트리는 대학원 포함본 기준 | §8-1 ② |
| D | **`courseType` 필터·배지를 모든 학생 목록에** | §7-4 |
| E | **학년 범위를 하드코딩하지 않는다** | 학부 1~4 / 대학원 1~3 — `courseType`에 따라 범위가 바뀐다 |
| F | 학과명 접미 `(대학원-석사)` 처리 | 그대로 표시하거나 `courseType` 배지로 대체 |
| G | **`(collegeCode, deptCode)` 쌍 식별** | 동명 학과가 과정별로 존재 — **학과명 매칭 금지** |
| H | 전공이 없는 학과도 목록에 노출 | 대학원 학과는 전공 미등록이 많다 |
| I | **`COMPLETED`(수료)를 학생 목록에서 빠뜨리지 않는다** | 대상 포함 여부는 정책 확인 (§9) |

> A·B의 **현행 상태**(이미 열려 있는 부분 / 실제로 막는 조건)는 `CURRENT.md` §4-7.

**전산원 요청 항목** (사용자 진행 예정)
1. `V_DEP_INF_ALL` 사용 승인 (대학원 포함 조직 트리)
2. `V_SUGANG` · `V_LECT_INF` 적재 활성화 — `GRAD_DIV`(과정구분) 포함
3. 학사 원본 뷰 `수강전체`(`VIEW_SUGANG_ALL`) · `수업개설`(`VIEW_SUUP_GAESUL`) 연계

---

## 9. 구현 전 확인 필요 <span>(임의 확정 금지)</span>

**미결 대장은 `DB.md` §9 하나다.** 여기서는 화면 구현에 직접 걸리는 것만 번호로 가리킨다.

| `DB.md` §9 | 항목 | 영향 화면 |
|---|---|---|
| #1 | 역량 `인문`(6번째)을 되살릴지 — 스키마 6 / 화면 5 | 역량 현황 · 비교과 결과 · 대시보드 |
| #2 | 관리자 제한일정을 학생 예약 화면에 반영할지 | 상담 제한일정(§3-1-⑦) · 학생 예약 |
| #5 | 대학원생 진단 학년 매핑 (학부 1~4 / 대학원 1~3) | 진단센터 · 검사 현황(§3-1-④) |
| #6 | 대학원 수료생(`COMPLETED`)을 상담·비교과 대상에 포함할지 | 전 학생 목록 |
| #7 | 비교과 참가자격 함수 `FN_GET_PRM_TRGT_YN()` 본문 | 비교과 신청(§3-6 S10) |
| #12 | 조교에게 학생 상세를 어디까지 공개할지 | 조교 학생현황(§3-2-①) |
| #13 | 교수상담을 타 학과까지 열지 | 교수상담 신청(§3-6 S4) |
| #14 | 비교과 운영자 역할을 신설할지 | §3-7 |
| #23 | **진단 문항·판정식** — 사용자가 추후 제공 | 진단센터 · 검사 현황(§3-1-④) |
| #24 | **취약관리형(하위) 비교과 신청 범위** | 비교과 신청(§3-6 S10) |
| #28 | **취약관리형 진입·탈출 정량 기준** | 진단 결과 · 학생 상세 |

**화면 쪽에서 이미 확정된 것**

| 항목 | 결정 |
|---|---|
| 상담 가능시간 방식 | **UI는 제한(불가) 등록** / **저장은 슬롯+사용여부**(현행 승계) — §3-1-⑦ |
| 진단 계승 범위 | **구조만 계승.** 문항·역량축·판정·대상자 규칙은 신설(§7-10) |
| **로드맵 개수** | **1개.** 재료 3(진단·상담 / 교과목·학과 / 외부활동) → **축 3**(IAP 실행 / 핵심역량 수행 / 내 성장 활동) × 5칸 — `PROCESS.md` §6 |
| **유형 강등** | **구현하지 않는다.** 승급만 — `PROCESS.md` §8 |
| `iapType` `R1`~`R6` | **폐기.** 상담에서 `studentType`을 확정하고 로드맵을 생성하므로 별도 분류가 없다 |
| 교수상담 도메인 분리 | `CounselRequestType`에 '교수'를 넣지 않는다 — 상담사 통계 오염 방지(§3-2-③) |
| 집단상담 도메인 분리 | 1:N은 `CounselRequest`와 별도 스토어(§3-1-⑨) |

---

## 10. AI 인프라 · AI 분석 DB <span>★ 별도 시스템</span>

### 10-1. 배치 구조

**클라우드 서버에 로컬 LLM + AI 프레임워크를 올린다.** AI가 분석해서 내놓는 결과값은
**서비스 DB가 아니라 별도 DB에 저장한다.**

```
  서비스 DB (존 A~L, 101테이블)          AI 분석 DB (존 Z, 11테이블)
  ┌──────────────────────────┐          ┌──────────────────────────┐
  │ 학사미러 · 상담 · 비교과      │  읽기   │ 입력 스냅샷 · 실행이력      │
  │ 진단 · 채용 · 포트폴리오      │ ──────> │ 적합도 · 근거 · 로드맵      │
  │ 게시판 · 알림               │         │ 첨삭 · 사용자평가          │
  └──────────────────────────┘          └──────────────────────────┘
              └────────── intg_uid ──────────┘
                    값으로만 이어진다 (FK 아님)
```

| 규칙 | 내용 |
|---|---|
| **조인키** | `intg_uid` **값** 하나뿐. 물리적으로 다른 DB라 **FK 제약을 걸 수 없다** — 정합성은 애플리케이션이 보장한다 |
| **방향** | **단방향.** 서비스DB는 AI DB를 참조하지 않는다. AI DB가 죽어도 상담·비교과·채용은 그대로 돌아가야 한다 |
| **이력** | 결과는 전부 append-only. 모델·프롬프트가 바뀌면 **과거 결과와 비교하지 않는다** |
| **재현성** | 모든 결과는 `model_id` + `prompt_id` + `snapshot_id` 를 달고 저장한다. 이 셋이 없으면 "왜 지난달과 다르지"에 답할 수 없다 |

### 10-2. 로드맵은 1개다 <span>★ 재료 3 → 축 3</span>

> **축의 정의와 생성·수정 규칙은 `PROCESS.md` §6이 단일 소스다.** 코드값은 §7-13. 여기에는 **저장 위치와 화면 매핑**만 둔다.

| | 무엇 | 사람이 손대나 | 저장 위치 |
|---|---|---|---|
| **로드맵** (정본) | **3축 × 5칸**(IAP 실행 / 핵심역량 수행 / 내 성장 활동) | ✅ 상담사 수정 · 학생 변경요청 | AI 초안 = `ai_roadmap`<br>**확정본 = `dc_roadmap`** (서비스DB) |
| **연 스냅샷** | 재생성 직전의 칸 구성 + 이행 결과 | ❌ append-only | `dc_roadmap_snapshots` |
| **직무 적합도** (참고) | 직무 추천 + 적합도 + 근거 | ❌ 읽기 전용 | `ai_job_fit` · `ai_job_fit_reason` (AI DB만) |

```
  재료A 진단·상담   ┐
  재료B 교과목·학과 ├──> AI 초안 ──> 상담사 조정 ──> 확정   dc_roadmap (초안/검토중/확정)
  재료C 외부스펙    ┘     3축 × 5칸      │             변경은 dc_roadmap_history 에 append
                                       └──> 참고지표   ai_job_fit (읽기 전용)

  IAP 축만 이후에 늘어난다 — 프로그램 개설 시 CARE 7+ 유형 + roadmapEntry(추천/필수)
  1년 주기: dc_roadmap_snapshots 에 얼리고 재생성
```

> **로드맵만 서비스DB에 실체를 갖는다.** 상담사가 고치고 학생이 변경을 요청하므로 조회·권한·이력이
> 필요하다. 적합도는 읽기 전용 산출물이라 AI DB에만 있으면 된다.

**화면 매핑**

| 화면 | 무엇 | 상태 |
|---|---|---|
| `/roadmap/ai` | **로드맵 정본** | 화면 ✅ / AI ❌ |
| `/roadmap/skill-tree` | **직무 적합도 참고 패널** | 화면 ✅ / AI ❌ |

> ⚠️ `/roadmap/skill-tree`는 **"학생의 기술역량 트리"가 아니다.** 라우팅에 `skill`이 들어갔다고
> 기술스택 화면으로 읽으면 안 된다. **직무 추천 + 적합도를 보여주는 참고용 페이지**다.

**적합도 입력 4열**

| 열 | 입력 | 소스 |
|---|---|---|
| 1 | **진단유형** | `dc_diag_attempt` → 6유형 |
| 2 | **1·2학년 수강과목 + 개설과목** | `dc_student_course` ∪ `dc_curriculum`(`rec_grade` ≤ 2) |
| 3 | **3·4학년 수강과목 + 개설과목** | 〃 (`rec_grade` ≥ 3) |
| 4 | **보유 자격증·어학** | `dc_student_cert` · `dc_lang_score` |

**폐기된 것 — 되살리지 말 것**

| 폐기 | 이유 |
|---|---|
| **"AI 로드맵 3종"(진로·직무·종합) 구분 · 종합로드맵** | **로드맵은 1개다** (2026-08-24 확정) |
| **과목↔스킬 수기 매핑** `dc_course_skill` · `dc_skill` · `dc_job_skill` · `dc_student_skill` | 수천 과목을 사람이 매핑하는 중간층은 운영 병목. **AI가 과목 리스트를 직접 읽는다** |
| **"직무 담기" 기능** · `dc_student_job_interest` | 학생의 목표 직무는 `dc_career_goal.target_job` 하나가 정식 소스 |
| **`dc_roadmap_override`** (전체 payload 덮어쓰기) | "무엇이 왜 바뀌었나"를 질의할 수 없고 상담사가 둘이면 최신 판정이 모호하다. **항목(`dc_roadmap_item`)을 고치고 `dc_roadmap_history`에 append** |

### 10-3. AI DB 테이블 <span>(존 Z)</span>

| 테이블 | 역할 |
|---|---|
| `ai_model_registry` | 모델 대장 — 기반모델 · 버전 · 파라미터 |
| `ai_prompt_template` | 프롬프트 대장 — 버전 · 출력스키마 |
| **`ai_input_snapshot`** | **★ 투입 입력 원본.** 위 4개 열이 그대로 저장된다. 학사 데이터가 나중에 바뀌어도 "그때 무엇을 보고 판단했는지"를 재현한다 |
| `ai_analysis_run` | 분석 실행 — 상태 · 토큰 · 소요시간 · 실행주체 |
| `ai_job_fit` | 직무 적합도 결과 |
| **`ai_job_fit_reason`** | **★ 판단 근거.** 근거 없는 퍼센트는 학생 신뢰를 못 얻는다 |
| `ai_roadmap` · `ai_roadmap_action` | AI 로드맵 — 서비스DB `dc_roadmap`(사람이 수정하는 것)과 분리 |
| `ai_job_reco` | AI 채용추천 (S17) |
| `ai_review` | AI 자소서·면접 첨삭 — 사람 첨삭(`dc_review_request`)과 분리 |
| `ai_feedback` | 사용자 평가 — AI 품질을 재는 유일한 신호 |

### 10-4. AI가 쓰이는 화면

| 화면 | 용도 | 상태 |
|---|---|---|
| `/roadmap/ai` | **로드맵 정본** — 3축 초안 생성 (§10-2) | 화면 ✅ / AI ❌ |
| `/roadmap/skill-tree` | **직무 적합도 참고 패널** (§10-2) | 화면 ✅ / AI ❌ |
| 진단 결과 | **진단 AI 분석 코멘트** — 로드맵 축A의 재료 | ❌ |
| 취업예측분석 · AI맞춤채용 | 합격 예측 · 기업 추천 | ❌ |
| AI 자소서 · 면접 | 작성 · 첨삭 | ❌ |
| 상담 기록 분석 | 상담기록지 → 요약·로드맵 항목 제안 | ❌ |

### 10-5. 미결 🔲

- 로컬 LLM **기반모델 선정** · 서버 사양(GPU) · 동시 처리량
- **분석 실행 시점** — 학생이 버튼을 누를 때만인가, 야간 배치도 도는가
- 학생 개인정보를 프롬프트에 넣는 범위 — **동의(`dc_agreement_consent`) 대상인가**
- AI DB **DBMS 선택** (서비스DB와 같은 Oracle인가, 별도인가)
- AI 결과의 **보존 기간** — append-only면 무한히 쌓인다

---

## 11. 참고 문서

| 문서 | 용도 |
|---|---|
| `CLAUDE.md` | 작업 규칙 · 코드 작성 규칙 · 문서 라우팅 (**사실은 담지 않는다**) |
| **`CURRENT.md`** | **현행 시스템의 사실** — 사이트맵 · 기능 · 권한 · DB구조 |
| **`PROCESS.md`** | **프로세스 최종안** — 게이팅 · 6유형 · 24주제 · 로드맵 3축 · 승급 |
| **`DB.md`** | 우리 DB 설계 · 소유권 경계 · 이관 전제 · **미결 대장** |
| `docs/DB_CURRENT.html` | 현행 DB 상세 (테이블 전수 · 다이어그램) |
| **`docs/DB_ERD_EDITOR.html`** | **신규 설계 ERD (편집 가능 · 132테이블 12도메인)** — 현재 기준. 좌측 레일에서 **역할별 사용 테이블** 필터 |
| **`docs/dbmeta/V_role.json`** | **역할 8종 × 테이블 132개 접근표** — 위 필터의 단일소스. `역할:권한[:범위]` 표기, 권한 `w`=생성·수정 / `r`=조회 |
| **`docs/DB_LEGACY_ERD.html`** | **현행 운영 DB ERD** (290노드 12도메인). 같은 엔진, 저장키만 다름. **관계선은 추정** — FK가 3건뿐이라 컬럼명으로 역산 |
| `docs/DB_ERD.html` | 이전 설계 ERD (읽기 전용 문서) |
| `DESIGN.md` · `UI.md` | 디자인 토큰 (새 팔레트·폰트 생성 금지) |
| `Counsel_README.md` · `STU_README.md` | 포털별 컨셉·사이트맵 원안 |
| `docs/dbmeta/*.sql` | 재조사용 추출 스크립트 |
| `origin/` | 현행 운영 소스 (MyBatis sqlmap 34 + 상담 컨트롤러 4) |
| **`docs/_analysis/`** | **현행 소스 전수 분석 결과** (2026-07-31, 원격 수행) — §3 화면 목록의 출처. 사이트맵 1,596 URL · 화면별 데이터흐름 22 · 역할별 접근범위 · 테이블 사용처 335 · 지정질문 15 답변 |

> `docs/_analysis/06_findings.md`는 **운영 시스템의 취약점 21건**을 담고 있다. 외부 공유 금지.

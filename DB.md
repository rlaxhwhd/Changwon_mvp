# DB.md — 우리 DB 설계 방향 <span>(설계 결정 · 이관 전제 · 미결 대장)</span>

> 국립창원대학교 드림캐치 **고도화** 프로젝트의 **데이터 설계 기준 문서**.
>
> **이 문서가 소유하는 것** — 다른 문서는 여기를 가리키기만 하고 내용을 복사하지 않는다.
>
> | 절 | 내용 |
> |---|---|
> | §3 | **데이터 소유권 경계** — 학사DB(읽기 전용) ↔ 드림캐치(전체 CRUD) |
> | §3-5 | **★ 학과 개편 대응 설계** — 신설·분리·통합·개명 |
> | §7 | **대학원 대응 설계 반영** |
> | §8-0 | **★ 이관 전제** — 마일리지만 제외, 전 도메인 이관 |
> | §8-4 | **개발 DB · 백엔드** — 스택 · 개발 서버 · DDL 정본 · 첫 대상 도메인 |
> | §9 | **미결 대장** — 확인 전까지 임의 확정 금지 |
>
> **개정 이력** — 2026-09-08 **§8-4 개발 DB·백엔드 신설**(FastAPI + PostgreSQL · DDL 정본 = 손으로 쓴 SQL · 첫 대상 = 상담 도메인) · **미결 #39 등재**(운영 DBMS 미확정).
> 2026-09-07 상담 2트랙(`careTrack`) 이관 전제 등재(§8-0·§8-2) · 로드맵 스냅샷 신설 등재(§8-2) · **미결 번호 정정**(로드맵 항목 옛 #12~#15가 일반 항목과 겹쳐 있었다 → #31~#34) · 신규 미결 #35~#38 등재 · #32 해소.
>
> **이 문서에 없는 것**
>
> | 내용 | 문서 |
> |---|---|
> | **현행 시스템의 사이트맵·기능·권한·DB구조** | **`CURRENT.md`** |
> | 진단→상담→로드맵 프로세스 | **`PROCESS.md`** |
> | 화면·기능 명세 · 필드 사전 · 코드 체계 | **`SPEC.md`** |
> | 매번 지켜야 할 코드 작성 규칙 | **`CLAUDE.md`** |
>
> | 근거 자료 | 내용 |
> |---|---|
> | **`CURRENT.md`** | **현행 실측 결과 정리본** — 아래 설계 결정의 근거 |
> | `docs/_analysis/` | 현행 소스 전수 분석 8파일 (⚠️ `06_findings.md`는 취약점 21건 — **외부 공유 금지**) |
> | `docs/DB_CURRENT.html` | 현행 DB 실사 상세 (다이어그램 3 + 표 17) |
> | `docs/DB_ERD_EDITOR.html` | **신규 설계 ERD** (편집 가능 · 132테이블) + 역할별 사용 테이블 필터 |
> | `docs/DB_LEGACY_ERD.html` | **현행 운영 DB ERD** (290노드) — 관계선은 컬럼명 역산 **추정** |
> | `docs/dbmeta/V_role.json` | 역할 8종 × 테이블 접근표 (위 필터의 단일소스) |
> | `docs/dbmeta/*.sql` · `out/*.csv` | 추출 스크립트 A~L (재조사용) · 조사 원본(gitignore) |

---

## 1. 조사 경위

**전산원에 DB 구조를 문의할 수 없는 사정**이 있어, 운영 중인 시스템을 직접 리버스 엔지니어링했다.

| 단계 | 방법 | 산출물 |
|---|---|---|
| 1 | DBeaver로 운영DB 접속 → Oracle 카탈로그 뷰(`USER_*`) 조회 | `docs/dbmeta/out/*.csv` |
| 2 | 운영 소스 수령 (`origin/sqlmap` 34개 + `origin/co` 컨트롤러 4개) | 스크립트 기반 SQL 전수 분석 |

**조사 원칙** — 읽기 전용 연결, 카탈로그 조회만, 행수는 옵티마이저 통계에서 읽음, 개인정보 미추출.

> **→ 조사 결과와 그 한계는 `CURRENT.md`가 단일 소스다.** 이 문서는 결과를 근거로 한 **설계 결정**만 담는다.

---

## 2. 현행 시스템 요약

> **→ `CURRENT.md` §0(개요) · §2(기능) · §4-1(DB 규모)로 이동했다.** 여기 복사하지 않는다.

설계에 직결되는 세 가지만 기억한다.

| 사실 | 설계에 주는 제약 |
|---|---|
| 실사용 테이블은 428개가 아니라 **100~150개** | 이관 범위가 그만큼이다 — 다대학 패키지 잔재는 가져오지 않는다 |
| **FK 3건 · PK 없는 테이블 122개(44%)** | 정합성을 애플리케이션이 전담한다. 신규 설계는 전 테이블 PK를 갖는데, **그대로 부으면 적재가 실패한다**(§9 #17) |
| **상태 변경 이력이 없다**(UPDATE 덮어쓰기) | 이력 테이블 신설이 불가피하다 — 이관 시 과거 이력은 복원 불가로 전제 |

---

## 3. 데이터 소유권 경계 <span>(확정)</span>

**한 줄 요약: 학사DB는 "누가·어디 소속인지"만 준다. "무엇을 했는지"는 전부 우리가 만든다. "무엇을 배웠는지(수강·성적)"는 받을 준비만 돼 있고 비어 있다.**

> **→ 현행이 실제로 무엇을 주고받는지(테이블·컬럼·행수·DB링크)는 `CURRENT.md` §4-2다.**

### 3-1. 경계 규칙

| 구역 | 대상 | 우리 권한 |
|---|---|---|
| **학사 유래** | 사람(학생·교직원) · 조직(단대·학과·전공) | **읽기 전용.** 수정 UI·로직을 만들지 않는다 |
| **학사 유래 (미적재)** | 수강 교과목 · 강의 · 성적 | 적재 활성화 요청 대상(§7) — **로드맵 축B의 전제 데이터** |
| **자체 소유** | 상담 · 진단 · 로드맵 · 비교과 · 채용 · 포트폴리오 · 게시판 | 전체 CRUD |

### 3-2. 조인키는 `INTG_UID` 하나

학생=학번, 교직원=사번. 컬럼명이 `USRID`·`USERID`·`STU_NO`로 갈려도 값은 같다.
**내부 surrogate를 새로 만들지 않는다** — 기존 상담 이력 24만 건이 이 키로 묶여 있다(§8-0).

### 3-3. 학사 마스터를 자체 테이블로 복사하지 않는다

미러 로더를 읽는다. 현행 `FU_CODE`(2020년 이후 갱신 0건)가 복사의 반례다.
불가피하게 미러링한다면 MERGE 컬럼 나열이 아니라 **스키마 드리프트 감지**를 붙인다(학사DB에 컬럼이 늘면 알림).

### 3-4. 학사 유래와 자체 발급 인원을 섞지 않는다

현행은 외부 위촉 상담사를 학사 적재본(`V_USR_INF`)에 직접 INSERT한다(`CURRENT.md` §4-2).
→ **야간 적재가 외부 인원을 지워버릴 위험이 상존한다.**
→ 우리는 `source: 'academic' | 'local'`로 분리하거나 별도 테이블로 둔다. 자격증명도 한 곳으로 모은다.

### 3-5. ★ 학과 개편 대응 <span>(신설·분리·통합·개명)</span>

**대학은 학과가 자주 신설·분리·통합·개명된다. 현행은 이걸 감당하지 못해 매번 사람이 개입해 왔다.**
운영 증상: **학사DB에는 있는 학과가 관리자 화면(`회원관리 > 조교학과 배정`)에만 없다** → 전산원에 학과 추가 요청 → 코드에 하드코딩 매핑.
이건 일회성 버그가 아니라 **상시 요구사항**이므로 설계에 넣는다.

> **→ 원인이 여러 겹인 구조(반입 배치 · 트리 이중화 · 필터 4개 · 더미 전공 행 · 파생 데이터 공백)는 `CURRENT.md` §4-6이 단일 소스다.**

#### 우리 설계 규칙

1. **복제하지 말고 조회한다.** 불가피하게 미러링한다면 MERGE 컬럼 나열이 아니라 **스키마 드리프트 감지**를 붙인다(학사DB에 컬럼이 늘면 알림).
2. **트리는 `V_DEP_INF_ALL` 하나만.** 두 벌 금지. 조회는 `getDeptTree()` **단일 진입점**을 거친다 — 현행처럼 227곳에 흩어지면 같은 문제가 반복된다.
3. **전공 없는 학과를 반드시 노출한다.** 전공 조건은 `ON` 절에. 더미 전공 행 금지.
4. **조직 필터를 SQL 리터럴이 아니라 데이터로** — `USE_YN`·`GRP_CD`·`UNIV_CODE` 플래그로만 거른다. 상위 조직 종류를 단대로 한정하지 않는다.
5. **`(collegeCode, deptCode)` 쌍으로 식별.** 학과명 매칭 금지.
6. **배정 `majorCode`는 NULL 허용 = "학부 전체"** 로 정의하고, 학생 조회가 `deptCode` 기준으로도 매칭되게 한다.
7. **이벤트 레코드에 학과 as-of 스냅샷**(코드 + 당시 명칭). 개편돼도 과거 상담·비교과 이력이 깨지지 않는다.
8. **학과 개편 이력 테이블 신설** — `DC_DEPT_LINEAGE(from_dept_cd, to_dept_cd, kind: 신설|분리|통합|개명, effective_from, reason)`. 현행에 없는 것이고, 없으면 **"작년 컴퓨터공학과 상담 실적"을 개편 후에 답할 수 없다.**
9. **★ 누락 감지 화면.** 학사DB 원본 ↔ 우리 트리를 대조해 **"학사DB에는 있는데 우리에 없는 학과 N건"** 을 관리자에게 상시 노출한다. 현행은 사람이 발견할 때까지 아무도 모른다.

---

## 4. 현행에서 확정된 것 → 설계 반영

> **→ 조교배정 화면의 학과 누락 SQL · 132종 조인 관계 · 역할별 화면 재사용 패턴은 `CURRENT.md` §4-6·§4-4·§1-5로 이동했다.**

설계로 옮긴 결론만 남긴다.

| 현행 사실 | 우리 설계 |
|---|---|
| 학생 조인이 `INTG_UID`로 통일돼 있다 | 그대로 계승 (§3-2) |
| 소속 조인이 세 갈래다(`DAEHAK_CD`·`HAKBU_CD`·`MAJOR_CD` / 교직원 `ORGID`) | 조직 참조를 **하나의 트리 조회 진입점**으로 통일 |
| 같은 화면을 `TYPE` 파라미터로 3역할이 공유한다 | **컴포넌트 하나 + 역할별 설정 객체**로 재사용 (`SPEC.md` §4-3) |
| 쿼리 이름과 실제 조회 대상이 한 단계 어긋나 있다 | 네이밍을 데이터 계층 이름과 일치시킨다 |

---

## 5. 권한·역할 체계

> **→ 현행 4테이블 RBAC 모델 · 역할 13개 · 부여 두 갈래 · 인가 실행부 실태는 `CURRENT.md` §3이 단일 소스다.**

**계승할 것과 고칠 것**

| | 내용 |
|---|---|
| **계승** | **4테이블 권한 모델**(역할 마스터 → 사람↔역할 → 역할↔메뉴 → 메뉴 트리)과 **자동/명시 두 갈래 부여**. 새 role 열거형을 만들지 말고 역할 코드를 추가한다 |
| **고칠 것 ①** | **"URL ↔ 필요권한" 매핑을 서버측 단일 지점에서 강제**하고, 권한이 없으면 핸들러 실행 전에 끊는다. 현행은 검사가 플래그일 뿐이고 REFERER가 있으면 검사 자체를 건너뛴다 |
| **고칠 것 ②** | **엑셀 export는 목록 API와 같은 권한·같은 쿼리를 탄다.** 현행은 14종 컨트롤러가 인가 로직이 없다 |
| **고칠 것 ③** | **행 수준 접근 제어를 한 곳에서 판정**한다 (`SPEC.md` §2 `AccessScope`). 현행은 SQL `<if>`에 흩어져 있고 파라미터 신뢰 지점이 있다 |
| **신설** | **데이터 열람 감사로그.** 현행은 권한 변경만 기록하고 상담기록·벌점 같은 민감 데이터의 열람 이력이 없다 |

---

## 6. 우리 프로젝트와의 기능 격차

> **→ 역할별 화면 목록·구현 상태·커버율은 `SPEC.md` §3이 단일 소스다.**

DB 관점에서만 의미 있는 두 가지를 남긴다.

**① 데이터는 있는데 받을 화면이 없는 도메인** — 이관 대상인데 화면이 없으면 이관이 무의미해진다.

| 도메인 | 현행 데이터 | 우리 화면 |
|---|---|---|
| 채용 지원 (`APPLICATIONMASTER`) | **1.7만 건** | ❌ 공고 열람만 있고 **지원 경로가 없다** |
| 진로설계 (`STU_COURSE_*`) | **1.4만 건** | ❌ 없음 |
| 이력서 첨삭 (`SS_JOB_RES`) | 워크플로 완비 | ❌ 없음 |
| 비교과 만족도·참여후기 | 운영 중 | ❌ 없음 |
| 비교과 그룹형·차수 (`EP_PRM_STEP`) | 운영 중 | ❌ 개인형만 |

**② 우리에게만 있는 것 (이관 대상 없음)** — 진단(`CCORE`·`C1`~`C6`) · **로드맵 1개 3축**(`PROCESS.md` §6) · 로드맵 변경요청함 · 성장경험일지 · 퀘스트/레벨 · 챗봇 · 열람 감사로그 · 상태 변경 이력.

---

## 7. 대학원 대응 <span>(필수 요건)</span>

**대학원 데이터는 학사DB에 이미 있다.** 막고 있는 것은 전부 애플리케이션 쪽 하드코딩이다.

> **→ 관문 두 겹의 실측(신분코드 리터럴 229곳 · 학과 트리 필터 4개 · 막히는 화면 5종)은 `CURRENT.md` §4-6·§4-7이 단일 소스다.**
> **→ 요구사항 A·B(비교과 신청 · 교수 상담신청)와 화면 구현 방법은 `SPEC.md` §8이다.**

### 설계 반영 사항

| # | 반영 |
|---|---|
| 1 | **대학원 포함 조직 트리를 쓴다.** 대학원 누락본을 쓰지 않는다 |
| 2 | **과정구분(학부/석사/박사/전문)을 필터·표시 축에 포함** |
| 3 | **학년 1~4 고정 금지.** 학부 1~4 / 대학원 1~3 |
| 4 | 학과명에 `(대학원-석사)` 접미가 붙어 온다 → 파싱하거나 과정구분 배지로 처리 |
| 5 | **동명 학과가 과정별로 여러 개** → `(단대코드, 학과코드)` 쌍으로 식별. **학과명 매칭 금지** |
| 6 | **★ 신분코드를 리터럴로 쓰지 않는다.** 학생 대상 판정은 집합 상수(`STUDENT_ENROLLED`)로. 학부 한정이 정책인 곳은 **이유를 주석으로 남긴다** — `SPEC.md` §7-2 |
| 7 | **대학원 수료생(`COMPLETED`)을 목록에서 빠뜨리지 않는다** — 대상 포함 여부는 정책 확인(§9 #6) |
| 8 | 진단 대상은 **학년이 아니라 C-CORE 결과 유형**으로 결정한다 → 학년 매핑 문제 자체가 사라진다(`PROCESS.md` §3) |

### 전산원 요청 항목

1. **대학원 포함 조직 트리(`V_DEP_INF_ALL`) 사용 승인**
2. **수강·강의 데이터(`V_SUGANG` · `V_LECT_INF`) 적재 활성화** — `GRAD_DIV`(과정구분) 포함
   → **로드맵 축B(수강 교과목·학과)와 직무 적합도의 전제**다. 테이블·DB링크·조인 쿼리가 이미 있고 **적재만 꺼져 있다**(`CURRENT.md` §4-2)
3. 학사 원본 뷰 **`수강전체`(`VIEW_SUGANG_ALL`)** · **`수업개설`(`VIEW_SUUP_GAESUL`)** 연계 — 뷰 이름을 알고 있으므로 구체적으로 요청 가능

---

## 8. 구축 방향

### 8-0. ★ 전제 — 이것은 이관 프로젝트다

**마일리지를 제외한 사실상 전 도메인이 현행 운영 데이터를 이관받는다.** 빈 DB로 출발하지 않는다.

| 도메인 | 이관 | 현행 규모 |
|---|---|---|
| 상담 (`COUNSEL_MASTER` · `COUNSEL_PROBLEM` · `COUNSEL_FAM`) | **O** | 5.4만 |
| 교수상담 (`CON_PROF_INFO` · `CO_ADVISER`) | **O** | 18.4만 |
| 비교과 (`EP_PRM` → `_STEP` → `_APP` → `_RESULT`) | **O** | 6.8만 |
| 상담사·배정 (`COM_CON_INF` · `COM_CON_TAR` · `FU_ASS_DEPT` · `TB_CARR_PROF_ASSI_DEPT`) | **O** | — |
| 채용·이력서 (`JOBSMASTER` 계열 · `SS_JOB_RES`) | **O** | 1.7만 |
| 역량 (`CA_SURVEY*` · `CA_GOAL*` · `TB_CARR_CAPA_STTS`) | **O** | 8.4만 |
| 공통코드·첨부·권한 (`SY_CODE` · `SY_FILE` · `SY_AUTH*`) | **O** | 3,874 등 |
| 진로설계 (`STU_COURSE_*`) | **O** | 1.4만 |
| **마일리지 (`EX_ITEM` · `_APP` · `_SCORE`)** | **X — 제외** | 1.1만 |
| 진단(`CCORE`·`C1`~`C6`)·로드맵·성장일지·퀘스트 | 대상 없음 (신설) | — |

**여기서 나오는 제약 5가지**

1. **`INTG_UID` 키 결정은 되돌릴 수 없다.** surrogate로 바꾸면 24만 건 상담 이력의 재매핑이 필요하다.
2. **모든 코드에 현행 값(`legacy`)을 반드시 남긴다.** 이것이 곧 이관 매핑표다. `legacy`가 비면 그 코드는 이관 스크립트를 쓸 수 없다 — `SPEC.md` §7-0.
3. **신설 컬럼은 NULL 허용 + 화면 폴백을 전제로 설계한다.** NOT NULL로 잡으면 이관이 막힌다.
   > **작동 예 — `careTrack`** (2026-09-07 신설). 진로취업 상담의 트랙(`general`/`care7`)은 현행 `COUNSEL_MASTER`에 없는 축이라 이관 데이터가 전부 NULL로 들어온다.
   > **NULL은 `care7`로 읽는다.** NULL을 `general`로 떨어뜨리면 이미 상담을 마친 학생의 로드맵·역량강화·취업지원이 한꺼번에 잠긴다 — 신설 컬럼이 **기존 행의 의미를 바꾸면 안 된다**는 것이 이 제약의 요지다.
   > 판정은 애플리케이션 한 곳(`isCare7()`)에서만 한다. 이관 정책 자체는 미결 **#36**.
4. **구조 변경은 "이관 매핑을 쓸 수 있는가"를 통과해야 한다.** 1:1이 아닌 변환(예: `COUNSEL_MASTER` 1행 → 신청/결과 2행 분해)은 변환 규칙을 함께 문서화한다.
5. **현행 데이터의 오염을 이관 시점에 정제할지 미리 정한다** — 고아 테이블 `COM_ASS_DEPT` 잔여 배정, `CON_GB` 1/3 라벨 중복 등.

### 8-1. 데이터 원칙

> **→ 원칙 12개는 `CLAUDE.md` §코드 작성 규칙이 단일 소스다.** 세 문서에 같은 목록을 두지 않는다.

DB 설계에서만 추가로 지키는 것.

| # | 원칙 |
|---|---|
| 1 | **`INTG_UID` 키 결정은 되돌릴 수 없다** (§8-0 제약 1) |
| 2 | **PK 없는 현행 테이블마다 "무엇이 같으면 같은 행인가"를 정한다** — 정하지 않으면 적재가 실패한다(§9 #17) |
| 3 | **이력 테이블을 함께 설계한다.** 현행에 이력이 없으므로 이관 시 과거 이력은 복원 불가로 전제한다 |
| 4 | **민감정보(상담 소견·심리검사)는 컬럼 분리 + 동의 이력 + 열람 감사**를 함께 둔다 |

### 8-2. 재사용 / 신설 판정

**그대로 계승**

| 도메인 | 계승 대상 |
|---|---|
| 사람·조직 | 사용자 미러 · 조직 트리(대학원 포함본) |
| 상담사 | 상담사 마스터 · 상담사↔단과대 매칭 |
| 비교과 | 3단 구조(프로그램→차수→신청) · **상태값 10종 + 노쇼 벌점 2단계** |
| 채용·이력서 | 채용공고 계열 · 이력서·자소서 첨삭 워크플로 |
| 시스템 | 공통코드(2단 계층) · 권한(4테이블 모델) · 첨부(다형 참조) · 동의 |

**구조 수정 후 계승**

| 도메인 | 수정 내용 |
|---|---|
| 상담 | 신청/결과 **분리** + `CONFIRMED` 상태 추가 + **배정 이력 신설** + **트랙 컬럼 `careTrack` 신설**(일반/CARE 7+ 연계 — `PROCESS.md` §2-1, NULL=`care7`) |
| 상담 가능시간 | 현행 슬롯 구조 승계 + `is_use`로 가능/불가 표현 (§9 ⚠). **교수 슬롯도 같은 테이블임에 주의** |
| 조교·교수 배정 | 조교 배정만 정본으로 승계(고아 테이블 폐기) → **`사용자↔조직 배정` 단일 테이블(역할 컬럼)로 통합** |
| 진단 | 현행 역량진단의 **4단 뼈대만** 계승. 문항·역량축·대상자 규칙은 신설 |
| 연계이송 | 컬럼 기록 → **새 상담건 생성**으로 |

**신설 불가피**

| 대상 | 비고 |
|---|---|
| **로드맵 1개 + 항목 + 변경이력** | `PROCESS.md` §6 |
| **로드맵 스냅샷** (append-only) | 재생성 전 칸 구성 + 이행 결과를 통째로 얼린다. **완료 칸을 이월하지 않으므로 지난 실적이 여기에만 남는다** — `PROCESS.md` §6-6 |
| 직무 적합도 + 판단 근거 | 읽기 전용, AI DB |
| 6유형·계층 판정 | 판정식은 미확정(§9 #23) |
| 성장경험일지 · 퀘스트·레벨·랭킹 · 학생×공고 매칭도 | 게임화·추천 |
| **열람 감사로그** | 현행에 없음 |
| **행 수준 접근 제어 통일** | `SPEC.md` §2 |
| **상태 변경 이력** | 현행은 권한변경 하나뿐 — 상담·비교과·채용은 UPDATE 덮어쓰기 |
| 교수 상담 노출 제어 | 현행 테이블 0행 |

**판정이 바뀐 것** <span>(2026-07-31 소스 분석 — 다시 뒤집지 말 것)</span>

| 항목 | 이전 → 현재 | 근거 |
|---|---|---|
| **진단** | 신설 불가피 → **구조 수정 후 계승** | 현행 역량진단이 **학생 단독 응시를 이미 지원**한다(4단 뼈대). 문항·역량축·대상자 규칙만 신설 |
| **노쇼 벌점** | 신설 → **그대로 계승** | 2단계 벌점이 상태값에 이미 있고 블랙리스트 화면도 운영 중 |
| **조교 배정 테이블** | 두 벌 병기 → **한 벌만 정본** | 다른 한 벌은 **읽는 statement 0건인 쓰기 전용 고아** — `CURRENT.md` §2-4 |
| **권한 부여 방식** | 테이블이 전부 → **코드가 자동 부여** | `CURRENT.md` §3-3 |

### 8-3. 이관 대장 <span>(도메인별 진행 상태 · 2026-09-09 갱신)</span> ★

**이 표가 「무엇이 남았나」의 정본이다.** 도메인을 하나 끝낼 때마다 여기를 고친다.
판정은 기억이 아니라 코드로 한다 — 데이터층 파일이 아직 `localStorage`/JSON 을 읽는지,
그 도메인 API 가 실제로 있는지 두 가지를 본다.

| # | 도메인 | 상태 | 남은 것 | 막고 있는 것 |
|---|---|---|---|---|
| 1 | **비교과** | **완료** | — | — |
| 2 | **채용·취업** | **완료** | — | — |
| 3 | **로드맵·IAP** | **완료** | — | — |
| 4 | **성장 활동** | **부분** | 오늘 미션 · 퀘스트/XP/레벨/랭킹 · STAR 판정(선발·마일리지·장학) · 채용 포트폴리오 제출 | **운영 정책 미확정**(#29 · #30 · `SPEC.md`:484) |
| 5 | **교수상담** | **완료** <span>(2026-09-11 · 0003)</span> | — | 교수 소속 라벨 폴백 제거 시점만 남음(#45) |
| 6 | **상담사 운영 설정** | 절반 | 가용시간 · 예약 제외시간 · 집단상담 | — |
| 7 | **진단 응시·채점** | 절반 | 문항 · 응답 · 응시 세션 · 채점 규칙 · 등급 판정 | **판정식 미확정**(#23 · `PROCESS.md` §9) |
| 8 | **공지·알림** | 거의 미착수 | 공지 테이블 · 알림 이력 · 읽음 상태 · CSV 내보내기 | — |
| 9 | **인증** | 미착수 | SSO · 실제 로그인 · 세션 · 운영 토큰 제거 · HTTPS | **학교 협의** |

> **완료 판정 근거** — pytest·`tsc -b` 통과만으로 완료로 적지 않는다. 두 SPA 를 실제로 띄워
> `/v2`(계획 없는 학생·확정 학생·재확정 대기 학생)와 `/admin`(진로·심리·교수·조교·시스템관리자
> 5역할)에서 **콘솔 오류 0**을 확인한 뒤 적는다. 1차 구현은 이 셋을 모두 통과하고도 화면이
> 죽어 있었다(계획 없는 학생의 프로필 필드 제거 · 부팅 로더의 권한 가드 누락).

**「절반」의 의미는 도메인마다 다르다** — 남은 양을 오해하기 쉬우므로 적어 둔다.

- **4 성장 활동의 「부분」** — 학생이 **직접 쓴 자료**(성장 기록·성장일지·프로젝트·스킬·자격·수상)와
  포트폴리오 조회/편집, STAR **조회**, 비교과 찜은 DB 로 갔다. 빠진 4건은 코드가 없어서가 아니라
  **운영 정책이 없어서** 뺐다 — 미션 문항·채점 기준, XP·레벨·랭킹 규칙, STAR 선발/장학 산식,
  제출 항목·열람 범위가 전부 화면 상수이거나 미결이다. 정책 없이 넣으면 임시 판정 로직이 되고
  실제 규칙이 오면 그 데이터를 버려야 한다(`CLAUDE.md` 14조).
  · 오늘 미션·퀘스트는 **화면 현행 유지**(서버 테이블 없음), 고정 레벨·랭킹은 제거·미지원.
  · STAR 는 기존 `dc.star_track` payload 를 **읽기만** 한다. 인증 단계·장학 구간은 `POLICY_PENDING` 으로
    내려보내고 화면이 null 을 0점·탈락으로 바꾸지 않는다.
  · 채용 포트폴리오 제출은 계속 503 이고 `canApplyWithPortfolio=false` 다.
- **5 교수상담** — 신청은 이미 상담 API 의 `PROF` 타입으로 들어간다. 남은 것은 프로필·기록·배정 3개다
- **6 상담사 설정** — 통계·대시보드는 API 스토어에서 파생되므로 이미 끝났다. 슬롯 관련 3개만 남았다
- **7 진단** — 응시 현황·결과 조회·코멘트·권유는 된다. 없는 것은 **채점 엔진 전체**이고,
  이것은 코드가 아니라 판정식이 없어서다. **임시 판정 로직을 만들지 않는다**(`CLAUDE.md` 14조)

**측정 지표** — 데이터층에서 실제로 `localStorage` 를 읽고 쓰는 파일 수.
활성 사용자 선택용 `session.ts` · `students.ts` 2개는 개발 스텁이므로 제외한다.

| 시점 | 파일 수 |
|---|---|
| 비교과 착수 전 (2026-09-08) | 22 |
| 비교과 완료 후 (2026-09-09) | 20 |
| 채용·취업 완료 후 (2026-09-09) | 16 |
| 로드맵·성장 완료 후 (2026-09-09) | **10** |
| 시드 JSON 퇴역 후 (2026-09-11 · 0003) | **1** — `psychTests.ts`(도메인 6). 팝업 「오늘 그만 보기」·채용 내보내기 키는 업무 데이터가 아니다 |

> 세는 규칙 — `localStorage.getItem/setItem` 을 **업무 데이터에** 쓰는 파일만 센다.
> 활성 사용자 키(`dc_active_staff`·`dc_active_student`)만 읽는 파일은 신원 조회이므로 세지 않는다.
> 채용에서 걷힌 것은 `jobsSource` · `jobApplications` · `jobApplicationEvents` · `jobWishlist` · `resumeMock` 5개다.
> 로드맵·성장에서 걷힌 것은 `roadmapGenerated` · `roadmapOverrides` · `roadmapRequests` ·
> `growthJournal` · `growthRecords` · `wishlist` 6개다. 화면에서는 `GrowthHome` 의 `useStoredList`
> 4키(`dc_growth_portfolio_*`)가 함께 사라졌다 — 그 훅이 **전 학생 공통 상수를 학생별 키에
> 자동 저장**해서 아무도 쓴 적 없는 실적이 모두에게 자기 것처럼 보이던 자리다.

**목록 밖 잔재** — 어느 항목에도 안 적혀 있어 그냥 지나치기 쉬운 것.

- **`src_admin/data/counselEvents.ts`** — `dc.counsel_event` 테이블은 있는데 프론트가 아직
  localStorage 에 상담 이력을 쌓는다. 상담 도메인이 끝난 것으로 보이지만 이 파일이 남아 있다
- **`src_v2/data/portfolio.ts`의 `INITIAL_*`** — 전 학생 공통 상수이고 학생별 영속 저장이 없다.
  채용 지원의 「드림캐치 포트폴리오」가 이것을 가리키므로 4번 도메인이 provider 를 만들 때까지
  그 제출 경로는 열지 않는다. **상수를 provider 로 승격하지 않는다**

**작업 순서와 근거** — 위 표의 상태가 아니라 **의존성과 경계의 선명함**으로 정한다.

1. ~~채용·취업~~ — **완료(2026-09-09, 마이그레이션 022·023)**. 비교과 패턴을 그대로 재사용했고
   파일 저장(#41)을 이 도메인에서 확정해 함께 만들었다. 포트폴리오 제출(`PORTFOLIO`)만
   4번 도메인의 학생별 영속 provider 를 기다린다 — **그 전까지 서버가 503 으로 거절한다**
2. **로드맵·성장활동** — 둘이 같은 화면을 공유하므로 함께 옮긴다
3. **교수상담·상담사 일정** — 상담 도메인 위에 얹힌다
4. **진단 채점 엔진** — 판정식이 오면
5. **공지·알림·통계**
6. **SSO·운영 인증** — 학교 협의 일정에 달렸다

> ⚠️ **수강·성적 연계는 전산원 요청과 동시에 착수한다.** 적재 활성화는 우리가 통제할 수 없는
> 리드타임이므로 다른 작업과 병행해 요청을 넣는다.
> **과목↔스킬 수기 매핑 테이블은 만들지 않는다** — AI가 과목 리스트를 직접 읽는다(`SPEC.md` §10-2).
> 이전에 있던 "학과별 대표 과목 30개 × 스킬 10종을 먼저 채운다"는 계획은 **폐기됐다.**

### 8-4. 개발 DB · 백엔드 <span>(2026-09-08 결정)</span>

**목적은 배선 검증 하나다.** 지금까지 학생 JSON과 로더로 고정해 온 데이터 배선이 **DB로 바뀌어도 그대로인가**를 확인한다. 성능·운영이 목적이 아니다.

| 항목 | 결정 | 비고 |
|---|---|---|
| 개발 DB | **PostgreSQL** (Docker Compose) | 운영 DBMS는 **미확정 — §9 #39** |
| 백엔드 | **FastAPI** (Python) | 개발 중에는 **로컬 PC 실행**, DB만 원격 |
| 배포 형태 | **Docker 컨테이너** | 학교 서버 수용 가능 확인(2026-09-08) |
| 개발 서버 | iwinv VPS · Rocky Linux 9 · 2 vCPU / 2 GB / SSD 50 GB | |
| 첫 대상 도메인 | **상담** (`dc_counsel_owners` · `dc_counsel_events` · `dc_counsel_records`) | |

**이 서버는 정본이 아니다.** 스키마 정본은 저장소의 DDL이고, VPS는 언제 재생성해도 되는 검증용이다. §9 미결이 닫히지 않은 상태에서 스키마 정본을 외부 서버에 두면 그것이 곧 부채가 된다.

**첫 대상을 상담으로 잡은 이유** — 이 도메인에만 **학생↔상담사 양방향 왕복**이 있다. 학생이 신청하면 상담사 접수함에 뜨고, 상담사가 확정하면 학생 화면이 바뀐다. `localStorage`에서는 "같은 브라우저라서" 되던 것이 DB에서는 진짜 왕복이 된다. 게다가 `dc_counsel_events`가 **append-only 이력**(`CLAUDE.md` 11조)이라 상태 전이 이력 패턴까지 한 번에 검증된다. 진단 판정식(#23)·심리검사(#19)·STAR 연계(#29) 같은 미결은 전부 이 도메인 **밖**이라 착수를 막지 않는다.

#### DDL 정본은 손으로 쓴 SQL 파일이다 <span>★</span>

ORM 모델에서 자동 생성(autogenerate)하지 않는다. **이것이 이관 프로젝트이기 때문이다**(§8-0).

| # | 이유 |
|---|---|
| 1 | 나중에 현행 29만 행을 부을 때 필요한 것은 ORM 모델이 아니라 **읽을 수 있는 DDL**이다 |
| 2 | 전산원과 주고받는 산출물이 **SQL**이다 |
| 3 | autogenerate는 `legacy` 열의 의미·이관 제약 같은 **의도를 담지 못한다**(§8-0 제약 2·3) |

ORM 모델은 DDL을 **따라 쓰되 정본이 아니다.** 둘이 어긋나면 SQL이 옳다.

#### 범위 밖

- **임베딩·벡터 검색(pgvector)** — 이 단계에서 다루지 않는다. 임베딩 생성은 별도 AI 서버 과제다
- **이관 스크립트** — Oracle → 신규 DB 적재는 §8-3 3단계이고 여기서 착수하지 않는다
- **SSO** — §8-3 5단계(학교 협의 선행). 그 전까지 요청자 판정은 스텁이다 → `SPEC.md` §4-2

---

### 8-5. 코드·메타데이터 관리 <span>(관리자 화면에서 고친다 · 2026-09-08 결정)</span> ★

**2026-09-08 착수 시 발견한 부채:** `CURRENT.md` 발견사항 #10이 「코드가 세 군데 흩어져 있다 — `SY_CODE` 테이블 / SQL `CASE WHEN` / JSP 하드코딩」인데, 초기 백엔드도 같은 구조였다. 아래는 발견 당시 상태이며, 실제 적용 현황은 이 절 마지막의 구현 현황을 따른다.

| 지금 코드값이 어디 있나 | 예 | 바꾸려면 |
|---|---|---|
| **DB `CHECK` 제약** | `type_code CHECK(IN ('CAREER','JOB','PSY','PROF'))` · `status_code` · `method_code` · `care_track` | `ALTER TABLE ... DROP CONSTRAINT` |
| **파이썬 리터럴** | `counsel.py` `LABELS={'REQ':'대기',…}` · `Literal['진로취업','심리','교수']` | 소스 수정 + 재배포 |
| **전용 테이블 1개** | `dc.student_type_code` (T1~T6) | 유일하게 SQL로 수정 가능 |

**상담유형 하나를 추가하는 데 DDL + 소스 수정 + 재배포가 필요하다.** 유지보수 담당자가 매번 SQL을 날려야 하는 구조는 운영에서 성립하지 않는다. 메뉴 관리 테이블과 **학과↔조교 배정 마스터는 아예 없다**(아래).

#### 코드를 두 층으로 가른다 <span>★ 이 절의 핵심</span>

`SPEC.md` §7-0 규칙 1-1(「DB의 코드 테이블은 TypeScript 시드에서 생성한다 — DB가 정본이 아니다」)은 **구조 코드에만 적용한다.** 그 규칙의 근거는 "코드값이 타입이어야 컴파일 타임에 오타가 잡힌다"인데, 그 근거는 **분기 대상이 아닌 코드에는 성립하지 않는다.**

| | **구조 코드** | **운영 코드** |
|---|---|---|
| 정의 | 애플리케이션이 **값으로 분기**한다 | 표시·분류·매핑만 한다 |
| 정본 | TypeScript (`src_v2/data/codes/`) | **DB (`dc.code_item`)** |
| 예 | 상담 상태 `REQ`/`CONFIRMED`/`DONE` · 신분 `UserType` · 로드맵 축 `RoadmapAxis` · 칸 상태 | 상담 24주제 `A01`~`A24` · 6유형 **명칭** · 진단 검사 명칭 · 프로그램 분류 · 반려사유 · **학과↔조교 배정** |
| 관리자 화면 | **조회만** (변경은 배포 사안) | **CRUD** |
| DB 제약 | `CHECK` 유지 | `CHECK` 금지 → `dc.code_item` **FK** |
| 판정 기준 | 값을 하나 더했을 때 **코드가 그 분기를 처리해야 하는가?** → 그렇다면 구조 코드 |

> ⚠️ **6유형은 두 층에 걸친다.** 코드값 `T1`~`T6`은 구조다(게이팅·프로그램 범위가 이 값으로 분기한다). **라벨 "진로탐색형"은 운영**이다.
> `dc.student_type_code`를 `dc.code_item` 그룹 `STUDENT_TYPE`으로 흡수하되 **코드값 자체는 TS 정본을 유지**한다. 화면에서 고칠 수 있는 것은 `label`·`sort_order`·`payload`까지다.

#### 테이블 설계 <span>(현행 `SY_CODE` 2단 계층 계승 — §8-2)</span>

```sql
CREATE TABLE dc.code_group (
    group_code    text PRIMARY KEY,
    label         text NOT NULL,
    managed_by    text NOT NULL CHECK(managed_by IN ('STRUCTURAL','OPERATIONAL')),
    legacy_group  text,                          -- SY_CODE.GRP_CODE (이관 매핑)
    is_active     boolean NOT NULL DEFAULT true,
    sort_order    integer NOT NULL DEFAULT 0
);

CREATE TABLE dc.code_item (
    group_code    text NOT NULL REFERENCES dc.code_group(group_code),
    code          text NOT NULL,
    label         text NOT NULL,
    legacy        text,                          -- 현행 값 = 이관 매핑표 (SPEC §7-0 규칙 2)
    up_group_code text, up_code text,            -- 2단 계층 (SY_CODE.UP_GRP_CODE/UP_CODE)
    sort_order    integer NOT NULL DEFAULT 0,
    is_active     boolean NOT NULL DEFAULT true, -- 폐지는 삭제가 아니라 false (규칙 5)
    payload       jsonb NOT NULL DEFAULT '{}',   -- 그룹별 부가속성 (예: 유형의 followUpTest)
    updated_at    timestamptz NOT NULL DEFAULT now(),
    updated_by    text REFERENCES dc.person(intg_uid),
    PRIMARY KEY (group_code, code),
    FOREIGN KEY (up_group_code, up_code) REFERENCES dc.code_item(group_code, code)
);

-- 코드 변경 이력 (append-only · CLAUDE.md 11조)
CREATE TABLE dc.code_item_event (
    id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    group_code text NOT NULL, code text NOT NULL,
    action     text NOT NULL CHECK(action IN ('CREATE','UPDATE','DEACTIVATE','REACTIVATE')),
    before     jsonb, after jsonb NOT NULL,
    changed_at timestamptz NOT NULL DEFAULT now(),
    changed_by text NOT NULL REFERENCES dc.person(intg_uid)
);
```

> **행을 지우지 않고 `is_active=false`로 내린다.** 과거 이벤트 레코드가 폐지된 코드를 참조한다(`SPEC.md` §7-0 규칙 5).
> **변경 이력이 필수다.** 현행은 권한 변경(`SY_AUTH_LOG`)만 남기고 코드 변경 이력이 없어 "이 라벨이 언제 왜 바뀌었나"를 추적할 수 없다(`CURRENT.md` 발견사항 #2).

#### 조직 배정 마스터 <span>(학과 ↔ 조교·교수)</span>

현행 `FU_ASS_DEPT`(190건)에 대응하는 표가 **없다.** `dc.staff_student_scope`는 (교직원, 학생) 쌍 파생 캐시라 "이 조교가 어느 학과를 맡는가"를 화면에서 고칠 수 없다.

```sql
CREATE TABLE dc.org_assignment (
    id           uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    staff_uid    text NOT NULL REFERENCES dc.staff(intg_uid),
    college_code text NOT NULL, dept_code text NOT NULL,
    role_code    text NOT NULL,                  -- assistant · professor · counselor
    valid_from   date NOT NULL, valid_to date,
    updated_at   timestamptz NOT NULL DEFAULT now(),
    updated_by   text REFERENCES dc.person(intg_uid),
    FOREIGN KEY (college_code, dept_code) REFERENCES dc.department(college_code, dept_code),
    UNIQUE (staff_uid, college_code, dept_code, role_code, valid_from)
);
```

- **`(단대코드, 학과코드)` 쌍으로 식별한다** — 학과명 매칭 금지(`CLAUDE.md` 7조. 동명 학과가 과정별로 존재한다).
- **`dc.staff_student_scope`는 이 표에서 파생되는 캐시로 재정의한다.** 정본은 `org_assignment`다.
- 기간(`valid_from`/`valid_to`)을 둔다 — 배정은 학년도마다 바뀌고, 과거 상담 건의 담당자 판정에는 **그 시점의 배정**이 필요하다.

#### 메뉴·권한 관리

현행 4테이블 모델(`SY_MENU` 343 · `SY_AUTH` 13 · `SY_MENU_AUTH` 972 · `SY_AUTH_USER` 52)을 계승한다(§8-2). **단, 우리 권한 판정은 메뉴에서 끝나지 않는다** — 행 수준 접근은 `SPEC.md` §2 `AccessScope`가 전담하고 메뉴 표는 **화면 노출만** 통제한다. 현행의 한계("메뉴에 없어도 URL은 살아 있다")를 그대로 옮기지 않는다.

**2026-09-08 사용자 지시로 메뉴 운영 관리를 앞당긴다.** 배포된 경로·컴포넌트는 소스에서 관리하고, 명칭·순서·노출은 DB에서 관리한다. SSO 전에도 개발 관리자 계정에 명시적으로 `AUTH0006`을 부여해 관리자 API를 검증한다. 다른 교직원 계정을 자동 승격하지 않는다. 메뉴 노출이 데이터 접근 권한을 부여하지 않는다.

#### 적용 순서

| # | 작업 | 검증 |
|---|---|---|
| 1 | `code_group`·`code_item`·`code_item_event` 마이그레이션 + 구조 코드 TS 시드 적재 | 구조 코드가 DB와 TS에서 동일 |
| 2 | 운영 코드 컬럼의 `CHECK` 제거 → `code_item` FK로 교체 | 새 상담주제를 **INSERT만으로** 추가 |
| 3 | `counsel.py`의 한글 라벨 리터럴 제거 → 코드 조회로 | `Literal['진로취업',…]` 소멸 |
| 4 | `org_assignment` 신설 + `staff_student_scope`를 파생으로 재정의 | 조교 배정 변경이 담당 학생 목록에 반영 |
| 5 | `/admin` 코드관리 화면 (그룹 목록 → 항목 CRUD → 변경 이력) | **SQL 없이** 라벨 변경이 학생 화면까지 반영 |

> **코드는 기동 시 1회 로드 후 메모리 캐시**한다(`SPEC.md` §7-0 규칙 7). 따라서 관리자 변경 시 **캐시 무효화 경로**가 함께 필요하다 — 현행이 매 요청 DB를 쳐서 `SY_CODE` 참조 statement 108개가 된 것을 반복하지 않는다.

#### 구현 현황 (2026-09-08)

- SQL 정본: `backend/migrations/009_administration.sql`부터. 가상서버 `dreamcatch`에 관리자 기반 테이블 적용 및 API 배포를 확인했다.
- `code_group` 5그룹 / `code_item` 47항목: 학생 유형 명칭 6, 진단 검사 명칭 7, 상담 주제 24, 구조 코드 상담 상태 6·상담 구분 4. 구조 그룹은 조회 전용이다. `fixed_codes` 운영 그룹은 명칭·정렬만 편집하며 T7 같은 코드 신설·기존 유형 폐지는 거부한다.
- `student_type_code`는 호환 조회 뷰다. 명칭은 `code_item`, 후속검사·프로그램 범위 등 분기 규칙은 `student_type_rule`에서 읽는다. 판정식·게이팅은 라벨 변경으로 바뀌지 않는다.
- 코드 저장은 `expectedVersion` 충돌 검사 + 변경 사유 + 이전/이후 값 기록을 한 트랜잭션으로 수행한다. 이력 테이블은 DB에서도 UPDATE/DELETE를 금지한다. 삭제 대신 비활성화한다.
- `metadata_revision`을 변경 트리거가 증가시킨다. API는 revision만 확인하고 항목 캐시를 갱신한다. 프론트엔드는 기동·창 포커스·30초 주기로 메타데이터를 갱신한다. 코드 원본 JSON으로 실패를 감추지 않는다.
- `/admin/system`: 코드 관리, 학과 담당 배정, 메뉴 관리, 변경 이력, 이관 확인 사항. 기존 디자인 토큰과 교직원 SPA를 사용한다.
- `org_assignment`는 기간별 배정 정본이다. 기존 코드가 유일하게 확인되는 교수 배정 JSON 3건을 이관했다. 학과명으로 코드를 추정하지 않는다. `staff_student_scope`는 조직 배정과 **별도 표시한 개발용 명시 권한**(`fixture_student_scope`)을 합친 뷰다. 학과 배정 종료 시 조직에서 파생된 조회 권한도 종료된다. 기존 전담학생 배정 등 다른 근거의 권한은 별개다.
- `menu` 45건과 `menu_auth`, `auth_role`, `auth_user`를 생성했다. `navConfig.ts`에서 초기 메뉴를 적재하고 이후 명칭·정렬·노출을 관리자 화면에서 수정한다. 새 경로·컴포넌트 추가는 배포 작업이다. 메뉴별 역할 허용 집합과 실제 역할 부여 UI는 후속 권한 관리 범위이며, 지금 화면에서 임의 권한을 늘리지 않는다.
- 상담 신청의 운영 주제는 `topic_code` → `code_item('COUNSEL_TOPIC', code)` FK로 연결한다. 자유 입력 목적과 신청 당시 주제 명칭은 별도 스냅샷으로 보존한다. 새 CARE 7+ 신청은 DB의 활성 주제 및 현재 학생 유형을 검사한다. 기존 자유 입력 목적은 임의 코드로 치환하지 않는다.
- 검증: 별도 PostgreSQL 테스트 DB의 관리자 권한 차단·라벨 변경·캐시 갱신·낙관적 잠금·이력 불변·조직 배정 범위·메뉴 검사 통과. 브라우저 관리자 저장도 확인했다. SSO와 공개 운영 인증은 아직 개발 스텁이며, API는 SSH 터널로만 노출한다.

**아직 전체 이관 완료가 아니다.** 남은 도메인과 진행 상태는 **§8-3 이관 대장**이 정본이다. 도메인을 옮길 때 운영 분류·명칭은 이 코드 관리 구조를 재사용하고, 권한·상태 전이 분기 코드는 별도로 보존한다.

2026-09-08 후속: 진단 응시/결과 조회·검사 현황·코멘트·권유 API를 추가했다(`015` 이후 migration). 사용자 제공 C2·C3·C4 결과 항목은 `diagnosis_factor_definition`, 운영 명칭은 `code_item.DIAGNOSIS_FACTOR`로 관리한다. 예시 숫자는 점수로 적재하지 않는다. C5·C6 정의는 대기한다. 상세 계약은 [docs/DIAGNOSIS_RESULT_CONTRACT.md](docs/DIAGNOSIS_RESULT_CONTRACT.md)를 따른다. 이 추가로 코드 그룹은 6개, 항목은 59개가 된다.

2026-09-10 후속(`039`·`040`): 현행 `SY_AUTH` 에서 남아 있던 두 사실을 옮겼다. `auth_role.base_group`(=`BASEGRUP_YN`, 신분 자동 부여 여부)·`legacy_code`(=`AUTH_CODE`, 매핑 전용·UNIQUE 없음 — 진로·심리 상담사가 모두 `AUTH0012` 에서 갈라진다)와 `auth_user_event`(=`SY_AUTH_LOG`, append-only)다. 역할 부여 API 는 아직 없다 — 회수는 DELETE 가 아니라 `valid_to` 를 닫는 방식이며 같은 사람에게 같은 역할을 다시 주려면 PK 가 막는다(재부여 필요 시 §9 로). `code_group.legacy_group` 을 처음 채웠다: `COUNSELOR_STATUS`←`0024`, `PROF_COUNSEL_TYPE`←`0131`(교수상담 DB 전환 때 쓸 사전), `COUNSEL_TYPE`←`0146`(진로·심리만 대응, 취업은 현행 하드코딩 분기라 코드 없음). `0145→0147` 심리검사와 `0143` SMART 5단계는 미결·불일치로 옮기지 않았다.

#### 범위 밖

- **게시판·배너·SMS·접속통계** — 현행 시스템관리 화면의 나머지다. 이관 대상이지만 이 절의 범위가 아니다
- **코드 그룹의 신설** — 그룹을 더하는 것은 곧 새 기능이므로 배포 사안이다. 관리자 화면이 다루는 것은 **항목 CRUD**까지다

### 8-6. 비교과 운영 <span>(신청·선발·출석·수료를 나눈다 · 2026-09-08 결정)</span> ★

`dc.program` 은 공고 속성을 `payload jsonb` 한 덩어리로, `dc.program_apply` 는 신청·선발·출석·
수료를 `outcome` 한 칸으로 갖고 있었다. 넷은 **서로 다른 시점에 서로 다른 담당자가 정하는
다른 사실**이라 한 칸에 넣으면 "선발됐지만 아직 결과 없음"을 표현할 수 없다.
`spec_v1` §5.4 의 「프로그램→차수→신청, 신청/선발/출석/수료 분리」를 따른다.

| 무엇 | 어디에 | 왜 |
|---|---|---|
| 공고 속성 | `dc.program` 열로 승격 (`payload` 삭제) | 목록 필터·마감 판정·통계가 jsonb 를 뒤지면 인덱스도 제약도 못 건다 |
| 분류 | `category_code` → `code_item(PROGRAM_CATEGORY)` **FK** | 운영 코드 — 관리자 화면에서 항목을 더한다 (§8-5) |
| 모집 상태·선발·출석·결과 | `CHECK` 제약 | 구조 코드 — 앱이 값으로 분기한다. 배포로만 바뀐다 |
| 신청 1건 | `selection_code` · `attendance_code` · `outcome_code` · `absence_points` 로 분리 | 한 칸이면 "선발 + 출석 + 미수료"를 못 적는다 |
| 상태 변경 이력 | `dc.program_apply_event` (append-only) | 신청 행이 삭제돼도 남아야 하므로 FK 를 걸지 않는다 |
| 벌점 | `dc.penalty_entry` (append-only) + `dc.penalty_total` 뷰 | **총점 열을 두지 않는다.** 부여·회수를 행으로 쌓고 합으로 읽는다 |

**제약으로 고정한 업무 규칙** — 애플리케이션이 잊어도 DB 가 거절한다.

- `outcome_code IS NULL OR selection_code='SELECTED'` — 선발하지 않은 학생에게 수료가 남으면 통계가 거짓이 된다
- `(selection_code='SELECTED') = (selected_at IS NOT NULL)` — 선발이면 시점이 반드시 있다
- `absence_points=0 OR outcome_code='ABSENT'` — 벌점은 불참에만 붙는다
- `roadmap_entry='NONE' OR array_length(care_types,1)>=1` — 대상 유형 없이 편입하면 붙을 학생이 없다
- `(kind='WAIVE') = (points<0)` — 부호가 뒤집히면 합계가 조용히 틀린다

**수료만 로드맵 칸을 닫는다.** 선발·출석으로는 닫지 않는다(`spec_v1` §7.2). 결과를 되돌리면
칸도 함께 열리며, 두 변경은 한 트랜잭션이다. 노쇼·불참 벌점도 같은 트랜잭션에서 매긴다 —
예전처럼 프론트가 출석을 저장한 뒤 따로 벌점을 부르면 두 브라우저가 동시에 처리할 때 어긋난다.

**정합성 판정은 서버 한 곳이다**(CLAUDE.md 규칙 5). 중복 신청·마감·모집 상태·정원 초과 선발·
선발 없는 이수 결과·남은 점수를 넘는 차감을 서버가 거절한다. 정원은 **신청이 아니라 선발**에
건다 — 신청은 초과를 받고 선발에서 자른다.

**과거 이력은 FK 를 걸지 않는다.** 로스터에서 옮겨 온 벌점이 현재 카탈로그에 없는 프로그램
(`prog_seed_*`)을 가리킨다. `dc.student_program_history` 와 같은 규약으로 제목을 스냅샷한다.

- 마이그레이션 `018_program_operations.sql` · API `backend/app/programs.py` · 테스트 `backend/tests/test_programs.py`
- 프론트 정본은 `shared/programStore.ts`(부팅 적재 + 동기 셀렉터). `dc_programs` · `dc_penalty_v2` localStorage 는 폐기됐다
- **차수(step) 테이블은 아직 만들지 않았다.** 화면이 차수를 표시만 하고 차수별 운영·출석 UI 가 없다. 회차별 출석을 받게 되면 `dc.program_step` 을 만들고 `program_apply.round_no` 를 FK 로 바꾼다

### 8-7. 채용·취업 <span>(전형은 데이터, 재지원은 회차, 서류는 파일 · 2026-09-09 결정)</span> ★

비교과(§8-6)의 규약을 그대로 따르되 **채용에만 있는 세 가지**를 더한다.

| 무엇 | 어디에 | 왜 |
|---|---|---|
| 전형 단계 | `dc.job_stage` (공고별 행) | enum 이 아니다 — 공고마다 단계 수와 이름이 다르고 담당자가 편집한다.<br>교내 절차 2단계(서류 검토·기업 전달)는 `system_key` 로 표시하고 **서버가 편집을 거부**한다. 전역 상수 ID 를 쓰지 않는다 — 그러면 서로 다른 공고의 이력이 같은 단계를 가리킨다 |
| 제출 회차 | `dc.job_application_attempt` (append-only) | 취소 후 재지원이 있다. 한 행을 덮어쓰면 **직전 제출 스냅샷·서류 귀속·취소 이력이 사라진다**. 지원 1행은 «현재»만 들고 제출은 회차로 쌓는다 |
| 서류 파일 | `dc.file_object` + 웹루트 밖 볼륨 | 바이트는 DB 밖에, 메타만 DB 에. 정적 서빙하지 않고 API 가 권한을 확인한 뒤 스트리밍한다 (#41 해소) |
| 기업 | `dc.company` | `job_company` 가 아니다 — 조교 「학과 추천기업 관리」가 같은 사전을 쓴다. 채용 전용 자산이 아니므로 도메인 이름을 붙이지 않는다 |
| 열람 감사 | `dc.job_access_event` (append-only) | `dc.admin_event` 에는 before/after 와 NOT NULL `reason` 이 있어 «열람» 사건을 담을 자리가 없다. CSV 본문·자소서 본문·학생 PII 는 넣지 않는다 |

**제약으로 고정한 업무 규칙**

- `source<>'manual' OR company_id IS NOT NULL` — 외부 수집 공고는 회사명 문자열뿐이다. 필수로 두면 **가져오는 것만으로 기업 사전이 오염된다**
- `(deadline_date IS NULL) = (deadline_mode='ALWAYS')` — 상시만 마감일이 없다
- `(status='CANCELED') = (canceled_at IS NOT NULL)`
- 활성 `position` 부분 유니크 인덱스 — 부분 인덱스는 `DEFERRABLE` 이 불가능하므로 재정렬은 **임시 음수 영역**을 거쳐 두 단계로 쓴다
- `job_application(id,current_attempt_no)` → attempt 는 `DEFERRABLE INITIALLY DEFERRED` — 첫 신청의 삽입 순환을 커밋 시점에 검증한다

**인가는 `require_staff` 가 아니라 메뉴 권한이다.** 채용 메뉴(`jobs`·`jobs.4`)는 `career` 에만
부여돼 있고 `dc.auth_user` 에는 개발 시스템관리자 한 명뿐이다 — `auth_user` 만 검사하면
**전 상담사가 403 이 된다.** `metadata.py` 의 메뉴 술어를 그대로 나눠 쓴다. 총괄에게 열려면
`menu_auth('jobs', …)` 행을 마이그레이션에 **명시적으로** 추가한다.

**학생 범위는 `dc.staff_student_scope` 만 본다.** `auth.student_access` 의 «과거 상담 담당이면
허용» UNION 을 채용으로 가져오지 않는다 — 비교과보다 엄격해지는 **의도적 분기**다.
목록·상세·이력·통계·CSV **전부**에 같은 술어를 건다(비교과 통계의 범위 누락을 계승하지 않는다).

**취업 게이트는 상담 게이트와 같은 함수를 쓴다**(`backend/app/gates.py`). 유형→후속 검사
매핑은 이미 `dc.student_type_rule.follow_up_test` 에 있으므로 파이썬으로 복제하지 않는다.
`care_track` 이 비어 있는 진로·취업 상담은 `care7` 로 읽는다(`PROCESS.md` §2-1 구현규칙 4).
잠긴 응답에는 사유 코드·문구·**다음 단계 경로**를 함께 담는다 — 화면이 문구를 다시 만들지 않게.

**「채용시 마감」은 최초 등록 때 한 번만 계산한다.** 현행 폼은 저장할 때마다 다시 계산해
**제목만 고쳐도 마감이 한 달 밀렸다.** 주석과 구현이 어긋난 버그이므로 등록 시점 고정으로 교정했다.

**AI 평가 결과는 새로 만들지 않고 재사용한다.** `dc.ai_run(subject_kind='RESUME')` 이 이미
자소서 ID `r1`·`r2` 를 가리키고 그 행은 append-only 라 사후 재매핑이 불가능하다 —
`dc.job_resume` 은 그 ID 를 보존한다. 자소서 쪽에 AI 결과 역참조 컬럼을 두지 않는다(규칙 3).

- 마이그레이션 `022_job_operations.sql`(DDL·코드) · `023_job_seed.sql`(파생 시드, `seed.py` 가 다시 돌린다)
- API `backend/app/jobs.py` · `files.py` · `gates.py` · 테스트 `backend/tests/test_jobs.py`
- 프론트 정본은 `shared/jobStore.ts`. `dc_jobs` · `dc_job_applications` · `dc_job_app_events` ·
  `dc_job_wishlist` · `dc_user_resumes_v1` localStorage 는 폐기됐다 — **소유자를 알 수 없어 수입하지 않았다**
- **아직 열지 않은 것** — 「드림캐치 포트폴리오」 제출(§8-3 4번의 provider 대기, 서버가 503 으로 거절) ·
  공통 동의 모델(#43) · 본문 inline 이미지의 파일 전환(기존 `data:` URL 은 그대로 둔다)

---

## 9. 미결 — 확인 전까지 임의 확정 금지

**이 프로젝트의 미결 대장은 이 절 하나다.** 다른 문서는 번호로 가리킨다.

> **번호 규약** — 번호는 **재사용하지 않는다.** 해소된 항목은 번호를 남긴 채 취소선으로 두고 결론을 「해소된 것」 표로 옮긴다. 결번(#25·#27)은 그렇게 걷힌 자리다.
> 2026-09-07에 로드맵 항목의 번호를 #12~#15 → **#31~#34**로 정정했다 — 일반 항목과 번호가 겹쳐 "번호로 가리킨다"는 규약이 깨져 있었다.

### 해소된 것 <span>(요약 — 근거는 `CURRENT.md`)</span>

| 항목 | 결론 |
|---|---|
| 역량 체계 3개 중 무엇인가 | 비교과(`EP_PRM`)계. **스키마 6 / 화면 5** — `CURRENT.md` §2-3 |
| 상담 가능시간 "가능" vs "제한" | 현행은 "가능" 등록. **우리는 UI=제한 등록 / 저장=현행 승계**(`is_use`) — 아래 ⚠ |
| 진로설계(`STU_COURSE_*`)가 쓰이는가 | 쓰인다. 단 **INSERT 0건** — 최초 생성 경로는 #8 |
| 교수 상담이 실제로 동작하는가 | 동작한다(18.4만). `PC_CON_PROF_*`·`CO_PROF`는 미사용 |
| `FU_CODE`의 정체 | 소스 참조 0건. 학과 목록의 출처가 아니다 |
| `CHECK_*` 심리검사 11종 | **살아 있다** (8,407행, 최종변경 2025-11). 초기 "죽은 스키마" 판정이 뒤집혔다 |
| `COM_CPRT_MEBR` / `COM_COMP_INF` | 전자는 가입 기업회원 자체, 후자는 취업통계·추천용 **기업 사전**(10.4만) |
| `iapType` `R1`~`R6` (구 #26) | **폐기 확정** (2026-08-24) — 아래 표 참조 |
| IAP 칸의 완료 기준 (구 #11) | **`outcomeStatus='수료'`** (2026-08-27). 선발·출석만으로는 칸이 완료되지 않는다 — 노쇼·미수료가 이행률을 올리지 못하게 한다 |
| 재생성 시 미완료 칸 처리 (구 #32) | **이월하지 않는다** (2026-09-07). 목표 직무가 바뀌면 세 축 전부의 기준이 바뀌어 옛 칸은 새 목표의 이행률 분모로서 의미가 없다. 지난 실적은 **스냅샷에서 조회** — `PROCESS.md` §6-6 |
| 상담이 진단 게이트에 걸리는가 | **트랙에 따라 갈린다** (2026-09-07). 일반 진로취업·심리·교수 상담은 **게이트 밖**, CARE 7+ 연계 상담만 진단 2종을 요구한다 — `PROCESS.md` §2-1 |
| 로드맵 재생성 주체 | **상담사만.** 학생은 변경 요청만 가능하며, 재생성은 정기(연 1회) 또는 **재상담 시 상담사가 실행**한다 (2026-09-07) — `PROCESS.md` §6-6 |
| 파일 저장 방식 (구 #41) | **컨테이너 볼륨 + 웹루트 밖 보관 + API 권한 확인 후 스트리밍** (2026-09-09). 정적 서빙하지 않고 저장 이름은 서버가 부여한다. 기존 `data:` URL 은 그대로 둔다 — 아래 #41 행과 §8-7 |

> **★ "소스 참조 0건" ≠ 죽은 테이블.** `origin/`은 운영 소스의 **일부**다.
> `CHECK_*`가 그 조각에 안 나올 뿐 운영은 계속 쓰고 있었다.
> 앞으로 "소스에 없다"만으로 폐기 판정하지 않는다.

> **⚠ `BASICSETTING` 결정은 뒤집혔다** (2026-08-04 승계 원칙).
> 로드맵·역량·게임화·진단 외에는 현행 구조를 승계한다.
> 슬롯은 현행 구조 그대로 두고 `is_use`로 가능/불가를 표현한다 → `dc_counsel_slot`.
> 57만 행이 이 구조로 쌓여 있어 바꾸면 이관이 불가능해진다. **행 수 감축은 이관 후 과제.**

### 남은 미결

| # | 항목 | 왜 막혀 있나 |
|---|---|---|
| 1 | **역량 `인문`(6번째)을 되살릴지** | 스키마·SQL은 6개를 계산하는데 화면만 5개다. 정책 결정 |
| 2 | **관리자 제한일정을 학생 예약 화면에 반영할지** | 현행은 반영 안 함(상담사가 슬롯을 지워야 함). 의도인지 미구현인지 현업 확인 |
| 3 | `V_DEP_INF`와 `V_DEP_INF_ALL`의 242개 차이가 정확히 무엇인가 | `docs/dbmeta/F_graduate_check.sql` F-3으로 확정 가능 |
| 4 | `FU_ASS_DEPT` 190건의 코드가 학사 코드 체계와 일치하는가 | `docs/dbmeta/H_dept_assign.sql` H-2로 확정 가능 |
| 5 | 대학원생 진단 정책 | C-2/C-3/C-4 학년 매핑이 학부 기준(1~4). 대학원은 1~3 |
| 6 | **대학원 수료생을 상담·비교과 대상에 포함할지** | 학적상태 `COMPLETED` 처리 정책 |
| 7 | **비교과 참가자격 판정 함수 `FN_GET_PRM_TRGT_YN()` 본문** | PL/SQL 안에 있어 소스로 확인 불가. 대학원생 신청 가능 여부의 최종 관문 |
| 8 | `STU_COURSE_INFO` 행의 최초 생성 경로 | INSERT 0건 / UPDATE 3건. 배치·트리거·외부 시스템 중 무엇인지 |
| 9 | **이관 시 현행 데이터를 정제할지** | `COM_ASS_DEPT` 잔여 배정, `CON_GB` 1/3 라벨 중복 |
| 10 | `SY_CODE` 그룹 `0012`·`0037`의 정체 | 현행 코드관리 화면이 하드코딩으로 숨긴다 |
| 11 | 지역청년 서브사이트를 같은 시스템에 둘지 | 별도 세션(`RegLoginSession`)·별도 회원 테이블·약 45화면. 사실상 두 번째 사이트 |
| 12 | **조교에게 학생 상세를 어디까지 공개할지** | 로드맵·진단결과 포함 여부. 행 수준 접근제어 정책 (`SPEC.md` §2) |
| 13 | **교수상담을 본인 학과만 허용할지 타 학과도 열지** | 현행은 본인 소속(`HAKBU_CD`) 교수만 조회된다. 대학원 요건(§7)과 함께 결정 |
| 14 | **비교과 운영자 역할을 신설할지** | 현행은 별도 역할(`Mn`, 30화면). 우리는 진로상담사가 겸하고 있다 (`SPEC.md` §3-7) |
| 15 | **`merge_V_DEP_INF`의 MERGE 본문** — 컬럼 목록이 고정인가, WHERE 필터가 있는가 | `origin/` 소스가 현재 로컬에 없어 미확인. 학과 반입 경로의 최종 관문 (§3-5 ①) |
| 16 | **학사DB → 드림캐치 노출 뷰의 정의** — 신설 학과가 뷰 단계에서 걸리는가 | 운영자가 전산원에 "V_ 테이블에 학과 추가"를 요청해 온 정황. 전산원 확인 필요 |
| 17 | **★ PK 없는 122개 테이블의 중복 판정 규칙** | 운영 275개 중 **122개(44%)** 에 PK가 없다. 신규 설계는 전 테이블 PK가 있어 **그대로 부으면 적재가 실패한다.** 테이블마다 "무엇이 같으면 같은 행인가"를 정해야 하고 **DB가 답을 주지 않는다** — 운영 담당자 확인 사항 |
| 18 | **첨부 다형 참조를 어떻게 승계할지** | 같은 컬럼이 서로 다른 도메인의 PK에 붙는다. `dc_file`이 이 패턴을 그대로 받을지, (테이블코드 + 행ID) 쌍으로 명시할지 |
| 19 | **심리검사 결과 11종을 어떻게 승계할지** | 검사 종류마다 전용 테이블 + 전용 컬럼이다. 공통 결과 테이블 + 검사별 항목 행으로 펼지, 검사별 테이블을 유지할지.<br>**⚠️ #23이 먼저다** — 심리검사 존치 자체가 미정이고 홀랜드는 미사용 확정. **설계 착수 금지** |
| 20 | **넓은 테이블의 죽은 컬럼** | 비교과 마스터는 125컬럼 중 27개가 값 0건이었다. 상담(46컬럼)·채용(82)·기업회원(53)에도 같은 조사를 돌릴지 |
| 21 | **반려사유를 필수로 만들지** | 현행은 반려 상태는 있는데 사유를 **한 번도 안 남겼다**(0건) |
| 22 | **연계상담에서 교수가 당사자인가** | 현행 연계 테이블은 교수·상담사 두 당사자를 컬럼으로 들고 있는데 역할 접근표에는 상담사·관리자만 있다.<br>게다가 **이 테이블을 쓰는 화면 기록이 한 건도 없다**(참조 0건) — 소스 조각에 안 들어온 것인지 기능 자체가 안 쓰이는지 미확인.<br>행수·최종변경일로 먼저 가른다 |
| 23 | **★ 진단 문항·판정식** | **사용자가 추후 제공**(2026-08-24).<br>확정된 것 — 순서·게이팅·6유형·3계층·24주제·후속진단 매핑·로드맵 3축(`PROCESS.md`) / 홀랜드 미사용 / 심리검사 존치 미정.<br>**판정 로직을 코드에 고정하지 않고 유형을 주입받는 값으로 다룬다**(`PROCESS.md` §9). 파생 미결은 #24·#28 |
| 24 | **취약관리형(하위) 비교과 신청 범위** | 상위=전체·중간=진로탐색/설정 카테고리만 확정. **하위 미정** (`PROCESS.md` §7) |
| 26 | ~~`iapType` `R1`~`R6` 존치 여부~~ | **해소(2026-08-24) — 폐기 확정.** 상담에서 `studentType`을 확정하고 로드맵 1개를 생성하므로 별도 R타입이 없다 |
| 28 | **취약관리형 진입·탈출 정량 기준** | "진단 결과 매우 저조" 컷오프 — #23과 함께 결정.<br>**강등은 구현하지 않는다**(2026-08-24 확정). 승급만 — 순서상승·최대 2단계·변경 시 C-CORE 재진단 (`PROCESS.md` §8) |
| 29 | **★ STAR 트랙 진단(`C-Core`·`C-2`)을 새로 응시하는가, 기존 결과를 연계하는가** | STAR 트랙(선발형)에 지원한 학생만 `C-Core`·`C-2`를 진행한다(2026-08-31 확정).<br>**미정** — 이미 전교생 파이프라인에서 `C-CORE`를 본 학생이 트랙에 선발되면 **다시 보는지, 기존 결과를 그대로 끌어 쓰는지.**<br>재응시라면 같은 학생에게 같은 검사의 결과가 2건 생겨 **어느 것이 확정 유형인지**를 정해야 한다(진단 이력은 append-only).<br>화면은 이 미정을 그대로 드러낸다 — `src_v2/pages/star/StarTrack.tsx` 축1의 "후속 진단 연계" 단계에 「연계 방식 미정」 표기. **판정 로직 착수 금지**(#23과 함께) |
| 30 | **C-PASS 인증 단계명이 근거마다 다르다** | 운영계획 본문 = `STAR-Pre Pass`→`STAR-Core Pass`→`STAR-Match Pass`(학년 진급형) / 인포그래픽 = `Bronze`~`Platinum`(**"예시" 표기**, 본문 배점 총합으로 도달 불가라 근거로 쓸 수 없음) / 화면 시안 `public_t/star.png` = `Basic`·`Pre`·`Pass`·`Elite` 4단계.<br>구현은 **시안 4단계**를 따랐다 — 정본이 정해지면 `src_v2/data/starTrack.ts`의 `CPASS_STAGES` 한 표만 고치면 된다 |

**로드맵 3축 구조에서 열린 미결** <span>(2026-08-27 등재 · 2026-09-07 번호 정정 — 옛 #12~#15는 위 일반 항목과 번호가 겹쳐 있었다)</span>

| # | 항목 | 왜 막혀 있나 |
|---|---|---|
| 31 | **로드맵 스냅샷 기준일** <span>(구 #12)</span> | `PROCESS.md` §6-6 **정기** 재생성 "1년마다"의 기산점 — 입학일 / 로드맵 최초 생성일 / 학년도(3월) 중 무엇인가. 전교생 일괄인지 학생별인지도 함께.<br>**재상담 재생성**은 기준일이 필요 없다 — 상담사가 누르는 시점이다 |
| ~~32~~ | ~~재생성 시 미완료 칸 처리~~ <span>(구 #13)</span> | **해소 (2026-09-07)** → 위 「해소된 것」 표 |
| ~~33~~ | ~~**유형 승급 시 IAP 칸 처리**~~ <span>(구 #14 · **2026-09-09 닫힘**)</span> | **유지한다.** 유형이 바뀌어도 이미 붙은 칸은 건드리지 않고, 새 유형 기준은 **로드맵 재생성**에서만 반영된다(재생성은 15칸을 새로 만들고 이월하지 않는다). 회수하면 학생이 이미 신청·선발한 프로그램의 칸까지 사라지고 수료한 칸을 떼면 이행률이 거꾸로 내려간다 → `.ai/handoff-db/0002-roadmap-growth/04-decisions.md` Q2 |
| 34 | **핵심역량 축(축 2)의 강의 데이터 부재** <span>(구 #15)</span> | 학과 개설강의·수강이력이 **미적재**(`CURRENT.md` §4-2). 데이터가 오기 전까지 축 2는 시드로만 채울 수 있다 — **구현 블로커**.<br>⚠ 적재된 교육과정도 **`curriYear` 2021치뿐**이라 2026학번은 매칭이 되지 않는다. 현재는 "그 학과의 최근 연도로 폴백"으로 넘기고 있다(`deriveSkillTree`) — 연도별 교육과정 적재 범위가 전산원 요청에 포함돼야 한다 |

**상담 2트랙에서 열린 미결** <span>(2026-09-07 등재 — `PROCESS.md` §2-1)</span>

| # | 항목 | 왜 막혀 있나 |
|---|---|---|
| 35 | **CARE 7+ 연계 상담의 사전문진표 양식** | **사용자가 추후 제공**(2026-09-07). 현재는 취업준비형 고학년용 2문항이 전 유형에 공통으로 나간다 — 신입생에게도 "상반기 공채 자소서·면접 준비는…"이 노출된다.<br>유형별(24주제 기반)로 나눌지 공통 1벌로 둘지도 이 결정에 딸린다. **일반 상담은 문진표 없음으로 확정** |
| 36 | **★ 기존 상담 5.4만 건을 어느 트랙으로 이관할지** | 현행 `COUNSEL_MASTER`에는 트랙 축이 없다. 화면·집계는 NULL을 `care7`로 읽어 넘기고 있지만(§8-0 제약 3), **이관 시 실제로 무엇을 적재할지는 별개 결정**이다.<br>전량 `care7`이면 "로드맵을 낳지 않은 상담"까지 파이프라인 상담으로 잡히고, 전량 `general`이면 이미 로드맵을 받은 학생의 근거가 사라진다.<br>후보 판정축 — 상담유형(`CAREER`/`JOB`) · 결과에 로드맵이 딸렸는지 · 상담일자 기준일. **운영 담당자 확인 사항** |
| 37 | **로드맵 스냅샷의 보존 기간과 조회 경로** | 스냅샷은 append-only로 쌓기만 하고 **조회 화면이 없다.** 완료 칸을 이월하지 않기로 했으므로(해소 #32) 학생의 지난 실적은 스냅샷에만 있는데, 학생·상담사 누가 어디서 보는지가 미정.<br>보존 기간(졸업 후 N년)도 개인정보 보관 정책과 함께 정해야 한다 |
| 38 | **AI 로드맵 생성 계약** | 재료 3종(진단·상담 / 교과목 / 스펙) → 3축 15칸을 **무엇이 뽑는가.** 현재는 학생 시드의 `roadmapOutcome`을 그대로 주입하는 자리표시자다(#23 진단 판정식과 같은 패턴).<br>입·출력 계약만 고정하면 주입 지점을 AI 호출로 갈아끼울 수 있다 — **판정 로직을 코드에 고정하지 않는다** |
| 39 | **★ 운영 DB가 무엇인가 — PostgreSQL인가 Oracle인가** | 개발·배선 검증은 **PostgreSQL**로 진행하기로 했다(§8-4). 그러나 **운영 반영 시 DBMS가 확정되지 않았다.**<br>현행이 Oracle이고 이관 대상이 29만 행이라 이 결정이 §8-0 이관 설계 전체에 걸린다 — 타입 매핑(`NUMBER`↔`numeric`, `DATE`↔`timestamptz`, `VARCHAR2`↔`text`)·시퀀스·PL/SQL 함수(#7 `FN_GET_PRM_TRGT_YN`)의 처리가 전부 달라진다.<br>**전산원 확인 사항 3가지** — ① 신규 PostgreSQL 인스턴스 허용 여부(현행이 Oracle이라 DBA 정책이 있을 수 있다) ② 기존 Oracle에 스키마를 얹는 방식만 허용하는지 ③ 컨테이너의 아웃바운드 인터넷 허용 여부.<br>⚠️ 확인 전까지 **이관 스크립트·타입 매핑표 착수 금지.** 배선 검증(§8-4)은 DBMS와 무관하므로 그대로 진행한다 |
| 40 | **구조 코드 / 운영 코드의 경계를 그룹별로 확정** | §8-5가 판정 기준(「값을 더했을 때 코드가 그 분기를 처리해야 하는가」)은 정했으나 **그룹별 배정은 미확정**이다.<br>경계에 걸린 것 — 상담 방식(`ONLINE`/`OFFLINE`. 현행 `CON_KIND`에 다른 값이 더 있는지 미확인) · 반려사유(#21 — 현행 0건) · 비교과 상태값 10종(§8-2 계승 대상인데 화면 분기가 붙어 있다).<br>⚠️ 운영 코드로 **잘못 분류하면** 화면에서 추가된 값을 코드가 처리하지 못해 런타임에 깨진다. **코드 그룹을 열 때마다 판정한다** |
| ~~41~~ | ~~**파일을 어디에 어떻게 저장할지**~~ | **해소 (2026-09-09, 채용 도메인에서 확정)** — ① 저장 위치 = **컨테이너 볼륨**(`deploy/compose.api.yaml` 의 명명 볼륨 `filedata`, 경로는 `DC_FILE_ROOT`). 오브젝트 스토리지를 도입하지 않았고 DB 안에 바이너리를 넣지 않았다. ② 접근 경로 = **API 가 중개한다.** 웹루트 밖에 두고 정적 서빙하지 않으며 `GET /api/v1/job-files/{id}` 가 소유(학생 본인) 또는 `dc.staff_student_scope` + `dc.menu_auth` 를 확인한 뒤 스트리밍한다. 저장 이름은 서버가 부여한 id 이고 업로드 원본 이름을 경로에 쓰지 않는다. ③ 기존 `data:` URL = **건드리지 않는다.** 새로 올리는 것만 파일 저장소로 가고 두 방식이 한동안 공존한다 — 의도된 것이며 결함으로 보고하지 않는다.<br>메타는 `dc.file_object`, 후속 운영 항목은 #42 다 |
| 42 | **★ 파일 볼륨의 백업과 서버 이전 절차** | #41 로 만든 파일 볼륨(`filedata`)은 **DB 백업(`pg_dump`)에 포함되지 않는다.** 지금은 DB 만 백업하면 되던 것이 둘이 됐고, 둘의 시점이 어긋나면 «DB 에는 지원이 있는데 서류 파일이 없는» 행이 생긴다.<br>정할 것 — ① 볼륨 백업 주기와 보관 위치 ② DB 복원 시 파일 복원과의 시점 정합 절차 ③ 서버 이전 시 볼륨 이관 방법 ④ 삭제(`state='DELETED'`)된 파일의 바이트 파기 시점.<br>⚠️ 지금은 **논리삭제만 하고 바이트를 지우지 않는다.** 파기 정책이 정해질 때까지 그대로 둔다 — 지운 뒤에는 되돌릴 수 없다 |
| 43 | **★ 공통 동의(consent) 모델** | 채용 지원(2026-09-09)은 **동의 없이** 열었다. 현행 `ReAgree`(개인정보 동의)가 있고 추천채용은 학생 정보를 기업에 전달하므로 언젠가 필요하지만, **임시 문안을 지어내면 그 동의가 무효가 되고 이력만 남는다.**<br>도입할 때는 **jobs 사유물로 만들지 않는다** — 공통 `dc.consent_policy(policy_key, revision, body_hash, …)` / `dc.consent_receipt(subject_kind, subject_id, policy_id, …)` 로 설계하고 채용은 `subject_kind='JOB_APPLICATION_ATTEMPT'` 로 참조만 한다. 비교과의 `program_apply.consents jsonb`(§8-6)도 나중에 같은 모델로 수렴시켜야 한다.<br>필요한 것 — 동의 제목·본문·필수/선택 구분·제공 대상(어느 기업에 무엇을)·보관 기간. **운영 담당자 확인 사항** |
| 44 | **학생 목록의 GPA 산식** <span>(2026-09-10 · 시드 퇴역 라운드)</span> | `dc.student_list.gpa`는 시드 `detail.gpa`(text)를 그대로 내보낸다. 더미의 가짜 GPA 폴백은 제거했고(`0003-seed-retirement` D1) `dc.student_course`가 있으니 가중평균으로 계산할 수 있지만 **학점 산식(재수강 처리·P/F 제외·기준 학기)이 미확정**이다. 확정 전까지 시드에 없는 학생은 `null`이며 집중관리(HIGH) 술어는 `null`을 저학점으로 보지 않는다 |
| 45 | **교수 소속(학과) 라벨의 이름 폴백 제거 시점** | 교수·조교 소속은 `dc.org_assignment`(코드 쌍)가 정본이지만 시드 교수 26명 중 대부분이 배정 행이 없어 `profile.major`(학과명)로 표시한다(046 backfill 은 유일성 가드로 채울 수 있는 것만 채운다). 인사 데이터가 붙어 `org_assignment`가 전원 채워지면 라벨 폴백을 지운다 — 그 전에 지우면 상담신청 교수 목록이 빈다 |
---

## 10. 근거 파일

| 파일 | 내용 |
|---|---|
| `docs/dbmeta/out/D1_tables.csv` | 테이블 428개 + 코멘트 + 행수 + 통계일 |
| `docs/dbmeta/out/E2_tables.csv` | 학사 적재 테이블 컬럼 명세 |
| `docs/dbmeta/out/E3_tables.csv` | 핵심 서비스 테이블 컬럼 명세 (905행) |
| `docs/dbmeta/out/E4_tables.csv` | `SY_CODE` 전체 3,874행 |
| `docs/dbmeta/out/E5_tables.csv` | 테이블별 INSERT/UPDATE/DELETE 실적 |
| `docs/dbmeta/out/E7_tables.csv` | 제약조건 (PK/UNIQUE/FK) |
| `docs/dbmeta/out/E8_tables.csv` | 실제 뷰 14개 |
| **`docs/dbmeta/out/l-1.csv`** | **운영 288객체 전체 컬럼 4,772행** — 컬럼 미확보 0 |
| **`docs/dbmeta/out/l-2a.csv`** | **뷰 14개 DDL 전문** — 조인 조건 27건의 출처 |
| `docs/dbmeta/out/l-3.csv` · `l-4.csv` · `l-5.csv` | 인덱스 · 반복 컬럼명 · 전체 활동 실적 |
| **`docs/dbmeta/out/k-2수정.csv`** | **`EP_PRM` 125컬럼 실사용** — 27개가 값 0건 |
| `docs/dbmeta/out/F2_dept_tree.csv` | 조직 트리 1,921행 |
| `docs/dbmeta/out/I2.csv` · `I3.csv` | 역할×메뉴 908행 · 역할 마스터 13건 |
| `origin/sqlmap/*.xml` | MyBatis sqlmap 34개 · statement 2,033개 |
| `origin/co/*.java` | 상담 도메인 컨트롤러 4개 |

### `docs/_analysis/` — 현행 소스 전수 분석 <span>(2026-07-31, 원격 수행)</span>

소스가 원격 PC에만 있어 `docs/REMOTE_ANALYSIS_PROMPT.md` 지시서로 그쪽에서 분석하고 산출물만 회수했다. **DB 미접속·소스 무수정·정적 분석 전용.**

| 파일 | 내용 | 범위 |
|---|---|---|
| `00_overview.md` | 스택·진입점·세션 구조·로그인 경로 11종 | 전수 |
| `01_sitemap.md` | 사이트맵 — 매핑 1,600 / 고유 URL 1,596 / JSP 화면 847 | **전수** |
| `02_screen_dataflow.md` | 화면별 데이터 흐름 ①~⑦ | 우선순위 1~3의 **22화면** |
| `03_access_scope.md` | 역할별 행 수준 접근 범위 + 판정(🟢🟡🔴⚪) | 전수 스캔 |
| `04_table_usage.md` | 테이블 사용처 R/C/U/D | 335 테이블 |
| `05_answers.md` | 지정 질문 15개 답변 | 전부 응답 |
| `06_findings.md` | **취약점 21 · 버그 17 · 죽은 기능 · 성능 위험** | ⚠️ **외부 공유 금지** |
| `99_progress.md` | 수치 정합성 + **다루지 못한 영역 자진 신고** | |

**미커버 영역** — 비교과 운영자(107매핑)·비교과 활동(78)·채용(145)·취업통계(61)·포트폴리오(40)·참여후기(54)는 사이트맵에는 있으나 데이터흐름 상세가 없다.
**원리적 한계** — 역할↔화면 매핑의 실체(`SY_MENU_AUTH` 972행)와 메뉴 노출 여부(`SY_MENU.USE_YN`)는 **DB 데이터라 소스로 확정 불가**. 사이트맵의 역할 컬럼은 URL 네임스페이스 규약 기반 **추정**이다.

**스택 요약** (`00_overview.md`)

| | |
|---|---|
| Java 1.7 · Spring MVC 3.x · MyBatis 3 · JSP+JSTL · Oracle | Maven/Gradle 없음 (Eclipse WTP) |
| Controller 89개 · JSP 1,246개 · sqlmap 34 / statement 2,033 | 단일 클래스 최대 305KB(`EpMnController`) |
| 인증 프레임워크 없음 (자체 세션 `CAREER_SESSION_KEY`) | 인터셉터·보안 필터 없음 |
| 일일 배치 `DairyBatch`(학사 동기화·SMS·역량집계) | **`web.xml`에서 주석 처리되어 미기동** — 별도 스케줄러 존재 여부 미확인 |

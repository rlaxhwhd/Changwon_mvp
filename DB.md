# DB.md — 현행 운영DB 분석 · 우리 DB 설계 방향

> 국립창원대학교 드림캐치 **고도화** 프로젝트의 **데이터 단일 기준 문서**.
> 조사 기간 2026-07-29 ~ 07-30 · 방법: 운영DB 직접 조사(DBeaver) + 운영 소스 전수 분석(`origin/`, `docs/_analysis/`)
>
> **이 문서가 소유하는 것** — 다른 문서는 여기를 가리키기만 하고 내용을 복사하지 않는다.
>
> | 절 | 내용 |
> |---|---|
> | §3 | **데이터 소유권 경계** — 학사DB(읽기 전용) ↔ 드림캐치(전체 CRUD) |
> | §3-5 | **★ 학과 개편 대응** — 신설·분리·통합·개명. 현행이 감당 못 해 매번 하드코딩하던 지점 |
> | §5 | **권한·역할 체계(RBAC)** — `SY_AUTH` 4단 모델과 실행부의 실태 |
> | §7 | **대학원 차단 원인** — 신분코드 하드코딩 · 학과 트리 |
> | §8-0 | **★ 이관 전제** — 마일리지만 제외, 전 도메인 이관 |
> | §9 | **미결 대장** — 확인 전까지 임의 확정 금지 |
>
> **이 문서에 없는 것** — 화면·기능·구현 상태는 **`SPEC.md` §3**, 구현 이력은 **`Progress.md`**.
>
> | 근거 자료 | 내용 |
> |---|---|
> | `docs/_analysis/` | 현행 소스 전수 분석 8파일 (⚠️ `06_findings.md`는 취약점 21건 — **외부 공유 금지**) |
> | `docs/DB_CURRENT.html` | 현행 DB 실사 상세 (다이어그램 3 + 표 17) |
> | `docs/DB_ERD_EDITOR.html` | **신규 설계 ERD** (편집 가능 · 132테이블) + 역할별 사용 테이블 필터 |
> | `docs/DB_LEGACY_ERD.html` | **현행 운영 DB ERD** (290노드) — 관계선은 컬럼명 역산 **추정** |
> | `docs/dbmeta/V_role.json` | 역할 8종 × 테이블 접근표 (위 필터의 단일소스) |
> | `docs/DB_ERD.html` | 신규 설계 서술본 (왜 그렇게 설계했나) |
> | `docs/dbmeta/*.sql` · `out/*.csv` | 추출 스크립트 A~L (재조사용) · 조사 원본(gitignore) |

---

## 1. 조사 경위

**전산원에 DB 구조를 문의할 수 없는 사정**이 있어, 운영 중인 시스템을 직접 리버스 엔지니어링했다.

| 단계 | 방법 | 산출물 |
|---|---|---|
| 1 | DBeaver로 운영DB 접속 → Oracle 카탈로그 뷰(`USER_*`) 조회 | `docs/dbmeta/out/D1,E2,E3,E4,E5,E7,E8,F2,I2,I3.csv` |
| 2 | 운영 소스 수령 (`origin/sqlmap` 34개 + `origin/co` 컨트롤러 4개) | 스크립트 기반 SQL 전수 분석 |

**조사 원칙** — 읽기 전용 연결, 카탈로그 조회만(사용자 테이블 스캔 없음), 행수는 옵티마이저 통계에서 읽음, 개인정보 미추출(예외: `SY_CODE` 공통코드 — 개인정보 없음).

### 확인 범위와 한계

- 소스는 **상담(Co) 도메인 컨트롤러와 sqlmap 전체**만 받았다. JSP 화면 파일은 없다 → **화면 레이아웃·버튼 배치는 미확인**, SQL과 데이터 흐름은 확정.
- 동적 SQL(MyBatis `<if>`/`<choose>`)은 태그를 제거하고 분석했으므로 조건부 조인 일부는 누락 가능.
- 아래 표기: **확정** = FK·뷰정의·소스 SQL로 증명 / **추정** = 정황 근거 / **미결** = 사람 확인 필요.

---

## 2. 시스템 개요

| 항목 | 값 |
|---|---|
| DBMS | Oracle |
| DB명 / 스키마 / 서버호스트 | `dream` / `DREAMCATCH` / `dreamcatch` |
| 테이블 | **428개** (실사용 100~150, 나머지는 백업·마이그레이션 잔해·미사용 기능) |
| 애플리케이션 | Java + **MyBatis** (`career.framework.*`), URL 패턴 `*.do` |
| SQL statement | **2,033개** (SELECT 1,352 / INSERT 289 / UPDATE 265 / DELETE 220) |
| 소스가 참조하는 테이블 | **270개** (428개 중) — 나머지 158개는 소스에서 아예 안 쓰임 |
| 제약조건 | 157건 중 **FK는 3건뿐** |
| 계정 권한 | CONNECT + RESOURCE (자기 스키마 CRUD, `SELECT ANY TABLE` 없음) |

### 이 시스템의 정체

**다대학 납품 패키지를 이식한 것이다.** 근거:

- `SY_CODE`에 전국 대학·전문대·대학원 **1,900여 개**가 코드로 등록 (그룹 `0070`/`0069`/`0071`)
- `EP_PRM` 컬럼 코멘트에 `[삭제]참가대상-학생-캠퍼스-승학/구덕/부민` — **동아대 캠퍼스명** 잔존
- 0행 미사용 테이블 100여 개(`EP_ACT_*` 예산·출장 20개, `EC_CAN_*` 취업동아리 6개, `ST_CAR_STAT_*` 취업통계 10개, `CA_SURVEY_*`, `PLC_*`, `PC_*`)

→ 창원대가 실제로 쓰는 기능은 일부다. **마이그레이션 범위가 428개가 아니라 100~150개**라는 뜻이며, 이는 좋은 소식이다.

### 도메인별 SQL 규모 (sqlmap)

| 파일 | 네임스페이스 | statement | 도메인 |
|---|---|---|---|
| `ep.xml` | Ep | 370 | 비교과 프로그램 |
| `co.xml` | Co | 323 | **상담** |
| `epAct.xml` | EpAct | 146 | 비교과 활동내역(예산·출장·회의록) |
| `re.xml` | Re | 143 | 채용 |
| `st.xml` | St | 120 | 취업통계 |
| `sy.xml` | Sy | 93 | 시스템(메뉴·권한·코드) |
| `common.xml` | Common | 90 | 공통(학사 연계 포함) |
| `ca.xml` | Ca | 86 | 역량 |
| `ex.xml` | Ex | 82 | 비교과 마일리지 |
| 그 외 25개 | | 580 | 기업·심리검사·포트폴리오·설문 등 |

---

## 3. 데이터 소유권 경계 <span>(확정)</span>

**한 줄 요약: 학사DB는 "누가·어디 소속인지"만 준다. "무엇을 했는지"는 전부 이 DB가 만든다. "무엇을 배웠는지(수강·성적)"는 받을 준비만 해두고 비어 있다.**

### 3-1. 학사DB(BRDB)에서 받는 것

DB 링크가 두 개 있다.

```
DREAMCATCH(dream) ──[ V_BRDB ]──> BRDB   ← 학사DB   (2020-05-07 생성, 현재 가동)
                  ──[ IC2_LINK ]─> IC2    ← 구 시스템 (IC2_* 테이블의 출처, 접속계정 HUMAN)
```

| 테이블 | 행수 | 최근 갱신 | 소스 참조 | 내용 |
|---|---|---|---|---|
| `V_USR_INF` (43컬럼) | 113,733 | 2026-07-28 | **632회** (최다) | **통합 사용자** — 학생·교수·조교·직원 전부, `USER_TY_CD`로 구분 |
| `V_DEP_INF` (9컬럼) | 1,679 | 2026-07-28 | **387회 / 23파일** | 조직 계층 (대학원 누락 의심 — §4-1 참조) |
| `V_DEP_INF_ALL` (10컬럼) | 1,921 | 2026-07-28 | 65회 / 7파일 | 조직 계층 + `UNIV_CODE`(과정구분) |
| `V_ADD_JOB_PART` | 17 | 2026-07-28 | — | 겸직·부서 배정 |
| `V_USR_INF_DUP_INF` | 116 | 2026-04-12 | — | 중복 사용자(학번·사번 동시 보유) |

#### `V_USR_INF`가 주는 필드 (43개)

| 구분 | 컬럼 |
|---|---|
| **식별** | `INTG_UID`(PK) · `LOGIN_ID` · `UUID` · `HS_UID` |
| **신원** | `USR_NM` · `EGNM`(영문) · `CHNM`(한자) · `USR_ENM` · `USR_HNM` · `SEX` · `AGE` · `PHOTO` · `FOR_YN` |
| **신분** | `USER_TY_CD`(학생/교수/조교/직원) · `USR_GBN` · `RANKID`(직위) · `HOFC_STA_CD`(학적·재직상태) |
| **소속** | `DAEHAK_CD`(대학) · `HAKBU_CD`(학부) · `MAJOR_CD`/`MAJOR_CD2`(전공/복수전공) · `MJ2_NM` · `ORGID`/`ORGZ_NM` · `UNIV_CD` |
| **학적** | `STU_SCHGR`/`GRADE`(학년) · `ENTER_DT` · `OUT_DT`/`OUT_GB`/`OUT_TODO`/`OUT_STAT` · `ISU_CNT`(이수학기) · `PLUR_GRAD_YN` · `ERASE_DT` |
| **관계** | **`PROF_ID`(지도교수)** · `DEAN_FLAG`(학과장) |
| **연락처** | `TEL` · `HP` · `EMAIL` · `POST` · `ADDR1` · `ADDR2` |

> ⚠️ **`V_USR_INF`는 113,733행 = 졸업생·퇴직자 누적.** 모든 조회 화면은 반드시 `HOFC_STA_CD`(학적상태) 필터를 걸어야 한다. 안 걸면 학과당 수십 년치 졸업생이 나온다.

#### `V_DEP_INF_ALL` — 조직 계층

대학·학부·학과·전공이 별도 테이블이 아니라 **하나의 자기참조 트리**다.

```
DEPT_CD      조직코드          DEPT_UP_CD   상위 조직코드   ← 계층의 근간
DEPT_NM      조직명            LVL          계층 레벨
DEPT_SUB_NM  보조명            GRP_CD/NM    계층 종류      ← 아래 표
UNIV_CODE    과정구분 (_ALL만)  USE_YN       사용여부(폐과)
ORDERING     정렬
```

**`GRP_CD` 체계 <span>(소스 `assDeptList2`로 확정)</span>**

| `GRP_CD` | `GRP_NM` | `LVL` | 건수 | 사용중 | 폐지 |
|---|---|---|---|---|---|
| `0001` | 단대 | 1 | 16 | 16 | 0 |
| `0002` | **학과** | 2 | 599 | 395 | 204 |
| `0003` | **전공** | 3 | 808 | 553 | 255 |
| `0004` | 기관 | 4 | 498 | 317 | 180 |

즉 **학사 조직은 실질 3단(단대–학과–전공)이고, `LVL4`는 학과 계층이 아니라 부속기관**이다(연구소·센터·대학원 사무조직).

**`USE_YN='N'`이 639건** — 폐지·통합된 조직이 행으로는 남는다.
⚠️ **다만 승계 관계는 알 수 없다.** 컬럼이 9개뿐이고 "이 학과가 무엇으로 통합·분리됐는지"를 가리키는 필드가 없다. `USE_YN='N'`은 "없어졌다"만 말하고 **어디로 갔는지는 말하지 않는다.** → 학과 개편 대응은 §3-5 참조.

**`LVL1` 단대 16개 실제 목록**

```
인문대학 · 사회과학대학 · 경상대학 · 경영대학 · 자연과학대학 · 공과대학
메카트로닉스대학 · 예술대학 · 미래융합대학 · 글로컬첨단과학기술대학
GAST-인공지능대학 · GAST-기계공학대학 · 거창캠퍼스 · 남해캠퍼스 · 본부 · 기관
```

> 🔧 **`src_admin/data/colleges.ts` 수정 필요**
> - `글로벌첨단과학기술대학` → **`글로컬첨단과학기술대학`**
> - 누락: `경상대학` · `미래융합대학` · `GAST-인공지능대학` · `GAST-기계공학대학` · `거창캠퍼스` · `남해캠퍼스`
> - **`간호대학`은 단과대가 아니다** — `간호학과`(코드 `334`)는 상위 `4`(자연과학대학) 소속

> ⚠️ **동명 학과가 여러 개 존재한다.** `신소재공학부`가 코드 `505`(공과대학)·`501`(메카트로닉스대학)·`1528`(대학원 석사)·`2528`(대학원 박사)로 4개다. **학과명으로 매칭하면 깨진다. 반드시 `(단대코드, 학과코드)` 쌍으로 다뤄야 한다.**

### 3-2. 받을 준비만 해두고 비어 있는 것 <span>(전부 0행)</span>

| 테이블 | 컬럼 | 상태 |
|---|---|---|
| `V_SUGANG` (15) | `YEAR`·`SMT`·`STUDENT_CD`·**`CURI_NUM`(과목코드)**·`COURSE_CLS`(이수구분)·**`GRAD_DIV`(과정구분)**·`STUDENT_YEAR`·**`GRADE`(등급)**·**`GPA`**·`FINISH_YN`·`CHK_RECURI`(재수강)·`CANCEL_YN` | 0행, 통계 2020-05-04 |
| `V_LECT_INF` (20) | `CURI_NUM`·**`CURI_NM`(과목명)**·**`CDT_NUM`(학점)**·`DEPT_CD`·`PROF_ID`·`PROF_NM`·`CAP_PER1~6`(정원)·`COURSE_CLS`·`GRAD_DIV` | 0행, 통계 2020-05-04 |
| `V_PC_SUGANG` (7) | 한글 발음 컬럼명 (`SUGANG_YUNDO`…) | 구버전, 0행 |
| `V_USR_SCORE` (34) | `V_USR_INF`와 동일 구조 | 이름만 성적, 실제로는 사용자정보 복제본 |
| `VIEW_SUGANG_ALL` | `SUGANG_YUNDO`·`SUGANG_HAKGI`·`SUGANG_GWAMOK`·`SUGANG_BUNBAN`·`GWAMOK_KORNAME`·`SUGANG_HAKBUN`·`SUGANG_GYOSU` | **학사 원본 뷰 이름 = `수강전체`** |
| `VIEW_SUUP_GAESUL` | `GAESUL_*` | **학사 원본 뷰 이름 = `수업개설`** |
| `VIEW_HAKGWA_HYUNJAE` | `DEPT_CD`·`DEPT_NM`·`SORT` | `학과현재` |
| `VIEW_MEMBER_SOSOK` | `USER_NUM`·`USER_DEPT_CD`·`USER_DEPT_NM`·`USER_GUBUN` | `멤버소속` |
| `COM_HAKJUK` (24) | 성명·학적상태·학번·학과·학년·이수학기·생년월일·입학일·졸업일·**학사경고**·학위종별·주소·이메일·휴대폰·**부전공1·2/복수전공1·2**·교직·교원자격증번호 | 가장 상세한 학적 스키마, 0행 |
| `COM_HAKBYUNDONG` | 학번·변동일자·구분·사유·기간·비고 | 학적변동 이력, 0행 |

#### ★ 쿼리는 이미 작성돼 있다

소스에 **수강·강의를 조인하는 SQL이 이미 존재한다.**

```
Ca.getGoalCour_List   ca.xml:334    FROM V_SUGANG S1 INNER JOIN V_LECT_INF L1 ON ...
Ca.getLecList         ca.xml:2345   FROM V_SUGANG S1 INNER JOIN V_LECT_INF G1 ON ...
Common.getLecList     common.xml:1325
```

조인키: `YEAR` + `SMT` + `GRAD_DIV` + `CURI_NUM` (4컬럼 복합).

즉 **교과 역량 산출에 수강 데이터를 쓰려고 만들어 놓고, 적재가 켜지지 않아 동작하지 않는 상태**다. 우리가 만들려는 스킬트리·직무로드맵의 전제 데이터가 정확히 이것이다.

> 💡 **이것이 이번 고도화의 최대 기회다.** 테이블도, DB 링크도, 조인 쿼리도 이미 있다. **새로 협의할 일이 아니라 적재를 켜는 문제**일 가능성이 높다.

### 3-3. 자체 소유 데이터

소스 SQL 전수 분석 결과: **쓰기가 있는 테이블 219개 / 읽기 전용 51개.**

| 영역 | 핵심 테이블 | 행수 |
|---|---|---|
| **상담** | `COUNSEL_MASTER`(46컬럼, 신청+결과 한 행) · `COUNSEL_PROBLEM` · `COUNSEL_FAM` | 5.4만 |
| **상담 가능시간** | `BASICSETTING` (일자×시×분 슬롯 전개) | 57만 |
| **상담사** | `COM_CON_INF`(51컬럼, 26명) · `COM_CON_TAR`(담당 단과대) · `CO_CON_SCH` | — |
| **교수상담** | `CON_PROF_INFO`(최다 사용) · `CO_ADVISER`(지도교수 배정) | 18.4만 / 1.4만 |
| **조교·교수 배정** | `FU_ASS_DEPT`(조교) · `TB_CARR_PROF_ASSI_DEPT`(교수) | 190 / 176 |
| **심리검사 8종** | `CHECK_MBTI`·`CHECKHOLLAND`·`CHECKENNEAGRAM`·`CHECKE_DISC`·`CHECKMMPI2`·`CHECKTA`·`CHECKUANDI`·`CHECKMLST` | 각 83~1,935 |
| **비교과** | `EP_PRM`(125컬럼) → `EP_PRM_STEP`(차수) → `EP_PRM_APP`(신청) → `EP_PRM_RESULT`(역량점수) | 807 / 6.8만 / 1.4만 |
| **비교과 마일리지** | `EX_ITEM` / `EX_ITEM_APP` / `EX_ITEM_SCORE` | 1.1만 |
| **역량 현황** | `TB_CARR_CAPA_STTS`(학점·토익·자격증·봉사·수상·인턴) · `TB_CARR_ETR_CAPA`(기업 요구역량) | 8.4만 / 463 |
| **진로설계** | `STU_COURSE_INFO`(+`_DTL`) · `STU_COURSE_CAREER_LICN`/`_LANG`/`_ACTIVE` · `ST_GOAL`(목표기업) | 1.4만 / 7만 |
| **채용** | `JOBSMASTER`(82컬럼) · `JOBSOCCUPATION` · `JOBSAREA` · `APPLICATIONMASTER`(지원) | 1.7만 / 3,812 |
| **직무 분류** | `FU_JOB_MSTR`(3계층) · `FU_PURPOSE_ADD_JOB`(희망직무) | 865 / 3만 |
| **이력서·자소서** | `SS_JOB_RES`(34컬럼, 상담사 첨삭 포함) · `JOB_RES*` · `JOB_SELF_INT` | — |
| **시스템** | `SY_CODE`(3,874) · `SY_MENU`(343) · `SY_AUTH`(13) · `SY_FILE`(4.2만) · `SY_AGREE`(동의) | — |
| **로그** | `SY_MENU_LOG`(647만) · `SY_SYSTEM_CONN_INFO`(449만) · `SY_LOGIN_LOG`(94만) · `SC_LOG_YYYYMM`(76개 테이블) | — |

### 3-4. ⚠️ 예외 — 학사 적재본에 쓰기가 있다

**소스 분석에서 발견한 중요한 예외다.** `V_USR_INF`는 학사 적재본인데 애플리케이션이 직접 쓴다.

| statement | 위치 | 하는 일 |
|---|---|---|
| `Co.setOutConInfo_Insert2` | `co.xml:372` | **`INSERT INTO V_USR_INF`** — 외부 위촉 상담사를 학사 적재본에 직접 추가 |
| `Co.setCnCt_Delte3` | `co.xml:923` | `DELETE FROM V_USR_INF WHERE INTG_UID IN (…) AND USER_TY_CD=? AND HOFC_STA_CD=?` — 외부 상담사 삭제 |
| `Common.delete_V_USR_INF_dup` | `common.xml:1652` | 중복 사용자 정리 |
| `Common.UPDATE_ORGID_U01` | `common.xml:2542` | `UPDATE V_USR_INF SET ORGID = (SELECT ORGID FROM V_USR_INF@V_BRDB WHERE emp_no = a.intg_uid)` — **DB 링크 직접 조회로 보정** |

**의미**

1. **학사DB에 없는 사람(외부 위촉 상담사)을 학사 적재본에 섞어 넣는다.** `USER_TY_CD` + `HOFC_STA_CD` 조합으로 구분한다. `COM_CON_INF.INOUT_GB`(내외부구분)와 `CONPWD`(자체 비밀번호)가 이 구조의 짝이다.
2. 학사DB 원본 테이블명도 `V_USR_INF`이고, **`@V_BRDB` 로 직접 조회하는 SQL이 소스에 존재**한다 → 필요 시 실시간 조회가 가능하다는 뜻.
3. 즉 `V_USR_INF`는 **순수 적재본이 아니라 "학사 데이터 + 외부 인원"이 섞인 하이브리드 테이블**이다.

> 🔧 **신규 설계에서는 이 혼합을 분리한다.** 학사 유래(`DC_USER` ← 미러)와 자체 발급(외부 상담사·기업 담당자)을 같은 테이블에 섞지 않고, `DC_USER.source`로 구분하거나 별도 테이블로 둔다. 현행 방식은 야간 적재가 외부 인원을 지워버릴 위험이 상존한다.

---

### 3-5. ★ 학과 개편 대응 <span>(신설·분리·통합·개명)</span>

**대학은 학과가 자주 신설·분리·통합·개명된다. 현행은 이걸 감당하지 못해 매번 사람이 개입해 왔다.**
운영 증상: **학사DB에는 있는 학과가 관리자 화면(`회원관리 > 조교학과 배정`)에만 없다** → 전산원에 학과 추가 요청 → 코드에 하드코딩 매핑.
이건 일회성 버그가 아니라 **상시 요구사항**이므로 설계에 넣는다.

#### 왜 그런가 — 원인이 여러 겹이다

| 층 | 원인 | 근거 |
|---|---|---|
| **① 반입** | **`V_DEP_INF`는 뷰가 아니라 로컬 테이블**이다. 학사DB에서 `DairyBatch`(매일 01:00) → `Common.merge_V_DEP_INF` **MERGE로 복제**한다 | `04_table_usage.md` §1 (쓰기 1건 = 배치) |
| | ⚠️ **그 배치가 `web.xml:20`에서 주석 처리되어 미기동.** 별도 스케줄러로 도는지 **미확인** — 최근 갱신은 2026-07-28이라 어떤 경로로든 돌고는 있다 | `00_overview.md` · `04_table_usage.md` §1 |
| | MERGE는 **컬럼 목록이 SQL에 고정**이다. 학사DB에 컬럼이 늘어도 따라오지 않고, 로컬 테이블 DDL도 수동이다 <span>(추정 — MERGE 본문 미확인)</span> | 구문 특성 |
| **② 이중화** | 트리가 **두 벌**이다. `V_DEP_INF`(1,679) / `V_DEP_INF_ALL`(1,921, 대학원 포함). 배치도 `merge_V_DEP_INF`와 `merge_V_DEP_INF_ALL`을 **따로** 돌린다 | `DairyBatch.java:82-87` |
| | 화면이 제각각 본다 — `V_DEP_INF` **193 statement** / `_ALL` **34 statement**. 한쪽에만 들어오면 화면마다 결과가 다르다 | `05_answers.md` Q3 |
| **③ 조회** | `Fu.assDeptList2`에 학과를 떨어뜨리는 필터가 **4개** (`SPEC.md` §3-4-① 표) | `fu.xml:331-372` |
| | 특히 **`WHERE C.USE_YN='Y'`로 outer join이 붕괴** → **전공(LVL3)이 없는 학과가 통째로 탈락.** 신설 학과의 전형적 상태다 | `fu.xml:357` |
| | 조직코드를 SQL 리터럴로 배제하는 곳이 있다 — `DEPT_CD NOT IN ('3')`. 분석 원문: *"조직 개편 시 소리 없이 깨짐"* | `06_findings.md` |
| **④ 파생** | 신설 학과는 **딸린 자체 데이터가 전부 비어 있다** — `FU_ASS_DEPT`(조교) · `TB_CARR_PROF_ASSI_DEPT`(교수) · `COM_CON_TAR`(상담사 단과대) · `CA_GOAL`(학과 역량목표). 자동 생성 경로가 없다 | `04_table_usage.md` |
| **⑤ 식별** | 동명 학과가 과정별로 존재한다 (`신소재공학부` 505·501·1528·2528). **학과명으로 매칭하면 깨진다** | §3-1 |
| **⑥ 이력** | `V_DEP_INF_ALL`은 컬럼이 9개뿐이라 **승계 관계를 표현할 수 없다.** `USE_YN='N'`은 "없어졌다"만 말하고 어디로 갔는지는 말하지 않는다 | §3-1 |

> **현행의 우회책 — 더미 전공 행.** ③을 피하려고 전공이 없는 학부를 **자기 자신을 전공으로 한 번 더 등록**해 둔다(`건축학부 / 건축학부 / 436 / 436`). 트리에 실재하지 않는 노드를 만드는 것이므로 **우리는 만들지 않는다.**

#### 우리 설계 규칙

1. **복제하지 말고 조회한다.** 불가피하게 미러링한다면 MERGE 컬럼 나열이 아니라 **스키마 드리프트 감지**를 붙인다(학사DB에 컬럼이 늘면 알림).
2. **트리는 `V_DEP_INF_ALL` 하나만.** 두 벌 금지. 조회는 `getDeptTree()` **단일 진입점**을 거친다 — 현행처럼 227곳에 흩어지면 같은 문제가 반복된다.
3. **전공 없는 학과를 반드시 노출한다.** 전공 조건은 `ON` 절에. 더미 전공 행 금지.
4. **조직 필터를 SQL 리터럴이 아니라 데이터로** — `USE_YN`·`GRP_CD`·`UNIV_CODE` 플래그로만 거른다. 상위 조직 종류를 단대로 한정하지 않는다.
5. **`(collegeCode, deptCode)` 쌍으로 식별.** 학과명 매칭 금지.
6. **배정 `majorCode`는 NULL 허용 = "학부 전체"** 로 정의하고, 학생 조회가 `deptCode` 기준으로도 매칭되게 한다.
7. **이벤트 레코드에 학과 as-of 스냅샷**(코드 + 당시 명칭). 개편돼도 과거 상담·비교과 이력이 깨지지 않는다.
8. **학과 개편 이력 테이블 신설** — `DC_DEPT_LINEAGE(from_dept_cd, to_dept_cd, kind: 신설|분리|통합|개명, effective_from, reason)`. 현행에 없는 것이고, 없으면 **"작년 컴퓨터공학과 상담 실적"을 개편 후에 답할 수 없다.**
9. **★ 누락 감지 화면.** 학사DB 원본 ↔ 우리 트리를 대조해 **"학사DB에는 있는데 우리에 없는 학과 N건"** 을 관리자에게 상시 노출한다. 현행은 사람이 발견할 때까지 아무도 모른다.

---

## 4. 소스 분석으로 확정된 것

### 4-1. ★ 조교학과 배정 화면 — 대학원 학과가 안 보이는 원인 <span>(확정)</span>

관리자 `회원관리 > 조교학과 배정`(`/Sa/Fu/sys_assDept.do`)의 목록 쿼리는 `Fu.assDeptList2`다.

```sql
FROM V_DEP_INF A                                          -- ① 학과
INNER JOIN      V_DEP_INF B ON A.DEPT_UP_CD = B.DEPT_CD   -- 단대
LEFT OUTER JOIN V_DEP_INF C ON C.DEPT_UP_CD = A.DEPT_CD   -- 전공
LEFT OUTER JOIN V_USR_INF D ON D.MAJOR_CD  = C.DEPT_CD    -- 학생 수 카운트
WHERE A.USE_YN='Y' AND B.USE_YN='Y' AND C.USE_YN='Y'      -- ② 여기가 문제
  AND A.DEPT_UP_CD IN (SELECT DEPT_CD FROM V_DEP_INF
                       WHERE LVL='1' AND USE_YN='Y' AND GRP_CD='0001')
  AND A.GRP_CD IN ('0002','0003')
...
LEFT OUTER JOIN FU_ASS_DEPT CAD ON T.MAJOR_CD = CAD.MAJOR_CD
```

**누락 원인 두 가지**

| # | 원인 | 결과 |
|---|---|---|
| ① | **`V_DEP_INF`를 쓴다** (`V_DEP_INF_ALL`이 아니다) | `_ALL`보다 **242개 적다.** 대학원 학과가 여기 빠져 있을 가능성이 높다 |
| ② | `LEFT OUTER JOIN … C` 인데 **`WHERE C.USE_YN='Y'`** | **outer join이 inner join으로 붕괴** → **전공(LVL3)이 없는 학과는 목록에서 사라진다** |

②가 특히 중요하다. SQL 문법상 `LEFT JOIN`한 테이블의 컬럼을 `WHERE`에서 조건 걸면 `NULL` 행이 제거되어 outer join의 의미가 사라진다. 대학원 학과는 전공이 등록되지 않은 경우가 많아 여기서 대량 탈락한다.

**정정** — 이전 조사 단계에서 목록 출처를 `FU_CODE`(자체 테이블, 2020년 이후 갱신 없음)로 추정했으나, **소스 확인 결과 `V_DEP_INF`가 맞다.** `FU_CODE`는 `Fu.getapicode_List` 1곳에서만 참조되는 별개의 죽은 테이블이다.

**`FU_ASS_DEPT`의 역할** — 목록 소스가 아니라 **배정 저장용**이다.

```sql
insertAssDept : INSERT INTO FU_ASS_DEPT (DEPT_CD, MAJOR_CD, USR_ID, USR_NM)
deleteAssDept : DELETE FROM FU_ASS_DEPT WHERE DEPT_CD=? AND MAJOR_CD=? AND USR_ID=?
```

PK가 없고 이 3컬럼 조합으로 관리한다 → **중복 방지는 애플리케이션 책임.** 삭제 이력도 남지 않는다.

> 🔧 **해결** — `V_DEP_INF_ALL`로 바꾸고, `C.USE_YN='Y'`를 `AND (C.USE_YN='Y' OR C.DEPT_CD IS NULL)` 또는 `ON` 절로 옮긴다. 그리고 `UNIV_CODE`를 표시·필터 축에 추가한다.

> ⚠️ **네이밍 함정** — `Fu.getDaehakCode_List`는 이름이 "대학코드"인데 실제로는 `GRP_CD='0002'`(**학과**)를 조회하고, `getHakgwaCode_List`는 `GRP_CD='0003'`(**전공**)을 조회한다. 화면 라벨과 쿼리 이름이 한 단계씩 어긋나 있다. 코드를 읽을 때 이름을 믿지 말 것.

### 4-2. 실제 조인 관계 <span>(FK가 없는 DB의 관계 복원)</span>

sqlmap에서 별칭을 해석해 **132종의 조인 관계**를 추출했다. 출현 빈도 상위:

| 빈도 | 조인 | 의미 |
|---|---|---|
| 48× | `ST_CAR_STAT_REPORT.INTG_UID = V_USR_INF.INTG_UID` | 취업통계 ↔ 사용자 |
| 28× | `EP_PRM.PRM_SEQ = EP_PRM_APP.PRM_SEQ` | 프로그램 ↔ 신청 |
| 14× | `V_DEP_INF.DEPT_CD = V_USR_INF.DAEHAK_CD` | 사용자 소속(대학) |
| 14× | `V_DEP_INF.DEPT_CD = V_USR_INF.HAKBU_CD` | 사용자 소속(학부) |
| 13× | `EP_PRM_APP.USRID = V_USR_INF.INTG_UID` | 신청자 ↔ 사용자 |
| 12× | `EP_PRM_APP.PRM_APP_SEQ = EP_PRM_MAPPING.PRM_APP_SEQ` | 신청 ↔ 팀 매핑 |
| 10× | `COUNSEL_PROBLEM.GRP_CODE/CODE = SY_CODE.GRP_CODE/CODE` | 상담 문제유형 ↔ 공통코드 |
| 6× | `EP_PRM_CHARGE.CH_UID = V_USR_INF.INTG_UID` | 프로그램 담당자 |
| 5× | `V_DEP_INF.DEPT_CD = V_USR_INF.ORGID` | 교직원 소속 |
| 3× | `V_LECT_INF.(YEAR,SMT,GRAD_DIV,CURI_NUM) = V_SUGANG.(…)` | **강의 ↔ 수강 (미가동)** |

**핵심: 학생 조인은 `INTG_UID`로 통일돼 있다.** 컬럼명은 테이블마다 `USRID`·`USERID`·`STU_NO`·`STU_ID`·`INTG_UID`로 갈리지만 값은 모두 `INTG_UID`(학번/사번)다.

**소속 조인은 세 갈래다** — 학생은 `DAEHAK_CD`(대학)·`HAKBU_CD`(학부)·`MAJOR_CD`(전공), 교직원은 `ORGID`. 화면 목적에 따라 다른 컬럼을 쓴다.

### 4-3. 화면 구조 — 역할별 재사용 패턴

`SY_MENU`(343건) + `SY_MENU_AUTH`(972건) + `SY_AUTH`(13건)로 역할별 화면이 결정된다.

**같은 화면을 파라미터로 역할 분기한다.**

```
/user/Co/CoMc070L.do?TYPE=T   상담사   → 포트폴리오 관리
/user/Co/CoMc070L.do?TYPE=A   조교     → 학생현황
/user/Co/CoMc070L.do?TYPE=M   프로그램담당자 → 포트폴리오 관리
```

→ **우리도 `StudentList`를 이 방식으로 재사용하면 조교 화면을 거의 새로 만들지 않아도 된다.**

---

## 5. 권한·역할 체계 (RBAC)

```
SY_AUTH        역할 마스터    13건   AUTH_CODE(PK)·AUTHNM·BASEGRUP_YN·USE_YN
   ↓ AUTH_CODE
SY_AUTH_USER   사람 ↔ 역할    52명   AUTH_USER = INTG_UID
SY_MENU_AUTH   역할 ↔ 메뉴   972건
   ↓ MENU_CODE
SY_MENU        메뉴 트리     343건   MENU_CODE(PK)·MENU_URL·MENU_LEVL·PRTCODE·USER_DVID
SY_AUTH_LOG    권한 변경 이력 278건
```

### 역할 13개 실제 현황

| AUTH_CODE | 역할명 | 기본그룹 | 배정 | 메뉴수 | 비고 |
|---|---|---|---|---|---|
| `AUTH0006` | 슈퍼관리자 | N | **28** | 238 | 실질 관리자 |
| `AUTH0012` | **상담사** | N | **19** | **14** | **실제 운영 상담사 역할** |
| `AUTH0005` | 상담사 | Y | 1 | **183** | 상담센터 총괄급 (회원·시스템 관리 포함) |
| `AUTH0003` | 교원 | Y | 1 | 95 | |
| `AUTH0002` | 조교 | Y | 2 | 91 | 실제 전용 화면은 5개 |
| `AUTH0011` | 교무과 학사관리과 | N | 1 | 7 | 장기결석·학사경고 관리 |
| `AUTH0009` | 창대한담당자(온라인멘토링) | N | 1 | 14 | |
| `AUTH0001` | 학생 | Y | **0** | 116 | |
| `AUTH0000` | 비로그인 | Y | 0 | 76 | |
| `AUTH0004` | 직원 | Y | 0 | 78 | 설명란에 사람 이름("박단비") |
| `AUTH0008` | 기업 | N | 0 | 50 | `TB_CARR_USER` 자체 인증 경로 사용 |
| `AUTH0007` | 창대한담당자 | N | 0 | 0 | 껍데기 |
| `AUTH0010` | 멘토 | N | 0 | 16 | `USE_YN='N'` 폐기 |

**`BASEGRUP_YN='Y'` = 학사 신분(`USER_TY_CD`)으로 자동 매핑되는 역할이다.** 학생·비로그인·조교·교원·직원이 여기 속하고 배정 인원이 0~2명인 이유가 이것이다(예외적 수동 배정만 등록). `'N'`인 역할(슈퍼관리자 28명, 상담사 19명 등)은 명시적으로 부여해야 한다.

> **소스로 확정 (2026-07-31).** 이 자동 매핑은 DB 플래그가 아니라 **자바 코드가 수행한다.** `PUtil.java:1002-1031`이 세션의 `USER_TY_CD` 1글자를 보고 `AUTH0001`(S 학생)·`AUTH0002`(A 조교)·`AUTH0003`(P 교수)·`AUTH0004`(M 직원)·`AUTH0005`(T 상담사)·`AUTH0008`(C 기업)·`AUTH0010`(MT 멘토)를 **권한 리스트에 덧붙인다.** `SY_AUTH_USER`에서 실제로 오는 것은 `AUTH0006`(슈퍼관리자)·`AUTH0007`뿐이다.
> → **`SY_AUTH_USER` 52명은 전체 권한자가 아니라 "추가권한 보유자" 명단이다.** 우리 설계도 이 두 갈래(자동/명시)를 유지한다 — `SPEC.md` §7-1.

**상담사 역할이 두 개다.** `AUTH0012`(19명, 14메뉴)가 실무이고 `AUTH0005`(1명, 183메뉴)는 총괄이다.

### ⚠️ 모델은 표준적인데 실행부가 강제하지 않는다

**4테이블 권한 모델 자체는 그대로 계승할 가치가 있다.** 문제는 이것을 **URL 레벨에서 강제하지 않는다는 점**이다.

| 실태 | 근거 |
|---|---|
| 인증·인가 **필터/인터셉터가 없다.** 각 컨트롤러의 `@ModelAttribute` 보일러플레이트가 대신한다 | `web.xml`의 필터는 인코딩 하나뿐. `<mvc:interceptors>` 선언 없음 |
| 인가 검사가 **`REFERER` 헤더가 없을 때만** 동작한다 | `CareerActionController.java:191-193` — REFERER가 있으면 즉시 `return` |
| 검사에 걸려도 `sendRedirect` 직후 `return`이라 **핸들러와 SQL은 그대로 실행**된다 | 화면엔 안 보이나 조회는 일어난다 |
| **엑셀 다운로드 컨트롤러 14종(엔드포인트 130개)** 은 기반 클래스를 상속조차 하지 않아 인가 로직이 없다 | 전체 1,600 매핑 중 약 295건(18%)이 검사를 거치지 않음 |

→ **재구축 필수 사항: "URL ↔ 필요권한" 매핑을 서버측 단일 지점에서 강제하고, 권한이 없으면 핸들러 실행 전에 끊는다.** 엑셀 export는 목록 API와 **같은 권한·같은 쿼리**를 타야 한다. 상세는 `docs/_analysis/06_findings.md`.

### 통제 범위는 "메뉴"까지다

`SY_MENU_AUTH`는 **어떤 메뉴를 볼 수 있나**만 통제한다. **어떤 학생 데이터를 볼 수 있나**(행 수준)는 통합 규칙 없이 도메인마다 흩어져 있다.

| 역할 | 데이터 범위 테이블 | 단위 |
|---|---|---|
| 상담사 | `COM_CON_TAR` | **단과대** |
| 조교 | `FU_ASS_DEPT` | **학과 + 전공** |
| 교수 | `TB_CARR_PROF_ASSI_DEPT` | 학과 + 전공 |

**데이터 열람 감사는 없다.** `SY_AUTH_LOG`는 권한 변경만 기록한다. 상담 기록·벌점 같은 민감 데이터의 열람 이력이 남지 않는다.

2025-03-01에 권한 체계를 개편한 흔적이 있다(`SY_AUTH_20250301`·`SY_MENU_20250301` 등 스냅샷). **권한 구조를 건드릴 때 백업을 뜨는 관행**이 있다.

---

## 6. 우리 프로젝트와의 기능 격차

> **→ 역할별 화면 목록·구현 상태·커버율은 `SPEC.md` §3이 단일 소스다.**
> 현행 847화면 전수 대조 결과가 거기 있고, 이 문서는 중복해서 적지 않는다.

여기서는 **DB 관점에서만 의미 있는 것** 두 가지를 남긴다.

**① 데이터는 있는데 화면이 없는 도메인** — 이관 대상인데 받을 화면이 없으면 이관이 무의미해진다.

| 도메인 | 현행 데이터 | 우리 화면 |
|---|---|---|
| 채용 지원 (`APPLICATIONMASTER`) | **1.7만 건** | ❌ 공고 열람만 있고 **지원 경로가 없다** |
| 진로설계 (`STU_COURSE_*`) | **1.4만 건** | ❌ 없음 |
| 이력서 첨삭 (`SS_JOB_RES`) | 워크플로 완비(`STATUS`·`CON_ID`·`CONCONT`) | ❌ 없음 |
| 비교과 만족도·참여후기 | 운영 중 | ❌ 없음 |
| 비교과 그룹형·차수 (`EP_PRM_STEP`) | 운영 중 | ❌ 개인형만 |

**② 우리에게만 있는 것 (이관 대상 없음)** — 로드맵 편집기·로드맵 변경요청함·진단 4종(C-2~C-CORE)·IAP·성장경험일지·퀘스트/레벨·챗봇·열람 감사로그.

---

## 7. 대학원 대응 <span>(필수 요건)</span>

**신규 프로젝트는 대학원을 지원해야 한다.** 대학원 데이터는 학사DB에 이미 있다 — `UNIV_CODE`가 과정 구분 코드다.

| `UNIV_CODE` | 의미 | 건수 |
|---|---|---|
| `00` | 학부 | 725 |
| `01` | **대학원 석사** | 364 |
| `02` | **대학원 박사** | 69 |
| `03`~`12` | 전문·특수대학원 | 226 |
| `90` | 기타 | 21 |
| (빈값) | 기관 | 515 |

대학원 학과는 **`LVL2`(학과)에 학부 학과와 나란히** 있고, 이름 뒤에 표기가 붙는다.

```
"2","1523",나노신소재공학과 (대학원-석사),"5",학과,"01",Y
"2","2526",나노신소재공학과 (대학원-박사),"5",학과,"02",Y
"2","1528",신소재공학부   (대학원-석사),"5",학과,"01",Y
```

대학원 조직 자체(`대학원`·`교육대학원`·`행정대학원`·`경영대학원`·`산업대학원`·`사회복지대학원`·`보건대학원`·`글로컬첨단과학기술공학대학원`)는 **`LVL4` "기관"**(`GRP_CD='0004'`)으로 등록돼 있다.

### ★ 대학원생을 막는 관문은 두 겹이다 <span>(2026-07-31 소스 실측)</span>

학과 트리만 고치면 **절반만 열린다.** 더 근본적인 차단은 **신분코드 하드코딩**이다.

| 관문 | 원인 | 규모 |
|---|---|---|
| **① 신분코드** | `USER_TY_CD` 리터럴 `'1101'`(재학생-학부)이 sqlmap에 박혀 있다 | **229곳**. `'1201'`(재학생-대학원)이 조건에 들어간 곳은 **단 3곳** |
| **② 학과 트리** | `V_DEP_INF`(대학원 미포함) 사용 + outer join 붕괴 + 상위조직 `GRP_CD='0001'` 한정 | 조교·교수 배정 화면 계열 |

**`USER_TY_CD` 코드값 확정** (`common.xml:280-288` 주석에서 직접 확인)

| 코드 | 의미 | 세션 |
|---|---|---|
| `1101` | 재학생-학부 | `S` |
| `1102` | 졸업생-학부 | `S` |
| **`1201`** | **재학생-대학원** | `S` |
| **`1202`** | **졸업생-대학원** | `S` |
| `1301` | 교원 | `P` |
| `1401` | 직원 | `M` |
| `1501` | 조교 | `A` |

**신분코드가 막고 있는 구체적 지점**

| 화면 | 조건 | 결과 |
|---|---|---|
| 전담교수 배정 대상 (`Co.getAdminMatch_List`, co.xml:6255) | `USER_TY_CD IN ('1101')` | **대학원생에게 전담교수 배정 불가** |
| 조교 담당 학생 (`Co.getAssiList`, co.xml:6352) | `USER_TY_CD IN ('1101')` | 조교 화면에 대학원생이 안 보임 |
| 상담 학생검색 팝업 (co.xml:7345·7347·7440·7451·7462·7473·7508) | `USER_TY_CD = '1101'` | 상담사가 대학원생을 검색 불가 |
| 역량진단 대상자 "전체" (ca.xml:746) | `USER_TY_CD = '1101'` | 대학원생이 진단 대상에서 제외 |
| 비교과 선발자 SMS (ep.xml:10020) | `USER_TY_CD = '1101'` | 선발돼도 문자 미발송 |

파일별 분포: `st`103(취업통계 — 학부 기준이 정책일 수 있음) · `ca`40 · `sy`24 · `co`22 · `fu`8 · `common`7 · `acSt`5 · `pc`5 · `epAct`4 · 기타.

> **반대로 현행도 대학원을 인지하는 곳이 있다.** 상담 슬롯의 재학/졸업 대상 판정은 `1101 OR 1201` / `1102 OR 1202`로 4종을 모두 본다(co.xml:7580-7581). **스키마는 이미 대학원을 담고 있고, 일관성만 없다.**

### 요구사항 — 반드시 열어야 하는 것 <span>(확정)</span>

| 요건 | 현행 상태 | 대응 |
|---|---|---|
| **대학원생이 비교과 프로그램에 신청할 수 있어야 한다** | **이미 열려 있다.** `ep.xml`에 학부 한정 필터가 없고, 참가대상 플래그가 `TRGT_STU21~24`(대학원)와 `TRGT_STU31~35`(학부)로 분리 존재. 단 신청 자격 판정이 PL/SQL `FN_GET_PRM_TRGT_YN()` 안에 있어 **함수 본문 확인 필요** | 참가대상 UI에 대학원 학년 노출 + 알림·SMS 대상에서 누락 방지 |
| **대학원생이 교수에게 상담을 신청할 수 있어야 한다** | **신청 기록(`CON_PROF_INFO`)에는 제약 없음.** 막히는 곳은 **교수 목록 조회**(`Co.getProfList`, co.xml:6665): `ORGID = 학생 HAKBU_CD` 매칭 + `RANKID IN ('1001','1002','1003')` 직급 3종 한정 | 소속 매칭을 `V_DEP_INF_ALL` 기준으로, 직급 화이트리스트를 코드 테이블로 분리 |

> **발견**: 오프라인 교수상담 슬롯은 **상담사와 같은 `BASICSETTING` 테이블**을 쓴다(`CONSULTANTID`에 교수 `INTG_UID`). 슬롯 모델을 상담사 전용으로 설계하면 안 된다. (`Co.getStuProfSch_List`, co.xml:7184)

### 설계 반영 사항

| # | 반영 |
|---|---|
| 1 | **`V_DEP_INF_ALL`을 쓴다.** `V_DEP_INF`는 242개 적고 대학원 누락 |
| 2 | **`UNIV_CODE`를 필터·표시 축에 포함** — 학부/석사/박사/전문대학원 구분 |
| 3 | **학년 1~4 고정 금지.** 대학원은 1~3학년. 현행 `EP_PRM`이 `TRGT_STU21~24`(대학원)와 `TRGT_STU31~35`(학부)를 분리한 이유 |
| 4 | 학과명에 `(대학원-석사)` 접미가 붙어 온다 → 파싱하거나 `UNIV_CODE`로 배지 처리 |
| 5 | **동명 학과가 과정별로 여러 개** → `(단대코드, 학과코드)` 쌍으로 식별. 학과명 매칭 금지 |
| 6 | **진단 학년 매핑 정책이 학부 기준**(C-2=1학년…C-4=4학년) → 대학원생 정책 **미정** |
| 7 | **★ 신분코드를 리터럴로 쓰지 않는다.** 학생 대상 판정은 반드시 집합 상수(`STUDENT_ENROLLED` = 재학생-학부 + 재학생-대학원)로. 학부 한정이 정책인 곳은 **그 이유를 주석으로 남긴다** — `SPEC.md` §7-2 |
| 8 | **대학원 수료생(`COMPLETED` 학적)을 목록에서 빠뜨리지 않는다** — 대상 포함 여부는 정책 확인 필요 |

### 전산원 요청 항목

1. **`V_DEP_INF_ALL` 사용 승인** — 대학원 포함 조직 트리
2. **`V_SUGANG` · `V_LECT_INF` 적재 활성화** — `GRAD_DIV`(과정구분) 포함. 스킬트리·직무로드맵의 전제
3. 학사 원본 뷰 **`수강전체`(`VIEW_SUGANG_ALL`)** · **`수업개설`(`VIEW_SUUP_GAESUL`)** 연계 — 뷰 이름을 알고 있으므로 구체적으로 요청 가능

---

## 8. 구축 방향

### 8-0. ★ 전제 — 이것은 이관 프로젝트다

**마일리지를 제외한 사실상 전 도메인이 현행 운영 데이터를 이관받는다.** 빈 DB로 출발하지 않는다.

| 도메인 | 이관 | 현행 규모 |
|---|---|---|
| 상담 (`COUNSEL_MASTER` · `COUNSEL_PROBLEM` · `COUNSEL_FAM`) | **O** | 5.4만 |
| 교수상담 (`CON_PROF_INFO` · `CO_ADVISER`) | **O** | 18.4만 |
| 비교과 (`EP_PRM` → `_STEP` → `_APP` → `_RESULT`) | **O** | 6.8만 |
| 상담사·배정 (`COM_CON_INF` · `COM_CON_TAR` · `FU_ASS_DEPT` · `TB_CARR_PROF_ASSI_DEPT`) | **O** | — |
| 채용·이력서 (`JOBSMASTER` 계열 · `SS_JOB_RES`) | **O** | 1.7만 |
| 역량 (`CA_SURVEY*` · `CA_GOAL*` · `TB_CARR_CAPA_STTS`) | **O** | 8.4만 |
| 공통코드·첨부·권한 (`SY_CODE` · `SY_FILE` · `SY_AUTH*`) | **O** | 3,874 등 |
| 진로설계 (`STU_COURSE_*`) | **O** | 1.4만 |
| **마일리지 (`EX_ITEM` · `_APP` · `_SCORE`)** | **X — 제외** | 1.1만 |
| 진단(C-2~C-CORE)·IAP·로드맵·성장일지·퀘스트 | 대상 없음 (신설) | — |

**여기서 나오는 제약 5가지**

1. **`INTG_UID` 키 결정은 되돌릴 수 없다.** surrogate로 바꾸면 24만 건 상담 이력의 재매핑이 필요하다.
2. **모든 코드에 현행 값(`legacy`)을 반드시 남긴다.** 이것이 곧 이관 매핑표다. `legacy`가 비면 그 코드는 이관 스크립트를 쓸 수 없다 — `SPEC.md` §7-0.
3. **신설 컬럼은 NULL 허용 + 화면 폴백을 전제로 설계한다.** NOT NULL로 잡으면 이관이 막힌다.
4. **구조 변경은 "이관 매핑을 쓸 수 있는가"를 통과해야 한다.** 1:1이 아닌 변환(예: `COUNSEL_MASTER` 1행 → 신청/결과 2행 분해)은 변환 규칙을 함께 문서화한다.
5. **현행 데이터의 오염을 이관 시점에 정제할지 미리 정한다** — 고아 테이블 `COM_ASS_DEPT` 잔여 배정, `CON_GB` 1/3 라벨 중복 등.

### 8-1. 데이터 원칙

1. **학사 유래 데이터는 읽기 전용으로 다룬다.** 학생 이름·학과·학년·학적상태를 우리 쪽에서 수정하는 UI/로직을 만들지 않는다.
2. **전 도메인 조인키는 `INTG_UID`.** 내부 surrogate를 새로 만들지 않는다 — 기존 상담 이력 24만 건이 이 키로 묶여 있다.
3. **학사 마스터를 자체 테이블로 복사하지 않는다.** 반드시 미러(`V_DEP_INF_ALL`)를 읽는다. `FU_CODE`(2020년 이후 갱신 0건)가 반례다.
4. **이벤트 레코드에 발생 시점 스냅샷을 함께 저장한다.** 현행 `EP_PRM_APP`이 이미 신청 시점 `HOFC_STA_CD`·`STU_SCHGR`·`EMAIL`·`HP`를 복사해 둔다.
5. **파생값은 원본에 넣지 않는다.** 매칭도·적합도·집계는 별도 구조로 분리 (현행 `JobPosting.match`가 반례).
6. **상태값은 코드+라벨로 분리한다** (`SY_CODE` 패턴). 한글 리터럴을 값 자체로 쓰지 않는다.
7. **FK가 없는 DB다.** 정합성은 애플리케이션이 전담한다는 전제로 방어 코드를 둔다.
8. **역할·권한은 `SY_AUTH` 체계를 계승한다.** 새 role 열거형을 만들지 말고 역할 코드를 추가한다. 학생은 권한 부여 대상이 아니라 `USER_TY_CD`로 판정한다.
9. **학사 유래와 자체 발급 인원을 같은 테이블에 섞지 않는다** (현행 `V_USR_INF`에 외부 상담사 INSERT가 반례).
10. **조회 화면은 학적상태 필터를 필수로 건다.** `V_USR_INF` 11.3만 행에 졸업생·퇴직자가 누적돼 있다.

### 8-2. 재사용 / 신설 판정

| 구분 | 대상 |
|---|---|
| **그대로 계승** | 사용자·조직(`V_USR_INF`/`V_DEP_INF_ALL`) · 상담사(`COM_CON_INF`) · 비교과 3단(`EP_PRM`→`_STEP`→`_APP`) · **비교과 상태값 10종 + 노쇼 벌점 2단계** · 채용공고(`JOBSMASTER` 계열) · 이력서·자소서(`SS_JOB_RES`) · 공통코드(`SY_CODE` 2단 계층) · 권한(`SY_AUTH` 4테이블 모델) · 첨부(`SY_FILE` `TAB_NM`+`TAB_SEQ`+`FILE_CD` 다형) · 동의(`SY_AGREE`) · 상담사↔단과대 매칭(`COM_CON_TAR`) |
| **구조 수정 후 계승** | 상담(`COUNSEL_MASTER` 신청/결과 분리 + `CONFIRMED` 상태 + 배정 이력 신설) · 상담 가능시간(`BASICSETTING` 슬롯 전개 → 규칙 기반. **교수 슬롯도 같은 테이블임에 주의**) · 조교 배정(`FU_ASS_DEPT` 승계, `COM_ASS_DEPT` 폐기) · 교수 배정(`TB_CARR_PROF_ASSI_DEPT`) → **조교·교수를 `사용자↔조직 배정` 단일 테이블(역할 컬럼)로 통합** · 진단(`CA_SURVEY` 4단 뼈대만 계승, 문항·역량축·대상자 규칙은 신설) · 연계이송(`LINK_*` 컬럼 기록 → 새 상담건 생성으로) |
| **신설 불가피** | IAP 6유형 · 경력개발 로드맵 · 스킬트리/직무적합도 · 성장경험일지 · 퀘스트·레벨·랭킹 · 학생×공고 매칭도 · **열람 감사로그** · **행 수준 접근 제어 통일** · **상태 변경 이력**(현행은 `SY_AUTH_LOG` 하나뿐, 상담·비교과·채용은 UPDATE 덮어쓰기) · 교수 상담 노출 제어(`CO_PROF`·`PC_CON_PROF_*` 전부 0행) |

**판정이 바뀐 것 (2026-07-31 소스 분석)**

| 항목 | 이전 판정 | 현재 판정 | 근거 |
|---|---|---|---|
| **진단 4종** | 신설 불가피 (`CHECK_*`가 상담에 종속) | **구조 수정 후 계승** | `CHECK_*` 8종은 **소스 참조 0건인 죽은 스키마**였다. 실제로는 `CA_SURVEY` 계통이 **학생 단독 응시를 이미 지원**한다(회차→대상자→응시→결과 4단 + 미실시 배지). 문항·역량축·판정·대상자 규칙만 신설 |
| **노쇼 벌점** | 신설 | **그대로 계승** | `EP_PRM_APP.STATUS`에 `9`=불참(벌점1점)·`10`=불참(벌점3점)이 이미 있고 블랙리스트 화면도 운영 중 |
| **조교 배정 테이블** | `COM_ASS_DEPT` / `FU_ASS_DEPT` 병기 | **`FU_ASS_DEPT`만 정본** | `COM_ASS_DEPT`는 **읽는 statement 0건인 쓰기 전용 고아**. `/Sa/Co/sys_assDept.do`는 읽기와 쓰기가 다른 테이블이라 **저장해도 아무 일이 안 일어난다**. 실동작은 `/Sa/Fu/sys_assDept.do` |
| **권한 부여 방식** | `SY_AUTH_USER` 52명이 전부 | **역할 기본권한은 코드가 자동 부여** | `PUtil.java:1002-1031`. 테이블에서 오는 것은 `AUTH0006`·`AUTH0007`뿐 |

### 8-3. 단계 계획

| 단계 | 작업 | 선행 | 검증 |
|---|---|---|---|
| 1 | 학과 트리를 `V_DEP_INF_ALL` 기반으로 구성 (`UNIV_CODE` 포함, `USE_YN` 필터, outer join 붕괴 회피) | 없음 | 대학원 학과가 목록에 나옴 |
| 2 | 사용자·조직 로더 구축 — `INTG_UID` 키, 학적상태 필터 | 1 | 조교 로그인 시 담당 학과 학생만 보임 |
| 3 | 상담·비교과 도메인 이관 (신청/결과 분리, 스냅샷 유지) | 2 | 학생 신청 → 상담사 접수함 왕복 |
| 4 | 진단·IAP·로드맵 신설 | 2 | 상담사 확정 → 학생 화면 반영 |
| 5 | **SSO 연동** (`V_USR_INF.LOGIN_ID` / `INTG_UID`) | 학교 협의 | 역할별 화면 진입 |
| 6 | **수강·성적 연계 활성화** → 스킬트리·직무적합도 | 전산원 요청 | 하드코딩 없이 렌더 |
| 7 | 관리자 확장 (포인트·통계·시스템 관리) · 감사·동의 | 3 | — |

> ⚠️ **6단계를 마지막에 두지 말 것.** 과목↔스킬 매핑은 개발이 아니라 **사람의 지식 입력**이라 리드타임이 가장 길다. 1단계와 병행해 "학과별 대표 과목 30개 × 스킬 10종"만 먼저 채워도 스킬트리가 실데이터로 돌기 시작한다.

---

## 9. 미결 — 확인 전까지 임의 확정 금지

### 답이 나온 것 <span>(2026-07-31 원격 소스 전수 분석)</span>

| 항목 | 답 |
|---|---|
| **역량 체계 3개 중 무엇인가** | **`EP_PRM`계다.** 화면 표시는 **5종**(지역형리더·창의적사고·실용적융복합·의사소통·글로벌)이고 **6번째 `인문`은 전 화면에서 주석 처리**되어 있다 — 스키마는 6, 화면은 5. 단 "역량"이라는 축이 3개 공존한다: 진단설문(`CA_SURVEY`) / 누적점수(`CA_GOAL`+`CA_GOAL_DATA`) / 마일리지 배분(`EX_ITEM` — 컬럼명은 아직 옛 체계). `SPEC.md` §7-9 |
| **상담 가능시간 "가능" vs "제한"** | **현행은 "가능"을 등록한다.** `BASICSETTING`(상담사가 슬롯 등록) ↔ `TB_CARR_CNSL_EXCL_HR`(관리자 제한구간)는 역할이 완전히 분리돼 있고, **학생 예약 화면은 `BASICSETTING`만 조회**한다. 제한 테이블은 "새 제한 등록 시 중복검사"에만 쓰인다.<br>→ **우리는 "제한(불가) 등록" 방식으로 간다** (사용자 확정). 기본값 전 시간대 가능 + 불가 시간만 등록. `BASICSETTING` 57만 행이 부푼 원인이 슬롯 전개다. `SPEC.md` §3-1-⑦·§3-5 |
| **진로설계(`STU_COURSE_*`)가 쓰이는가** | **쓰인다.** R12·U3. 학생 화면(`나의 진로설계서` STEP1~4)·관리자 집계·인재검색 탭 모두 살아 있다. 단 **INSERT statement가 0건** — 행 최초 생성 경로는 미확인 |
| **교수 상담이 실제로 동작하는가** | **동작한다.** `CON_PROF_INFO` 계통(18.4만). 2025-04에도 신규 화면이 추가됐다. `PC_CON_PROF_*`·`CO_PROF`는 미사용 |
| ~~**`CHECK_*` 8종의 정체**~~ | ~~**소스 참조 0건.** 죽은 스키마이거나 다른 시스템 소유~~ → **2026-08-05 정정, 아래 참조** |
| **`FU_CODE`의 정체** | **소스 참조 0건.** 조교학과 배정은 `V_DEP_INF`를 쓴다 |

### 정정된 것 <span>(2026-08-05 · L-1·L-2a·L-5 실사)</span>

| 항목 | 예전 판정 | 실측 | 근거 |
|---|---|---|---|
| **`CHECK_*` (11종)** | 죽은 스키마 | **살아 있다.** `CHECKHOLLAND`·`CHECKMMPI2` 최종변경 **2025-11-15**, `CHECK_MBTI` 2025-10-14. 11종 합계 **8,407행**. `COUNSELIDX`로 `COUNSEL_MASTER`에 붙는다 | `out/l-5.csv` · `out/l-1.csv` |
| **`COM_CPRT_MEBR`** | 기업 담당자 계정 | **가입한 기업회원 자체**(53컬럼·258행). 기업정보를 `COM_COMP_INF`와 중복 보유. `RTN_CONT`=반려사유 있음. `CAREER1~14`=기업 요구역량 14항목 | `out/l-1.csv` |
| **`COM_COMP_INF`** | 기업 마스터 | 맞다. 단 **104,128행은 가입 기업이 아니라 취업통계·추천용 기업 사전**이다. `COMP_PWD`(MD5 32자)를 따로 보유 — 자격증명이 두 군데 | `out/l-1.csv` |

> **★ 소스 참조 0건 ≠ 죽은 테이블.** `origin/`은 운영 소스의 **일부**(sqlmap 34 + 컨트롤러 4)다.
> `CHECK_*`가 그 조각에 안 나올 뿐 운영은 계속 쓰고 있었다. 앞으로 "소스에 없다"만으로 폐기 판정하지 않는다.

> **⚠ `BASICSETTING` 항목의 결정은 뒤집혔다.** 위 표의 "→ 우리는 제한(불가) 등록 방식으로 간다"는
> 2026-08-04 **승계 원칙**(로드맵·역량·게임화·진단 외에는 현행 구조 승계)으로 무효가 됐다.
> 현행 구조를 그대로 승계하고 `is_use`로 가능/불가를 표현한다 — 57만 행이 이 구조로 쌓여 있어 이관이 불가능해지기 때문.
> 행 수 감축은 이관 후 별도 과제. → `dc_counsel_slot`

### 남은 미결

| # | 항목 | 왜 막혀 있나 |
|---|---|---|
| 1 | **역량 `인문`(6번째)을 되살릴지** | 스키마·SQL은 6개를 계산하는데 화면만 5개다. 정책 결정 |
| 2 | **관리자 제한일정을 학생 예약 화면에 반영할지** | 현행은 반영 안 함(상담사가 슬롯을 지워야 함). 의도인지 미구현인지 현업 확인 |
| 3 | `V_DEP_INF`와 `V_DEP_INF_ALL`의 242개 차이가 정확히 무엇인가 | `docs/dbmeta/F_graduate_check.sql` F-3으로 확정 가능 |
| 4 | `FU_ASS_DEPT` 190건의 코드가 학사 코드 체계와 일치하는가 | `docs/dbmeta/H_dept_assign.sql` H-2로 확정 가능 |
| 5 | 대학원생 진단 정책 | C-2/C-3/C-4 학년 매핑이 학부 기준(1~4). 대학원은 1~3 |
| 6 | **대학원 수료생을 상담·비교과 대상에 포함할지** | 학적상태 `COMPLETED` 처리 정책 |
| 7 | **비교과 참가자격 판정 함수 `FN_GET_PRM_TRGT_YN()` 본문** | PL/SQL 안에 있어 소스로 확인 불가. 대학원생 신청 가능 여부의 최종 관문 |
| 8 | `STU_COURSE_INFO` 행의 최초 생성 경로 | INSERT 0건 / UPDATE 3건. 배치·트리거·외부 시스템 중 무엇인지 |
| 9 | **이관 시 현행 데이터를 정제할지** | `COM_ASS_DEPT` 잔여 배정, `CON_GB` 1/3 라벨 중복 |
| 10 | `SY_CODE` 그룹 `0012`·`0037`의 정체 | 현행 코드관리 화면이 하드코딩으로 숨긴다 |
| 11 | 지역청년 서브사이트를 같은 시스템에 둘지 | 별도 세션(`RegLoginSession`)·별도 회원 테이블·약 45화면. 사실상 두 번째 사이트 |
| 12 | **조교에게 학생 상세를 어디까지 공개할지** | 로드맵·진단결과 포함 여부. 행 수준 접근제어 정책 (`SPEC.md` §2) |
| 13 | **교수상담을 본인 학과만 허용할지 타 학과도 열지** | 현행은 본인 소속(`HAKBU_CD`) 교수만 조회된다. 대학원 요건(§7)과 함께 결정 |
| 14 | **비교과 운영자 역할을 신설할지** | 현행은 별도 역할(`Mn`, 30화면). 우리는 진로상담사가 겸하고 있다 (`SPEC.md` §3-7) |
| 15 | **`merge_V_DEP_INF`의 MERGE 본문** — 컬럼 목록이 고정인가, WHERE 필터가 있는가 | `origin/` 소스가 현재 로컬에 없어 미확인. 학과 반입 경로의 최종 관문 (§3-5 ①) |
| 16 | **학사DB → 드림캐치 노출 뷰의 정의** — 신설 학과가 뷰 단계에서 걸리는가 | 운영자가 전산원에 "V_ 테이블에 학과 추가"를 요청해 온 정황. 전산원 확인 필요 |
| 17 | **★ PK 없는 122개 테이블의 중복 판정 규칙** | 운영 275개 중 **122개(44%)** 에 PK가 없다. 신규 설계는 전 테이블 PK가 있어 **그대로 부으면 적재가 실패한다.** 테이블마다 "무엇이 같으면 같은 행인가"를 정해야 하고 DB가 답을 주지 않는다 — 운영 담당자 확인 사항. 행 많은 순: `IC2_JOB_PRM_APP`(128,233) · `STU_COURSE_INFO_DTL`(70,570) · `IC2_PA_GOAL_DATA`(58,152) · `STU_CON_POINT`(29,328) |
| 18 | **`SY_FILE.TAB_SEQ` 다형 참조를 어떻게 승계할지** | 뷰 DDL에서 같은 컬럼이 `EP_PRM.PRM_SEQ`와 `EP_CUR_GUBUN.CODE` 양쪽에 붙는다. `dc_file`이 이 패턴을 그대로 받을지, (테이블코드 + 행ID) 쌍으로 명시할지 설계 결정 |
| 19 | **`CHECK_*` 11종(심리검사 결과)을 어떻게 승계할지** | 검사 종류마다 전용 테이블 + 전용 컬럼(홀랜드는 `CODER`~`CODEC` 6코드, MBTI는 다름). 신규 설계는 `dc_group_counsel`에 흡수했는데 **검사별 결과 구조를 담을 자리가 없다.** 공통 결과 테이블 + 검사별 항목 행으로 펼지, 검사별 테이블을 유지할지 → **⚠️ #23이 먼저다.** 심리검사 존치 자체가 미정이고 **홀랜드는 쓰지 않기로 확정**됐다. 설계 착수 금지 |
| 20 | **`EP_PRM` 외 넓은 테이블의 죽은 컬럼** | K-2를 `COUNSEL_MASTER`(46컬럼) · `JOBSMASTER`(82) · `COM_CPRT_MEBR`(53)에도 돌릴지. EP_PRM은 125개 중 27개가 값 0건이었다 |
| 21 | **반려사유를 필수로 만들지** | 현행은 반려 상태(`REQ_STAT_GB='R'`, `JOBSMASTER.ISAUTH`)는 있는데 사유를 **한 번도 안 남겼다**(`REQ_RTN_CONT` 0건). 신규에서 필수화할지 정책 결정 |
| 22 | **연계상담(`dc_counsel_link`)에서 교수가 당사자인가** | 현행 `CO_CONNETC_INFO`는 `PROF_ID`·`CON_ID` 두 당사자를 컬럼으로 들고 있는데 `V_role.json`에는 상담사·관리자만 있다. 그런데 `docs/_analysis/`에 이 테이블을 쓰는 화면 기록이 **한 건도 없다**(참조 0건) — 교수가 연계를 신청하는 화면이 있는데 우리가 받은 소스 조각에 안 들어온 것인지, 기능 자체가 안 쓰이는 것인지 미확인. `SELECT COUNT(*), MAX(UPDDATE) FROM CO_CONNETC_INFO` 로 먼저 가른다 |
| 23 | **★ 진단 스펙 전체 미확정 — 사용자가 나중에 확정해 전달** <span>(2026-08-06)</span> | 검사 **종류** · 검사별 **유형 결과** · **검사식(판정 로직)** 셋 다 미정. 확정된 것은 둘뿐 — ① **홀랜드는 쓰지 않는다** ② **심리검사 자체가 없어질 수 있다.** `C2`·`C3`·`C4`·`CCORE`(`SPEC.md` §7-4)는 **잠정 명칭이며 바뀔 수 있다.** → **E 도메인 5테이블(`dc_diag_*`)·`dc_iap`·#19를 확정 전까지 구현·이관설계하지 말 것.** 코드값을 화면·JSON에 박으면 재작업이 된다. 명칭 변경 시 손댈 곳: `SPEC.md` §7-4(단일소스) · `DB.md` 441·550줄 · `src_admin/data/*.seed.json` |

---

## 10. 근거 파일

| 파일 | 내용 |
|---|---|
| `docs/dbmeta/out/D1_tables.csv` | 테이블 428개 + 코멘트 + 행수 + 통계일 |
| `docs/dbmeta/out/E2_tables.csv` | 학사 적재 테이블 컬럼 명세 |
| `docs/dbmeta/out/E3_tables.csv` | 핵심 서비스 테이블 컬럼 명세 (905행) |
| `docs/dbmeta/out/E4_tables.csv` | `SY_CODE` 전체 3,874행 |
| `docs/dbmeta/out/E5_tables.csv` | 테이블별 INSERT/UPDATE/DELETE 실적 |
| `docs/dbmeta/out/E7_tables.csv` | 제약조건 (PK/UNIQUE/FK) |
| `docs/dbmeta/out/E8_tables.csv` | 실제 뷰 14개 |
| **`docs/dbmeta/out/l-1.csv`** | **운영 288객체 전체 컬럼 4,772행** — 컬럼 미확보 0 |
| **`docs/dbmeta/out/l-2a.csv`** | **뷰 14개 DDL 전문** — 조인 조건 27건의 출처 |
| `docs/dbmeta/out/l-3.csv` · `l-4.csv` · `l-5.csv` | 인덱스 · 반복 컬럼명 · 전체 활동 실적 |
| **`docs/dbmeta/out/k-2수정.csv`** | **`EP_PRM` 125컬럼 실사용** — 27개가 값 0건 |
| `docs/dbmeta/out/F2_dept_tree.csv` | 조직 트리 1,921행 |
| `docs/dbmeta/out/I2.csv` · `I3.csv` | 역할×메뉴 908행 · 역할 마스터 13건 |
| `origin/sqlmap/*.xml` | MyBatis sqlmap 34개 · statement 2,033개 |
| `origin/co/*.java` | 상담 도메인 컨트롤러 4개 |

### `docs/_analysis/` — 현행 소스 전수 분석 <span>(2026-07-31, 원격 수행)</span>

소스가 원격 PC에만 있어 `docs/REMOTE_ANALYSIS_PROMPT.md` 지시서로 그쪽에서 분석하고 산출물만 회수했다. **DB 미접속·소스 무수정·정적 분석 전용.**

| 파일 | 내용 | 범위 |
|---|---|---|
| `00_overview.md` | 스택·진입점·세션 구조·로그인 경로 11종 | 전수 |
| `01_sitemap.md` | 사이트맵 — 매핑 1,600 / 고유 URL 1,596 / JSP 화면 847 | **전수** |
| `02_screen_dataflow.md` | 화면별 데이터 흐름 ①~⑦ | 우선순위 1~3의 **22화면** |
| `03_access_scope.md` | 역할별 행 수준 접근 범위 + 판정(🟢🟡🔴⚪) | 전수 스캔 |
| `04_table_usage.md` | 테이블 사용처 R/C/U/D | 335 테이블 |
| `05_answers.md` | 지정 질문 15개 답변 | 전부 응답 |
| `06_findings.md` | **취약점 21 · 버그 17 · 죽은 기능 · 성능 위험** | ⚠️ **외부 공유 금지** |
| `99_progress.md` | 수치 정합성 + **다루지 못한 영역 자진 신고** | |

**미커버 영역** — 비교과 운영자(107매핑)·비교과 활동(78)·채용(145)·취업통계(61)·포트폴리오(40)·참여후기(54)는 사이트맵에는 있으나 데이터흐름 상세가 없다.
**원리적 한계** — 역할↔화면 매핑의 실체(`SY_MENU_AUTH` 972행)와 메뉴 노출 여부(`SY_MENU.USE_YN`)는 **DB 데이터라 소스로 확정 불가**. 사이트맵의 역할 컬럼은 URL 네임스페이스 규약 기반 **추정**이다.

**스택 요약** (`00_overview.md`)

| | |
|---|---|
| Java 1.7 · Spring MVC 3.x · MyBatis 3 · JSP+JSTL · Oracle | Maven/Gradle 없음 (Eclipse WTP) |
| Controller 89개 · JSP 1,246개 · sqlmap 34 / statement 2,033 | 단일 클래스 최대 305KB(`EpMnController`) |
| 인증 프레임워크 없음 (자체 세션 `CAREER_SESSION_KEY`) | 인터셉터·보안 필터 없음 |
| 일일 배치 `DairyBatch`(학사 동기화·SMS·역량집계) | **`web.xml`에서 주석 처리되어 미기동** — 별도 스케줄러 존재 여부 미확인 |

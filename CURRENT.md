# CURRENT.md — 현행 드림캐치 <span>(운영 중인 시스템의 사실만)</span>

> **이 문서가 소유하는 것** — 현행 운영 시스템의 ①사이트맵 ②기능 ③권한 ④DB구조.
> **사실만 담는다.** 우리가 무엇을 만들지·왜 그렇게 설계할지는 쓰지 않는다.
>
> | 찾는 것 | 문서 |
> |---|---|
> | 우리가 만들 화면·필드·코드체계 | `SPEC.md` |
> | 진단→상담→로드맵 프로세스 최종안 | `PROCESS.md` |
> | 우리 DB 설계·이관 전제·미결 대장 | `DB.md` |
>
> **근거** — `docs/_analysis/`(소스 전수분석 8파일) · `docs/DB_CURRENT.html` · `docs/DB_LEGACY_ERD.html` · `docs/dbmeta/out/*.csv`
> 조사: 운영DB 직접 조회(DBeaver, 읽기전용) 2026-07-29~30 + 운영소스 정적분석 2026-07-31
> ⚠️ `docs/_analysis/06_findings.md`는 **취약점 21건** — 외부 공유 금지.
>
> 표기: **확정** = FK·뷰정의·소스SQL로 증명 / **추정** = 정황 근거 / **미확인** = 사람 확인 필요

---

## 0. 한눈에

| 항목 | 값 |
|---|---|
| 스택 | Java 1.7 · Spring MVC 3.x · MyBatis 3 · JSP+JSTL · Oracle |
| 빌드 | Maven/Gradle 없음 (Eclipse WTP) |
| 규모 | Controller 89 · JSP 1,246 · sqlmap 34 / statement 2,033 |
| URL 매핑 | 1,600 (고유 1,596) — 그중 **JSP 화면 847** |
| DB | 테이블 **428개** (실사용 100~150) · 소스 참조 270 · **FK 3건** |
| 인증 | 프레임워크 없음. 자체 세션 `CAREER_SESSION_KEY` · 인터셉터·보안필터 없음 |
| 배치 | `DairyBatch`(학사동기화·SMS·역량집계) — **`web.xml`에서 주석 처리, 미기동** |

**이 시스템의 정체 — 다대학 납품 패키지의 이식본이다.**

- `SY_CODE`에 전국 대학·전문대·대학원 **1,900여 개**가 코드로 등록(그룹 `0070`/`0069`/`0071`)
- `EP_PRM` 컬럼 코멘트에 `[삭제]참가대상-학생-캠퍼스-승학/구덕/부민` — **동아대 캠퍼스명** 잔존
- 0행 미사용 테이블 100여 개(`EP_ACT_*` 20 · `EC_CAN_*` 6 · `ST_CAR_STAT_*` 10 · `PLC_*` · `PC_*`)

→ 창원대가 실제로 쓰는 기능은 일부다. **이관 범위는 428개가 아니라 100~150개.**

---

## 1. 사이트맵

### 1-1. URL 1,600건의 구성

| 구분 | 수 | 성격 |
|---|---|---|
| **JSP 렌더링 화면** | **847** (고유 844) | 사이트맵의 대상 |
| Ajax 응답(`jsonView`) | 480 | 화면 아님 — 목록·검증·저장 API |
| 처리 후 `redirect` | 133 | 화면 아님 — 등록·수정·삭제 액션 |
| 엑셀·파일 다운로드 등 | 140 | 대부분 `*ExcelController` |

### 1-2. 역할별 화면 수

| 역할 | 화면 | URL 네임스페이스 |
|---|---|---|
| **관리자** | **315** | `/Sa/*` · `/admin/*` |
| **학생** | 약 200 | `/user/**/Ms*` · `/user/My/*` · `/user/portfolio/*` |
| 비교과 운영자 | 약 60 | `/user/Ep/Mn*` · `/user/Ex/ExMn*` |
| 지역청년 (별도 서브사이트) | 약 45 | `/reg*` · `/user/In/*` · `/user/It/*` |
| **교수** | 34 | `/user/Pc/PcM[ap]*` · `/user/Ca/CaMp*` · `/user/Co/CoMp*` |
| **상담사** | 33 | `/user/Co/CoMc*` · `/user/Fu/CoSimri*` |
| 직원(교무·학사) | 24 | `/User/Wa/*` · `/user/Co/CoEm*` · `/User/St/*` |
| 기업 | 13 | `/user/Cp/*` · `/user/infaco/*` |
| **조교** | **7** | `/user/Co/CoAs*` |
| 멘토 | 6 | `/user/Mt/MtMm*` |

> ⚠️ **역할 컬럼은 "기획상 이 역할용 화면"이라는 뜻이지 "이 역할만 접근 가능"이 아니다.**
> 실제 노출은 `SY_MENU_AUTH`(DB 972행) 판정이고, URL은 메뉴에 없어도 살아 있다.
> 역할↔화면 매핑의 실체는 DB 데이터라 **소스로 확정 불가 — 위 표는 네임스페이스 기반 추정.**

### 1-3. 관리자 메뉴 트리 <span>(315화면)</span>

| 영역 | 경로 | 주요 화면 |
|---|---|---|
| 시스템관리 | `/Sa/Sy` | 메뉴·권한그룹·메뉴권한·게시판·배너·**코드관리** · SMS이력 · 접속이력·통계 · 권한이력 |
| 상담관리 | `/Sa/Co` | 상담사관리 · 상담현황(5종) · 상담결과 · 일정 · 통계 · **조교학과배정** · **전담교수매칭** · **상담제한일정** · 직원상담 · 집단상담 |
| 비교과 | `/Sa/Ep` | 개설신청현황 · 프로그램조회(목록·달력·월력) · 구분관리 · 만족도조사 · 개인형 6탭 · 그룹형 4탭 · **블랙리스트** |
| 채용·해외취업 | `/Sa/Re` | 8종 카테고리 각 `L/I/D/U` 세트 + 신청자관리 + 인재검색 팝업 12종 |
| 취업통계 | `/Sa/St` | 조사차수 · 예비/본조사 · 취업자현황 · 통계 12종 엑셀 · KEDI · 학과목표 |
| 역량 | `/Sa/Ca` | 학생별 역량현황 · **학과별 PA역량목표** · 핵심역량 시뮬레이션 · **역량진단관리** |
| 데이터분석 | `/Sa/Dt` | 스마트·지원체계·프로그램·상담·마일리지구간·마일리지순위 6종 |
| 마일리지 | `/Sa/Ex` | 항목관리 · 승인관리 · 자격증 · 마일리지 조회 |
| 진로목표·워크넷 | `/Sa/Fu` | 워크넷 검사결과 · 진로탐색·진로목표 현황 · **전담조교 매칭** · **교수학과 배정** |
| 기타 | | 취업동아리 `/Sa/Ec` · 멘토 `/Sa/Mt` · 기업 `/Sa/Cp`·`/Sa/Et` · 교수상담 `/Sa/Pc` · 진로로드맵 `/Sa/Rm` · 진로취업카드 `/Sa/Ss` · 게시판 `/Sa/Bd` · 장기결석·학사경고 `/Sa/Wa` · 포트폴리오 `/Sa/Pf` |

### 1-4. 사용자 메뉴 트리

| 역할 | 화면 |
|---|---|
| **상담사** | 예약상담(`CoMc010L`) → 상세팝업·변경정보·상담작성·심리검사작성·**재배정**(`CoMcReAssing`) · 일정관리(월별) · 상담결과관리 · **집단상담**(`CoMc050L` 일반 / `CoMc060L` 심리검사) · 통계 4종 · 심리검사 결과 작성·조회 |
| **조교** <span>(전 7화면)</span> | 전담교수 배정 유무 확인(`CoAs010L`)→배정팝업 `Advis` · 전담교수 상담실적(`CoAs020L`→`020DL`→엑셀) · **학생별 역량현황**(`CoAs030L`·`CoAs040L`) |
| **교수** | 지도교수 상담(`PcMp010L`/`PcMa010L`) · 신규작성 · 집단상담 · 인쇄 · 상담내역 · **온라인 상담**(`PcMpOnlineL`) · 일정 3종 · 학생별 역량현황 · 역량진단 · 학생별 진로목표 · 푸시전송 |
| **학생** | 마이홈 · 상담·검사 내역(`MySt020L~070L`) · 진로설계서 · 상담신청(`CoVi010M`→`CoViConL`→`CoAppD`) · 지도교수 상담(온·오프 2트랙) · 비교과 · 마일리지 · 역량진단 · **e포트폴리오 18화면** · 진로취업카드 · 채용 16화면 |
| 기업·멘토·지역청년 | 기업정보수정 · 멘티현황·활동관리 · 지역청년(`RegLoginSession` 별도 세션) |

> **팝업 계열 `pop_*` 약 260개**는 구조상 메뉴에 없고 부모 화면에서 `window.open`으로만 열린다.

### 1-5. 같은 화면을 파라미터로 역할 분기한다

```
/user/Co/CoMc070L.do?TYPE=T   상담사        → 포트폴리오 관리
/user/Co/CoMc070L.do?TYPE=A   조교          → 학생현황
/user/Co/CoMc070L.do?TYPE=M   프로그램담당자 → 포트폴리오 관리
```

---

## 2. 기능

### 2-1. 도메인별 SQL 규모 <span>(sqlmap)</span>

| 파일 | 네임스페이스 | statement | 도메인 |
|---|---|---|---|
| `ep.xml` | Ep | 370 | 비교과 프로그램 |
| `co.xml` | Co | 323 | **상담** |
| `epAct.xml` | EpAct | 146 | 비교과 활동내역(예산·출장·회의록) |
| `re.xml` | Re | 143 | 채용 |
| `st.xml` | St | 120 | 취업통계 |
| `sy.xml` | Sy | 93 | 시스템(메뉴·권한·코드) |
| `common.xml` | Common | 90 | 공통(학사 연계 포함) |
| `ca.xml` | Ca | 86 | 역량 |
| `ex.xml` | Ex | 82 | 비교과 마일리지 |
| 그 외 25개 | | 580 | 기업·심리검사·포트폴리오·설문 등 |

### 2-2. 도메인별 기능 요약

| 도메인 | 하는 일 | 핵심 테이블 | 규모 |
|---|---|---|---|
| **상담(상담사)** | 신청 → 슬롯확정 → 상담 → 결과작성. 재배정·취소·진로↔심리 연계이송. 신청과 결과가 **한 행**에 들어간다 | `COUNSEL_MASTER`(46컬럼) · `COUNSEL_PROBLEM` · `COUNSEL_FAM` | 5.4만 |
| **상담 가능시간** | 상담사가 **가능 슬롯을 등록**. 일자×시×분으로 행 전개. 관리자 제한구간은 별 테이블이며 학생 예약 화면은 이걸 안 본다 | `BASICSETTING` / `TB_CARR_CNSL_EXCL_HR` | **57만** |
| **상담사 관리** | 내외부 구분 · 상담분야 3 · 방식 3 · 요일 5 · 장소 · 온라인 제한수 · 담당 단과대 · **자체 비밀번호** | `COM_CON_INF`(51컬럼, 26명) · `COM_CON_TAR` | — |
| **교수상담** | **이 시스템에서 가장 많이 쓰이는 기능.** 온·오프라인 2트랙. 상담구분 6종(`SY_CODE` 0131) | `CON_PROF_INFO` · `CO_ADVISER`(지도교수 배정) | **18.4만** / 1.4만 |
| **심리검사** | 외부 검사도구 결과를 상담사가 입력. **문항·채점 로직이 시스템에 없다.** 상담 신청 절차 안에서만 이뤄진다 | `CHECK_*` 11종 (`COUNSELIDX`로 상담건에 붙음) | 8,407 |
| **비교과** | 개설신청→승인→운영→신청→선발→출석→수료. 개인형/그룹형 2종, 차수 운영, 노쇼 벌점 2단계, 블랙리스트, 만족도조사, 참여후기 | `EP_PRM`(125컬럼) → `EP_PRM_STEP` → `EP_PRM_APP` → `EP_PRM_RESULT` | 807 / 6.8만 / 1.4만 |
| **마일리지** | 항목 등록 → 학생 신청 → 승인 → 점수. 항목별 역량 반영비율 보유 | `EX_ITEM` · `EX_ITEM_APP` · `EX_ITEM_SCORE` | 1.1만 |
| **역량** | 회차 개설→대상자 지정→응시→결과 4단. 교과(`SUB_PA_n`)+비교과(`EXT_PA_n`) 누적 달성률, 학과 목표 | `CA_SURVEY*` · `CA_GOAL` · `CA_GOAL_DATA` · `TB_CARR_CAPA_STTS` | 8.4만 |
| **채용** | 8종 카테고리 공고 · 지원 · 인재검색 · 기업 요구역량 | `JOBSMASTER`(82컬럼) · `APPLICATIONMASTER` · `JOBSOCCUPATION` · `JOBSAREA` | 1.7만 / 3,812 |
| **이력서·자소서** | 학생 작성 → 상담사 첨삭. `STATUS`(미신청/신청/완료)·`CON_ID`·`CONCONT`·`REQCONT`·`RE_DATE`로 **워크플로 완비** | `SS_JOB_RES`(34컬럼) · `JOB_RES*` · `JOB_SELF_INT` | — |
| **진로설계** | 나의 진로설계서 STEP1~4 · 목표기업 · 자격·어학·활동 | `STU_COURSE_INFO`(+`_DTL`) · `_CAREER_LICN`/`_LANG`/`_ACTIVE` · `ST_GOAL` | 1.4만 / 7만 |
| **직무 분류** | 3계층 직무 사전 · 희망직무 | `FU_JOB_MSTR` · `FU_PURPOSE_ADD_JOB` | 865 / 3만 |
| **시스템** | 코드·메뉴·권한·첨부·동의 | `SY_CODE`(3,874) · `SY_MENU`(343) · `SY_AUTH`(13) · `SY_FILE`(4.2만) · `SY_AGREE` | — |
| **로그** | 메뉴·접속·로그인 이력 | `SY_MENU_LOG`(647만) · `SY_SYSTEM_CONN_INFO`(449만) · `SY_LOGIN_LOG`(94만) · `SC_LOG_YYYYMM` 76개 | — |

### 2-3. 상태값 체계 <span>(현행 실측값)</span>

**상담** `COUNSEL_MASTER.COUNSELSTATUSIDX` — `1` 신청 / `2` 완료 / `3` 학생취소 / `4` 상담사취소
→ **배정 대기 상태가 없다.** 학생이 슬롯을 고르는 순간 상담사가 확정된다.
→ 재배정은 `CONSULTID`만 바꾸고 **상태도 이력도 남지 않는다.** 취소도 `CANCELREASON` 3컬럼에 마지막 1건만 덮어쓴다.

**상담 유형** `COUNSELTYPEIDX` — `1` 진로 / `2` 취업 / `3` 심리 (교수상담은 별 테이블)

**교수상담** `CON_PROF_INFO` — 상태 `0001` 신청 / `0002` 완료 / `0003` 학생취소 · 방식 `CON_KIND` = `ON`/`OF` · 세부구분 `CON_TYPE`(`SY_CODE` 0131 6종) · 예약여부 `ISRESERVATION`

**비교과 신청** `EP_PRM_APP.STATUS` — 단계별로 쓸 수 있는 값이 갈린다(`APP_GB`=A 신청자관리 / C 선발자관리)

| 값 | 의미 | 단계 |
|---|---|---|
| `1` | 신청(접수대기) | 신청자관리 |
| `7` | 접수완료(대기) | 신청자관리 |
| `2` | 선발 | 양쪽 |
| `3` / `4` | 탈락 / 취소 | 신청자관리 |
| `8` | 참석 | 선발자관리 |
| **`9` / `10`** | **불참 (벌점 1점 / 3점)** | 선발자관리 |
| `5` / `6` | 수료 / 미수료 | 선발자관리 |
| `9999` | 삭제 | 양쪽 |

집계 정의: 신청자수 = `≠취소` · 수료자수 = `수료`.
⚠️ 그룹형 수료 판정이 두 벌이다 — `EP_PRM_MAPPING.STATUS='2'` vs `EP_PRM_APP.STATUS='5'`.

**비교과 개설** `EP_PRM.REQ_STAT_GB` — `T` 임시저장 / `A` 신청 / `R` 반려 / `S` 승인
⚠️ 반려 사유(`REQ_RTN_CONT`)를 **한 번도 남긴 적이 없다**(0건).

**연계이송** `LINK_YN`/`LINK_TYPE`/`LINK_RCT_YN`/`LINK_RCT_TYPE` — `0001` 미결정 / `0002` 교차(진로↔취업) / `0003` 타부서 / `0004` 지도교수 / `0005` 기타
→ **연계해도 새 상담건이 생기지 않고 컬럼만 기록된다.** 접수 여부는 상담사가 수기 갱신.

**역량 6종** `PA_1`~`PA_6` = 지역형리더 · 창의적사고 · 실용적융복합 · 의사소통 · 글로벌 · 인문
→ **스키마는 6, 화면은 5.** `PA_6`(인문)은 전 화면에서 주석 처리돼 있다.
→ 하위역량 **17종**(`CAP_QUS_GB2`)이 별도로 존재한다:

```
인성·소양 · 비전제시 및 실행능력 · 공동체 윤리의식 · 도전정신
분석적·비판적 사고력 · 추론적·대안적 사고력 · 문제해결력
전공지식활용능력 · 통합적 사고력 · 가치창출능력
토론과 조정력 · 의사표현 및 전달능력 · 경청과 이해능력
다문화 이해 및 수용능력 · 외국어 구사능력 · 국제적 교류 및 협업능력 · 세계시민의식
```

→ "역량" 축이 **3개 공존**한다: 진단설문(`CA_SURVEY`) / 누적점수(`CA_GOAL*`) / 마일리지 배분(`EX_ITEM` — 컬럼명은 아직 옛 체계 `job_rate`·`fusion_rate`).

### 2-4. 데이터는 있는데 화면이 없거나 그 반대인 것

| 도메인 | 현행 데이터 | 비고 |
|---|---|---|
| 채용 지원 `APPLICATIONMASTER` | 1.7만 | 운영 중 |
| 진로설계 `STU_COURSE_*` | 1.4만 | **INSERT statement 0건** — 행 최초 생성 경로 미확인 |
| `CO_PROF` · `PC_CON_PROF_*` (교수 상담노출 제어) | **0행** | 화면은 있고 데이터가 없다 |
| `COM_ASS_DEPT` (조교 배정) | 쓰기만 | **읽는 statement 0건인 고아.** `/Sa/Co/sys_assDept.do`는 저장해도 아무 일이 안 일어난다 |
| `FU_CODE` | — | 소스 참조 1곳. 학과 목록의 출처가 아니다 |

---

## 3. 권한

### 3-1. 4테이블 RBAC 모델

```
SY_AUTH        역할 마스터    13건   AUTH_CODE(PK)·AUTHNM·BASEGRUP_YN·USE_YN
   ↓ AUTH_CODE
SY_AUTH_USER   사람 ↔ 역할    52명   AUTH_USER = INTG_UID
SY_MENU_AUTH   역할 ↔ 메뉴   972건
   ↓ MENU_CODE
SY_MENU        메뉴 트리     343건   MENU_CODE(PK)·MENU_URL·MENU_LEVL·PRTCODE·USER_DVID
SY_AUTH_LOG    권한 변경 이력 278건
```

### 3-2. 역할 13개 실제 현황

| `AUTH_CODE` | 역할명 | 기본그룹 | 배정 | 메뉴수 | 비고 |
|---|---|---|---|---|---|
| `AUTH0006` | 슈퍼관리자 | N | **28** | 238 | 실질 관리자 |
| `AUTH0012` | **상담사** | N | **19** | **14** | **실제 운영 상담사** |
| `AUTH0005` | 상담사 | Y | 1 | **183** | 상담센터 총괄급 (회원·시스템 관리 포함) |
| `AUTH0003` | 교원 | Y | 1 | 95 | |
| `AUTH0002` | 조교 | Y | 2 | 91 | 실제 전용 화면은 5개 |
| `AUTH0011` | 교무과 학사관리과 | N | 1 | 7 | 장기결석·학사경고 |
| `AUTH0009` | 창대한담당자(온라인멘토링) | N | 1 | 14 | |
| `AUTH0001` | 학생 | Y | **0** | 116 | |
| `AUTH0000` | 비로그인 | Y | 0 | 76 | |
| `AUTH0004` | 직원 | Y | 0 | 78 | 설명란에 사람 이름 |
| `AUTH0008` | 기업 | N | 0 | 50 | `TB_CARR_USER` 자체 인증 |
| `AUTH0007` | 창대한담당자 | N | 0 | 0 | 껍데기 |
| `AUTH0010` | 멘토 | N | 0 | 16 | `USE_YN='N'` 폐기 |

> **상담사 역할이 두 개다.** `AUTH0012`(19명·14메뉴)가 실무, `AUTH0005`(1명·183메뉴)가 총괄.

### 3-3. 권한 부여가 두 갈래다 <span>(확정 — `PUtil.java:1002-1031`)</span>

```
자동 부여    학생 · 조교 · 교수 · 직원 · 상담사 · 기업 · 멘토
  → 세션의 USER_TY_CD 1글자를 보고 자바 코드가 권한 리스트에 덧붙인다.
  → SY_AUTH.BASEGRUP_YN='Y' 가 이 뜻이다. 테이블에 등록하지 않는다.

명시 부여    슈퍼관리자(AUTH0006) · AUTH0007
  → SY_AUTH_USER 에 등록해야 들어온다.
```

→ **`SY_AUTH_USER` 52명은 전체 권한자 명단이 아니라 "추가권한 보유자" 명단이다.**
→ 그래서 "학생 0명 / 조교 2명"은 인원수가 아니다.

**`USER_TY_CD` 코드값** <span>(`common.xml:280-288` 주석에서 확정)</span>

| 코드 | 의미 | 세션 1글자 |
|---|---|---|
| `1101` | 재학생-학부 | `S` |
| `1102` | 졸업생-학부 | `S` |
| **`1201`** | **재학생-대학원** | `S` |
| **`1202`** | **졸업생-대학원** | `S` |
| `1301` | 교원 | `P` |
| `1401` | 직원 | `M` |
| `1501` | 조교 | `A` |

### 3-4. 인가 실행부의 실태 <span>(모델은 표준인데 강제하지 않는다)</span>

| 실태 | 근거 |
|---|---|
| 인증·인가 **필터/인터셉터가 없다.** 컨트롤러 `@ModelAttribute` 보일러플레이트가 대신한다 | `web.xml` 필터는 인코딩 하나. `<mvc:interceptors>` 선언 없음 |
| 인가 검사가 **`REFERER` 헤더가 없을 때만** 동작한다 → 정상 내부 이동에서는 검사 자체를 하지 않는다 | `CareerActionController.java:191-193` |
| 검사에 걸려도 결과는 `RETOK='N'` **플래그**일 뿐, 차단은 각 컨트롤러가 `sendRedirect`로. `return` 직후라 **핸들러와 SQL은 그대로 실행된다** | 화면엔 안 보이나 조회는 일어난다 |
| **RETOK 검사가 아예 없는 컨트롤러 29개.** 그중 엑셀 다운로드 14종(엔드포인트 130개)은 기반 클래스를 상속조차 하지 않는다 | 전체 1,600 매핑 중 약 295건(18%)이 미검사 |

> **통제 범위는 "메뉴"까지다.** `SY_MENU_AUTH`는 어떤 메뉴를 볼 수 있나만 통제한다.
> **데이터 열람 감사는 없다.** `SY_AUTH_LOG`는 권한 변경만 기록한다.
> 2025-03-01에 권한 체계를 개편한 흔적(`SY_AUTH_20250301` 등 스냅샷) — 권한 구조를 건드릴 때 백업을 뜨는 관행이 있다.

### 3-5. 데이터 접근 범위 <span>(행 수준) — 통일되어 있지 않다</span>

| 역할 | 범위 | 결정 테이블 | 통일된 규칙인가 |
|---|---|---|---|
| **관리자** | **전체 (제한 조건 자체가 없음)** | — | 예 — 일관되게 무제한. **단과대·학과 단위 관리자 개념이 없다** |
| **상담사** | ① 본인 상담건 `COUNSEL_MASTER.CONSULTID=세션ID`<br>② 학생 노출 범위 **담당 단과대** `COM_CON_TAR.DAEHAK_CD` | `COUNSEL_MASTER` · `COM_CON_TAR` | **아니오** — 학생측 슬롯 필터가 `ADMIN_YN` 파라미터로 우회 가능 |
| **교수** | ① 본인 상담건 `CON_PROF_INFO.PROF_ID`<br>② 학과 `V_USR_INF.ORGID`/`HAKBU_CD` **OR** `TB_CARR_PROF_ASSI_DEPT`<br>③ 지도학생 `V_USR_INF.PROF_ID` | 3개 테이블 | **아니오** — 소속·배정·지도 3축이 화면마다 다르게 조합 |
| **조교** | 배정 학과 `FU_ASS_DEPT.USR_ID=세션ID` → `MAJOR_CD` 또는 `DEPT_CD` | `FU_ASS_DEPT` | **아니오** — 화면 4개 중 1개(`CoAs020L`)가 파라미터를 신뢰 |
| **학생** | 본인 건만 (`USERID`/`INTG_UID`/`USRID` = 세션ID) | 도메인별 | 예 (대체로 일관) |
| 기업 | 본인 등록 공고 (`SESSION_USER_TY_CD='C'` 분기) | `RE_REC_INFO` · `COM_CPRT_MEBR` | 부분적 |
| 멘토 | 본인 멘티 `TB_MENTEE_INFO.MENTO_ID` | | 예 |
| 비교과 담당자 | 본인 등록 프로그램 `EP_PRM.REGID` (단 `ADMIN_YN='Y'`면 해제) | `EP_PRM` | 예 |
| 직원 | 제한 규칙이 사실상 없음(전체) | — | — |

**범위 판정 방식** — 세션 기반(안전)과 **요청 파라미터 기반(위조 가능)이 혼재**한다. SQL의 `<if>` 안에 흩어져 있다.

| 층위 | 실태 |
|---|---|
| URL 접근 제어 | 인터셉터·필터 **없음** (§3-4) |
| 화면 노출 제어 | `SY_MENU_AUTH` 기반 메뉴 트리만. 메뉴에 없어도 URL은 살아 있다 |
| **데이터 범위 제어** | **SQL `<if>` 안에 분산.** 통일 규칙 없음 |

> 파라미터 신뢰 지점 예시 — 조교 상담실적(`HAKBU_CD` 변조) · 학생 예약슬롯(`ADMIN_YN=Y`로 단과대 제한 해제) · 비교과 목록(`ADMIN_YN=Y`로 전 학부 노출). 상세 판정표는 `docs/_analysis/03_access_scope.md`.

---

## 4. DB 구조

### 4-1. 개요

| 항목 | 값 |
|---|---|
| DBMS / DB / 스키마 | Oracle · `dream` / `DREAMCATCH` |
| 테이블 | **428개** — 실사용 100~150, 소스 참조 270, 소스에서 아예 안 쓰임 158 |
| SQL statement | 2,033 (SELECT 1,352 / INSERT 289 / UPDATE 265 / DELETE 220) |
| 쓰기 있는 테이블 / 읽기 전용 | 219 / 51 |
| 제약조건 | 157건 중 **FK는 3건** |
| **PK 없는 테이블** | 운영 275개 중 **122개(44%)** |
| 계정 권한 | CONNECT + RESOURCE (`SELECT ANY TABLE` 없음) |

### 4-2. 학사DB 경계 — DB 링크 2개

```
DREAMCATCH(dream) ──[ V_BRDB ]──> BRDB   ← 학사DB   (2020-05-07 생성, 가동 중)
                  ──[ IC2_LINK ]─> IC2    ← 구 시스템 (IC2_* 테이블의 출처)
```

**한 줄 요약: 학사DB는 "누가·어디 소속인지"만 준다. "무엇을 했는지"는 전부 이 DB가 만든다. "무엇을 배웠는지(수강·성적)"는 받을 준비만 해두고 비어 있다.**

#### 받고 있는 것

| 테이블 | 행수 | 소스 참조 | 내용 |
|---|---|---|---|
| `V_USR_INF` (43컬럼) | **113,733** | **632회** (최다) | 통합 사용자 — 학생·교수·조교·직원 전부, `USER_TY_CD`로 구분 |
| `V_DEP_INF` (9컬럼) | 1,679 | **387회 / 23파일** | 조직 계층 — **대학원 누락** |
| `V_DEP_INF_ALL` (10컬럼) | 1,921 | 65회 / 7파일 | 조직 계층 + `UNIV_CODE`(과정구분) |
| `V_ADD_JOB_PART` | 17 | — | 겸직·부서 배정 |
| `V_USR_INF_DUP_INF` | 116 | — | 중복 사용자(학번·사번 동시 보유) |

`V_USR_INF`가 주는 43필드 — 식별(`INTG_UID` PK·`LOGIN_ID`·`UUID`) · 신원(`USR_NM`·영문·한자·`SEX`·`AGE`·`PHOTO`·`FOR_YN`) · 신분(`USER_TY_CD`·`RANKID`·`HOFC_STA_CD`) · 소속(`DAEHAK_CD`·`HAKBU_CD`·`MAJOR_CD`/`MAJOR_CD2`·`ORGID`) · 학적(`STU_SCHGR`·`ENTER_DT`·`OUT_*`·`ISU_CNT`) · 관계(**`PROF_ID` 지도교수**·`DEAN_FLAG`) · 연락처

> ⚠️ **113,733행 = 졸업생·퇴직자 누적.** 조회 화면은 반드시 `HOFC_STA_CD` 필터를 걸어야 한다.

#### 받을 준비만 해두고 비어 있는 것 <span>(전부 0행)</span>

| 테이블 | 담을 것 |
|---|---|
| `V_SUGANG` (15컬럼) | `YEAR`·`SMT`·`STUDENT_CD`·**`CURI_NUM`**·`COURSE_CLS`·**`GRAD_DIV`**·**`GRADE`**·**`GPA`**·`FINISH_YN`·재수강 |
| `V_LECT_INF` (20컬럼) | `CURI_NUM`·**`CURI_NM`**·**`CDT_NUM`(학점)**·`DEPT_CD`·`PROF_ID`·`CAP_PER1~6` |
| `VIEW_SUGANG_ALL` | 학사 원본 뷰 이름 = **`수강전체`** |
| `VIEW_SUUP_GAESUL` | 학사 원본 뷰 이름 = **`수업개설`** |
| `COM_HAKJUK` (24컬럼) | 가장 상세한 학적 스키마 — 학사경고·학위종별·부전공1·2·복수전공1·2·교직 |
| `COM_HAKBYUNDONG` | 학적변동 이력 |
| `V_PC_SUGANG` · `V_USR_SCORE` · `VIEW_HAKGWA_HYUNJAE` · `VIEW_MEMBER_SOSOK` | 구버전·복제본 |

**★ 조인 쿼리는 이미 작성돼 있다.** `Ca.getGoalCour_List`(ca.xml:334) · `Ca.getLecList`(ca.xml:2345) · `Common.getLecList`(common.xml:1325)가 `V_SUGANG ⨝ V_LECT_INF`를 조인한다. 조인키는 `YEAR`+`SMT`+`GRAD_DIV`+`CURI_NUM` 4컬럼 복합.
→ **교과 역량 산출에 수강 데이터를 쓰려고 만들어 놓고 적재가 켜지지 않은 상태다.**

#### ⚠️ 예외 — 학사 적재본에 쓰기가 있다

| statement | 위치 | 하는 일 |
|---|---|---|
| `Co.setOutConInfo_Insert2` | co.xml:372 | **`INSERT INTO V_USR_INF`** — 외부 위촉 상담사를 학사 적재본에 직접 추가 |
| `Co.setCnCt_Delte3` | co.xml:923 | 외부 상담사 삭제 |
| `Common.delete_V_USR_INF_dup` | common.xml:1652 | 중복 사용자 정리 |
| `Common.UPDATE_ORGID_U01` | common.xml:2542 | `@V_BRDB` **DB링크 직접 조회**로 `ORGID` 보정 |

→ `V_USR_INF`는 순수 적재본이 아니라 **"학사 데이터 + 외부 인원"이 섞인 하이브리드 테이블**이다.
→ 구분은 `USER_TY_CD` + `HOFC_STA_CD` 조합. 짝이 되는 것은 `COM_CON_INF.INOUT_GB`(내외부)와 `CONPWD`(자체 비밀번호).
→ `@V_BRDB` 실시간 조회가 소스에 존재한다 = 필요 시 실시간 조회가 가능하다는 뜻.

### 4-3. 조직 트리 구조

대학·학부·학과·전공이 별도 테이블이 아니라 **하나의 자기참조 트리**다.

```
DEPT_CD      조직코드      DEPT_UP_CD   상위 조직코드   ← 계층의 근간
DEPT_NM      조직명        LVL          계층 레벨
DEPT_SUB_NM  보조명        GRP_CD/NM    계층 종류
UNIV_CODE    과정구분(_ALL만)  USE_YN    사용여부(폐과)
ORDERING     정렬
```

| `GRP_CD` | `GRP_NM` | `LVL` | 건수 | 사용중 | 폐지 |
|---|---|---|---|---|---|
| `0001` | 단대 | 1 | 16 | 16 | 0 |
| `0002` | **학과** | 2 | 599 | 395 | 204 |
| `0003` | **전공** | 3 | 808 | 553 | 255 |
| `0004` | 기관 | 4 | 498 | 317 | 180 |

→ 학사 조직은 **실질 3단(단대–학과–전공)**이고, `LVL4`는 학과 계층이 아니라 **부속기관**(연구소·센터·대학원 사무조직)이다.

| `UNIV_CODE` | 의미 | 건수 |
|---|---|---|
| `00` | 학부 | 725 |
| `01` | 대학원 석사 | 364 |
| `02` | 대학원 박사 | 69 |
| `03`~`12` | 전문·특수대학원 | 226 |
| `90` | 기타 | 21 |
| (빈값) | 기관 | 515 |

**`LVL1` 단대 16개**

```
인문대학 · 사회과학대학 · 경상대학 · 경영대학 · 자연과학대학 · 공과대학
메카트로닉스대학 · 예술대학 · 미래융합대학 · 글로컬첨단과학기술대학
GAST-인공지능대학 · GAST-기계공학대학 · 거창캠퍼스 · 남해캠퍼스 · 본부 · 기관
```

- **`간호대학`은 단과대가 아니다** — `간호학과`(코드 `334`)는 상위 `4`(자연과학대학) 소속
- 대학원 학과는 **`LVL2`에 학부 학과와 나란히** 있고 이름 뒤에 `(대학원-석사)` 표기가 붙는다
- 대학원 조직 자체(`대학원`·`교육대학원`·`행정대학원`…)는 **`LVL4` 기관**으로 등록돼 있다
- **`USE_YN='N'`이 639건.** 폐지·통합 조직이 행으로 남는다 — 다만 컬럼이 9개뿐이라 **승계 관계는 알 수 없다**
- ⚠️ **동명 학과가 과정별로 여러 개다.** `신소재공학부` = `505`(공과대)·`501`(메카트로닉스대)·`1528`(석사)·`2528`(박사)

### 4-4. 실제 조인 관계 <span>(FK가 없는 DB의 관계 복원 — 132종 추출)</span>

| 빈도 | 조인 | 의미 |
|---|---|---|
| 48× | `ST_CAR_STAT_REPORT.INTG_UID = V_USR_INF.INTG_UID` | 취업통계 ↔ 사용자 |
| 28× | `EP_PRM.PRM_SEQ = EP_PRM_APP.PRM_SEQ` | 프로그램 ↔ 신청 |
| 14× | `V_DEP_INF.DEPT_CD = V_USR_INF.DAEHAK_CD` | 사용자 소속(대학) |
| 14× | `V_DEP_INF.DEPT_CD = V_USR_INF.HAKBU_CD` | 사용자 소속(학부) |
| 13× | `EP_PRM_APP.USRID = V_USR_INF.INTG_UID` | 신청자 ↔ 사용자 |
| 12× | `EP_PRM_APP.PRM_APP_SEQ = EP_PRM_MAPPING.PRM_APP_SEQ` | 신청 ↔ 팀 매핑 |
| 10× | `COUNSEL_PROBLEM.GRP_CODE/CODE = SY_CODE.GRP_CODE/CODE` | 상담 문제유형 ↔ 공통코드 |
| 6× | `EP_PRM_CHARGE.CH_UID = V_USR_INF.INTG_UID` | 프로그램 담당자 |
| 5× | `V_DEP_INF.DEPT_CD = V_USR_INF.ORGID` | 교직원 소속 |
| 3× | `V_LECT_INF.(YEAR,SMT,GRAD_DIV,CURI_NUM) = V_SUGANG.(…)` | **강의 ↔ 수강 (미가동)** |

- **학생 조인은 `INTG_UID`로 통일돼 있다.** 컬럼명은 `USRID`·`USERID`·`STU_NO`·`STU_ID`로 갈리지만 값은 모두 `INTG_UID`(학번/사번).
- **소속 조인은 세 갈래** — 학생은 `DAEHAK_CD`·`HAKBU_CD`·`MAJOR_CD`, 교직원은 `ORGID`.

### 4-5. 구조적 특징과 한계

| # | 사실 | 근거 |
|---|---|---|
| 1 | **FK 3건 / PK 없는 테이블 122개(44%)** — 정합성을 DB가 보장하지 않는다 | `E7_tables.csv` |
| 2 | **상태 변경 이력이 없다.** `SY_AUTH_LOG`(권한변경)뿐이고 상담·비교과·채용은 UPDATE 덮어쓰기 | 소스 전수 |
| 3 | **`COUNSEL_MASTER` 46컬럼에 신청과 결과가 한 행**으로 들어간다 | `E3_tables.csv` |
| 4 | **`BASICSETTING` 57만 행** — 상담 슬롯을 일자×시×분 행으로 전개한 결과 | `D1_tables.csv` |
| 5 | **`EP_PRM` 125컬럼 중 27개가 값 0건** | `k-2수정.csv` |
| 6 | **`EP_PRM_APP`이 신청 시점 스냅샷을 복사해 둔다** (`HOFC_STA_CD`·`STU_SCHGR`·`EMAIL`·`HP`) | 이 DB의 유일한 as-of 패턴 |
| 7 | **`CON_PROF_INFO`도 신청 시점 `USER_TY_CD`·`STU_SCHGR`를 스냅샷 저장** | 같은 패턴 |
| 8 | **`SY_FILE`이 다형 참조**(`TAB_NM`+`TAB_SEQ`+`FILE_CD`) — 도메인별 첨부 테이블이 없다 | 4.2만 행 |
| 9 | **`SY_CODE` 2단 계층**(`UP_GRP_CODE`/`UP_CODE`) — 상담분야→세부분야 | 3,874행 |
| 10 | **코드가 세 군데 흩어져 있다** — `SY_CODE` 테이블 / SQL `CASE WHEN` / JSP 하드코딩 | `SY_CODE` 참조 statement 108개 |
| 11 | **`USER_TY_CD` 리터럴 `'1101'`이 sqlmap 229곳에 박혀 있다.** `'1201'`(대학원)이 들어간 곳은 3곳 | 파일별: `st`103·`ca`40·`sy`24·`co`22·`fu`8 |
| 12 | **자격증명이 여러 군데** — `COM_CON_INF.CONPWD`(외부 상담사) · `COM_COMP_INF.COMP_PWD`(MD5 32자) · `TB_CARR_USER_LOGIN`(기업) | `l-1.csv` |
| 13 | 업로드 파일을 **웹 문서 루트 안**에 두고 확장자를 부분문자열로 검사한다 | `06_findings.md` |
| 14 | **비어 있지 않은 백업 스냅샷 테이블 관행** — `SY_AUTH_20250301`·`SY_MENU_20250301` | 권한 개편 흔적 |

#### `SY_CODE` 그룹코드 대장 <span>(소스에서 확인된 것)</span>

| 그룹 | 용도 |
|---|---|
| `0024` | 상담사 상태 |
| `0034` | 학적·재직 상태명 |
| `0053` · `0054` · `0103` | 상담 세부분야 (취업전략센터 / 학생과 / 기초교육원) |
| `0060` | 비교과 그룹 상태 |
| `0131` | **교수상담 세부구분** 6종 |
| `0143` · `0146` | 상담 유형 계열 |
| `0145` → `0147` | **상담분야 → 세부분야 (2단 계층)** |
| `0150` | 역량 |
| `0069` · `0070` · `0071` | 전국 대학·전문대·대학원 1,900여 개 (패키지 잔재) |
| `0012` · `0037` | **현행 코드관리 화면이 하드코딩으로 숨긴다** — 정체 미확인 |

> 전수는 `docs/dbmeta/out/E4_tables.csv`(3,874행).

### 4-6. 학과가 화면에서 사라지는 원인 <span>(상시 운영 이슈 · 확정)</span>

증상: **학사DB에는 있는 학과가 관리자 `회원관리 > 조교학과 배정`에만 없다.**
쿼리는 `Fu.assDeptList2`(fu.xml:331-372).

```sql
FROM V_DEP_INF A                                          -- ① 학과
INNER JOIN      V_DEP_INF B ON A.DEPT_UP_CD = B.DEPT_CD   -- 단대
LEFT OUTER JOIN V_DEP_INF C ON C.DEPT_UP_CD = A.DEPT_CD   -- 전공
LEFT OUTER JOIN V_USR_INF D ON D.MAJOR_CD  = C.DEPT_CD    -- 학생 수
WHERE A.USE_YN='Y' AND B.USE_YN='Y' AND C.USE_YN='Y'      -- ② 여기가 문제
  AND A.DEPT_UP_CD IN (SELECT DEPT_CD FROM V_DEP_INF
                       WHERE LVL='1' AND USE_YN='Y' AND GRP_CD='0001')
  AND A.GRP_CD IN ('0002','0003')
```

| # | 원인 | 떨어지는 학과 |
|---|---|---|
| ① | **`V_DEP_INF`를 쓴다** (`_ALL`이 아니다) | 대학원 전부 (242개 차이) |
| ② | `LEFT OUTER JOIN C` 인데 **`WHERE C.USE_YN='Y'`** → outer join이 inner join으로 붕괴 | **전공이 없는 학과** ← 신설 학과의 전형 |
| ③ | 상위조직을 `LVL='1' AND GRP_CD='0001'`(단대)로 한정 | 기관(`0004`) 밑에 달린 학과 |
| ④ | 학과 자신이 `GRP_CD IN ('0002','0003')` | 그룹코드가 다르게 부여된 신설 학과 |

**딸린 사실**

- `V_DEP_INF`는 뷰가 아니라 **로컬 테이블**이다. `DairyBatch`(01:00) → `Common.merge_V_DEP_INF` MERGE로 복제한다. **그 배치가 `web.xml:20`에서 주석 처리돼 미기동** — 별도 스케줄러 존재 여부 **미확인**(최근 갱신 2026-07-28이라 어떤 경로로든 돌고는 있다).
- 트리가 **두 벌**이다. 배치도 `merge_V_DEP_INF`와 `merge_V_DEP_INF_ALL`을 따로 돌린다(`DairyBatch.java:82-87`). 화면이 제각각 본다 — `V_DEP_INF` 193 statement / `_ALL` 34 statement.
- 조직코드를 SQL 리터럴로 배제하는 곳이 있다 — `DEPT_CD NOT IN ('3')`.
- **더미 전공 행 관행** — ②를 우회하려고 전공 없는 학부를 자기 자신을 전공으로 한 번 더 등록해 둔다(`건축학부 / 건축학부 / 436 / 436`).
- 신설 학과는 **딸린 자체 데이터가 전부 비어 있다** — `FU_ASS_DEPT`·`TB_CARR_PROF_ASSI_DEPT`·`COM_CON_TAR`·`CA_GOAL`. 자동 생성 경로가 없다.
- `FU_ASS_DEPT`는 목록 소스가 아니라 **배정 저장용**이며 **PK가 없다**(`DEPT_CD`+`MAJOR_CD`+`USR_ID` 조합 관리, 삭제 이력 없음).
- ⚠️ **네이밍 함정** — `Fu.getDaehakCode_List`는 이름이 "대학코드"인데 실제로는 `GRP_CD='0002'`(**학과**)를, `getHakgwaCode_List`는 `'0003'`(**전공**)을 조회한다. 화면 라벨과 쿼리 이름이 한 단계씩 어긋나 있다.

### 4-7. 신분코드가 대학원생을 막는 지점 <span>(실측)</span>

| 화면 | 조건 | 결과 |
|---|---|---|
| 전담교수 배정 대상 (co.xml:6255) | `USER_TY_CD IN ('1101')` | **대학원생에게 전담교수 배정 불가** |
| 조교 담당 학생 (co.xml:6352) | `USER_TY_CD IN ('1101')` | 조교 화면에 대학원생이 안 보임 |
| 상담 학생검색 팝업 (co.xml:7345 외 6곳) | `USER_TY_CD = '1101'` | 상담사가 대학원생을 검색 불가 |
| 역량진단 대상자 "전체" (ca.xml:746) | `USER_TY_CD = '1101'` | 대학원생이 진단 대상에서 제외 |
| 비교과 선발자 SMS (ep.xml:10020) | `USER_TY_CD = '1101'` | 선발돼도 문자 미발송 |

**반대로 대학원을 인지하는 곳도 있다** — 상담 슬롯의 재학/졸업 판정은 `1101 OR 1201` / `1102 OR 1202` 4종을 모두 본다(co.xml:7580-7581).
→ **스키마는 이미 대학원을 담고 있고, 일관성만 없다.**

**막고 있지 않은 것**

| 요건 | 현행 상태 |
|---|---|
| 대학원생 비교과 신청 | **이미 열려 있다.** `ep.xml`에 학부 한정 필터가 없고 참가대상 플래그가 `TRGT_STU21~24`(대학원)·`TRGT_STU31~35`(학부)로 분리 존재. 단 자격 판정이 PL/SQL `FN_GET_PRM_TRGT_YN()` 안에 있어 **본문 미확인** |
| 대학원생 교수상담 신청 | **신청 기록(`CON_PROF_INFO`)에는 제약 없음.** 막히는 곳은 **교수 목록 조회**(co.xml:6665) — `ORGID = 학생 HAKBU_CD` 매칭 + `RANKID IN ('1001','1002','1003')` 직급 3종 한정 |

> **발견** — 오프라인 교수상담 슬롯은 **상담사와 같은 `BASICSETTING` 테이블**을 쓴다(`CONSULTANTID`에 교수 `INTG_UID`, co.xml:7184).

---

## 5. 조사의 한계

- 소스는 **상담(Co) 도메인 컨트롤러 4개 + sqlmap 34개 전체**만 받았다. **JSP 화면 파일은 없다** → 화면 레이아웃·버튼 배치는 미확인. SQL과 데이터 흐름은 확정.
- 동적 SQL(`<if>`/`<choose>`)은 태그를 제거하고 분석했으므로 조건부 조인 일부 누락 가능.
- **역할↔화면 매핑의 실체(`SY_MENU_AUTH` 972행)와 메뉴 노출(`SY_MENU.USE_YN`)은 DB 데이터라 소스로 확정 불가.**
- **★ "소스 참조 0건"은 "죽은 테이블"이 아니다.** `origin/`은 운영 소스의 일부다. `CHECK_*` 11종을 죽은 스키마로 판정했다가 실측(최종변경 2025-11-15, 8,407행)에서 뒤집혔다.
- 미커버 영역 — 비교과 운영자(107매핑)·비교과 활동(78)·채용(145)·취업통계(61)·포트폴리오(40)·참여후기(54)는 사이트맵에는 있으나 데이터흐름 상세가 없다.

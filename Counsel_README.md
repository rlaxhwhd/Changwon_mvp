# 드림캐치(DREAMCATCH) — 상담사 포털 스펙 (`/admin`)

> 국립창원대학교 역량개발관리시스템 "드림캐치"의 **상담사(예비 관리자) 화면** 스펙.
> 학생 포털(`/v2`)과 짝을 이루는 **운영 측** 화면으로, 학생의 진단→상담→로드맵→수행 데이터를 다룬다.
> 프론트엔드 전용 · 백엔드 없음 · 데이터는 **JSON 동적**(하드코딩 금지, `students.ts` 패턴 미러) · 데스크톱 전용.
> 디자인은 **`DESIGN.md` 단일 소스**를 따른다.
>
> _작성: 2026-07-09 · 상태: 스펙 확정, 구현 전_

---

## 0. 확정 사항 (결정 로그)

| # | 항목 | 결정 |
|---|---|---|
| 1 | 포털 형태 | 학생과 분리된 **별도 SPA** — `admin.html` → `src_admin/`, `basename '/admin'` |
| 2 | 상담사 역할 | **2종: 진로취업상담사 · 심리상담사** (교수는 제외 → 별도 교수 포털) |
| 3 | 채용공고 데이터 | **추후 사용자 제공** — 스키마·화면 자리만 잡고 placeholder로 대기 |
| 4 | 데이터 방식 | **JSON 동적** (`json-dynamic-screen` 스킬 준수: 스키마→JSON→로더/스토어→화면 구독) |
| 5 | 연계 방식 | 백엔드 없음 → **localStorage 공유 스토어**로 학생↔상담사 데이터 연결 (동일 브라우저 데모 범위) |

---

## 1. 학생 ↔ 상담사 연계 흐름 (핵심)

```
[학생]  진단(M1 유형분류)  ─────────────▶  학생 6유형 산출        (검사 자동)
                                              │
[상담사] 진단결과로 상담 진행 ──────────────▶  상담 완료
                                              │
                                        IAP 유형(R1~R6) 확정      (상담사+AI)  ★상담사가 확정
                                              │
[AI]    로드맵 자동 생성
[상담사+학생] 함께 논의 ────────▶ ★상담사가 로드맵 수정·확정      ★상담사 전용 편집 권한
                                              │
[학생]  로드맵 변경 원함 ──▶ 상담 재신청 ──▶ [상담사] 반영         (양방향 루프)
                                              │
[학생]  로드맵 / 내 성장 / 퀘스트 수행 ──────▶ 수행 데이터 축적
                                              │
[상담사] 학생 수행 데이터 전부 열람 ◀─────────                   (모니터링)
```

**권한 원칙**
- **로드맵 수정은 상담사(진로)만.** 학생 화면은 로드맵을 *조회·수행*만 하고, 바꾸려면 상담을 신청한다.
- **IAP 유형 확정**도 상담사 몫 (진단 6유형은 검사가 1차 산출, 상담사가 확정·보정).
- 학생의 모든 수행 데이터(퀘스트·비교과·출석·성장일지·포트폴리오)는 담당 상담사가 열람 가능.

---

## 2. 상담사 역할 & 권한 매트릭스

| 기능 | 진로취업상담사 | 심리상담사 |
|---|:---:|:---:|
| 진로취업 상담 접수·진행·완료 | ✅ | — |
| 심리 상담 접수·진행·완료 | — | ✅ |
| 학생 상세 조회 (진단·성장·포트폴리오) | ✅ | ✅(제한) |
| IAP 유형 확정 | ✅ | — |
| **로드맵 조회·수정·확정** | ✅ | — |
| 로드맵 변경 요청 처리 | ✅ | — |
| **채용공고 CRUD** | ✅ | — |
| 비교과 프로그램 운영 *(선택)* | ✅ | — |

> 심리상담사는 상담(심리)과 학생 심리·성향(M7 NEO 등) 위주로 제한 열람. 로드맵·채용공고는 진로상담사 전용.
> **교수상담**(학생 화면 `/counsel/professor`)은 이 포털이 아닌 **별도 교수 포털**에서 처리(추후).

---

## 3. 진입 구조 (멀티 SPA — 학생 패턴 그대로)

```
admin.html  ──▶  src_admin/main.tsx  ──▶  App.tsx (RouterProvider, basename '/admin')

vite.config.ts multi-spa-fallback 확장:  '/admin*' → admin.html
build.rollupOptions.input 에  admin: resolve(__dirname, 'admin.html')  추가
```

- 학생 `v2.html`/`src_v2` 와 **완전 분리된 엔트리**. 라우팅·레이아웃·상태 독립.
- 공유되는 건 **데이터 계층(localStorage 스토어)** 과 **디자인 토큰(DESIGN.md)** 뿐.

---

## 4. 사이트맵 (라우트)

역할에 따라 노출 메뉴가 다르다. `[진로]` 표시는 진로상담사 전용.

```
/admin
├── /login                          로그인 (역할 선택: 진로취업상담사 / 심리상담사)
├── /                               홈 대시보드 (역할별 위젯: 오늘 일정·미처리 신청·담당 학생 요약)
│
├── 상담 관리   /counsel
│   ├── /counsel/requests           신청 접수함 (내 담당 유형만 · 대기/확정/완료 탭)
│   ├── /counsel/schedule           일정·예약 캘린더 + 가능 시간대 설정
│   ├── /counsel/session/:studentId 상담 진행 (학생 진단결과 열람 + 상담 기록지 작성)
│   └── /counsel/records            완료 상담 내역·코멘트 관리
│
├── 학생 관리   /students
│   ├── /students                   담당 학생 목록 (학과·학년·유형·트랙·진행률 필터)
│   └── /students/:id               학생 상세 (진단·IAP·로드맵·성장·퀘스트·포트폴리오 통합)
│
├── 로드맵 관리 /roadmap            [진로]
│   ├── /roadmap/requests           로드맵 변경 요청함 (학생 재신청 접수)
│   └── /roadmap/:studentId         로드맵 조회·수정·확정  ★핵심 편집기
│
├── 채용공고   /jobs                [진로] · 데이터 추후 제공
│   ├── /jobs                       공고 목록 (게시·마감·출처 관리)
│   ├── /jobs/new                   공고 등록
│   └── /jobs/:id/edit              공고 수정·삭제
│
├──  비교과 운영 /programs     [진로]
│   ├── /programs                   프로그램 목록
│   ├── /programs/new               등록
│   ├── /programs/:id               신청자·출석 관리
│   └── /programs/blacklist         블랙리스트 관리 (미참여자 벌점 부여)
│
└── /settings                       내 프로필·가능 시간대·알림
```

**상단 네비 순서(안):** 홈 → 상담 관리 → 학생 관리 → 로드맵 관리`[진로]` → 채용공고`[진로]` → (비교과) → 설정

---

## 5. 기능 상세

### A. 상담 관리
- **신청 접수함** — 학생이 넣은 상담 요청(내 담당 유형만). 상태: 대기·확정·완료·취소. 확정/거절/일정 변경.
- **일정·예약** — 가능 시간대 설정, 캘린더로 예약 관리.
- **상담 진행 화면** — 학생 진단결과·프로필·이전 이력을 좌측에 띄우고 우측에 **상담 기록지** 작성.
- **완료 처리** — 소견·코멘트 저장 → 학생 화면 `상담 현황`(`/counsel/record`)에 반영.
- **IAP 확정** — 6유형 + 상담결과로 R1~R6 확정 (진로상담사).

### B. 로드맵 관리 `[진로]` — 핵심
- 학생별 AI 생성 로드맵 **조회**.
- **수정 편집기** — 단·중·장기 항목 추가/편집/삭제, 우선순위(P0~P2)·중요도(필수/중요/권장) 조정, 태스크 편집.
- **확정/버전** — 변경 이력 기록, 학생 화면에 반영.
- **변경 요청함** — 학생이 신청한 로드맵 변경 상담을 접수해 반영.

### C. 학생 모니터링
- **담당 학생 목록** — 필터·검색.
- **학생 상세** — 진단결과·IAP·로드맵 진행률·퀘스트/성장 수행·비교과 이수·출석/일일미션·포트폴리오·성장경험일지 통합 열람.
- **집중관리 플래그** — 취약관리형(R5)·저활동 학생 알림.

### D. 채용공고 `[진로]` — 데이터 추후 제공
- 공고 **등록/수정/삭제(CRUD)**. 출처(외부 API 연동분 vs 직접 등록) 구분, 게시·마감 관리.
- ⚠️ **실제 공고 데이터는 사용자가 추후 제공** → 지금은 스키마와 CRUD 화면 골격만.

### E. 비교과 프로그램 운영 `[진로]` *(선택)*
- 프로그램 등록·수정, 신청자 관리, 출석 체크. 학생 화면 "비교과프로그램 신청"의 공급 측.
- **블랙리스트 관리** (`/programs/blacklist`) — 프로그램을 **신청해놓고 미참여(노쇼)한 학생에게 벌점 부여**. 출석 체크 결과와 연동해 미참여자를 자동 추림. 벌점 누적·차감·해제 관리, 부여 사유 기록. 벌점은 학생 화면에도 반영(누적 벌점·사유 열람).

### F. 공통
- **홈 대시보드**, **로그인/역할**, **알림**, **설정(가능 시간대)**.

---

## 6. 데이터 모델 (JSON 동적 · `students.ts` 패턴 미러)

`json-dynamic-screen` 원칙: **스키마 → JSON → 로더/스토어 → 화면 구독**. 하드코딩 리터럴 금지.

```
src_admin/data/
├── counselors.ts            # 상담사 로더 + 활성 상담사 스토어 (students.ts 미러)
├── counselors/
│   ├── career_kim.json      # 진로취업상담사 프로필(이름·역할·담당학과/범위)
│   └── psych_lee.json       # 심리상담사 프로필
├── schema/                  # 타입 정의(단일 소스)
│   ├── counselor.ts         # 상담사·역할·권한
│   ├── counselRequest.ts    # 상담 신청/예약/기록지
│   └── roadmapEdit.ts       # 로드맵 수정·확정·버전
└── jobsSource.ts            # 채용공고 CRUD 소스 (데이터 추후 주입)
```

- 학생 데이터는 **`src_v2/data/students*.json`를 공유 읽기** (상담사가 학생을 조회).
- 상담사 고유 데이터(상담사 프로필·예약·기록)는 `src_admin/data`에 별도 JSON.

---

## 7. 연계 메커니즘 (백엔드 없이 학생↔상담사 연결)

동일 브라우저 내 **localStorage 공유 스토어**로 두 SPA를 잇는다 (데모 범위).

| 이벤트 | 쓰기 주체 | localStorage 키(안) | 읽기 주체 |
|---|---|---|---|
| 상담 신청 | 학생 `/counsel/*` | `dc_counsel_requests` | 상담사 접수함 |
| 상담 완료·코멘트 | 상담사 | `dc_counsel_records` | 학생 `/counsel/record` |
| IAP 확정 | 상담사 | `dc_iap_result` | 학생 진단결과·IapSummaryBanner |
| **로드맵 수정** | 상담사 | `dc_roadmap_overrides[studentId]` | 학생 로드맵(base JSON + override **병합** 표시) |
| 로드맵 변경 요청 | 학생 | `dc_roadmap_requests` | 상담사 변경 요청함 |
| 비교과 미참여 벌점 | 상담사 블랙리스트 | `dc_penalty[studentId]` | 학생 마이페이지·성장(누적 벌점·사유) |

> 핵심: 학생 화면은 base 학생 JSON을 그대로 두고, **상담사 수정분(override)을 얹어 병합**해 렌더한다. 이래야 원본 JSON을 안 건드리면서도 "상담사가 고치면 학생 화면에 반영"이 성립한다.

---

## 8. 추후 결정 / 대기 항목

- [ ] **채용공고 실제 데이터** — 사용자 제공 대기. 받는 즉시 `jobsSource` 스키마 확정 + 학생 `/jobs`와 공유.
- [ ] **비교과 운영(E)** 포함 여부 — MVP 범위 확정 필요.
- [ ] **교수 포털** — 학생 `/counsel/professor` 요청의 처리 주체. 이 포털과 별개로 후속 설계.
- [ ] 로드맵 override 스키마 세부 — `RoadmapPhase`(students.ts) 구조 재사용 예정.

---

## 9. 다음 단계 (구현 착수 시)

실제 `/admin` 화면 제작은 **`dreamcatch-orchestrator` 하네스**로 진행한다:
`team-lead`(계획) → `planner`(스키마·라우트 확정) → `designer`(DESIGN.md 적용) → `developer`(JSON 동적 구현) → `qa`(모듈별 점진 검증).

착수 순서(권장): ① SPA 스캐폴딩(`admin.html`+`src_admin`+라우터) → ② 데이터 스키마·로더 → ③ 로그인·홈 대시보드 → ④ 상담 관리 → ⑤ 학생 상세 → ⑥ **로드맵 편집기(핵심)** → ⑦ 채용공고(데이터 수령 후) → ⑧ 연계(localStorage) 검증.

# 프로젝트 공통 작업 지침

이 저장소에서 작업하는 모든 세션에 적용한다. 사용자가 2026-09-16 확정한 작업 원칙이다.
현재 대화의 명시적인 사용자 지시가 우선하며, 이미 확정된 내용은 다시 묻지 않는다.

## 1. 불확실한 사항과 분기 확인

- 모르는 업무 규칙, 요구사항 또는 구현 방향이 여러 갈래로 나뉘는 사항은 사용자에게 질문한다.
- 특히 데이터 의미, 집계 기준, 기간·초기화 규칙, 권한, 완료 판정, 외부 연계 계약은 임의로 확정하지 않는다.
- 코드·문서·DB를 읽어 확인할 수 있는 사실은 먼저 조사한다. 조사 후에도 모르는 부분과 선택지의 차이를 짧게 설명하고 질문한다.
- 답변이 필요한 부분은 기다리되, 답변과 무관하게 진행할 수 있는 조사·구현·검증은 계속한다.

## 2. Chrome MCP 기반 웹 검증

- 웹 화면 또는 API 연결을 변경하면 Chrome MCP로 실제 대상 화면을 열어 확인한다.
- 콘솔 오류, 실패한 요청, HTTP 상태, 응답 시간, 중복 호출, 불필요한 요청과 큰 응답을 확인한다.
- 느린 요청은 네트워크 시간과 서버 처리·DB 시간을 구분해서 원인을 찾는다. DB가 원인이라고 추측만으로 결론 내리지 않는다.
- 발견한 오류와 성능 문제는 작업 범위 안에서 수정하고, 같은 사용자 동작을 다시 실행해 해결 여부를 확인한다.
- 결과에는 확인한 화면·주요 동작과 남은 오류·느린 요청을 사실대로 기록한다. 성능 개선은 가능하면 변경 전후 같은 조건으로 비교한다.
- Chrome MCP가 없거나 연결되지 않으면 먼저 도구 검색·연결 상태를 확인한다. 여전히 사용할 수 없으면 그 사실과 브라우저 미검증 범위를 알린다. 빌드·타입 검사만으로 실제 화면·네트워크 검증까지 완료했다고 보고하지 않는다.
- 설치 여부는 현재 노출 도구 목록만으로 단정하지 않는다. `codex mcp list`와 `claude mcp list`를 확인한다. Claude의 플러그인 제공 MCP는 `.claude.json`의 `mcpServers`에 직접 표시되지 않을 수 있다.
- 2026-09-16 이 PC에서 확인: Codex 전역 `chrome-devtools` 등록 완료, Claude `plugin:ecc:chrome-devtools` 기존 연결 정상. 설정·확인 방법은 [Chrome MCP 연결 기록](docs/CHROME_MCP_SETUP.md)을 참고한다.
- 현재 세션에 도구가 노출되지 않아도, 등록된 공식 MCP 서버에 표준 stdio MCP 클라이언트로 직접 연결할 수 있으면 이를 이용해 검증한다. 직접 연결 검증과 세션 내장 도구 노출 여부는 구분해서 보고한다.
- 콘솔·네트워크 자료에서 인증 토큰, 비밀번호, 개인정보를 불필요하게 출력하거나 문서에 남기지 않는다.

## 3. DB 구조 및 응답 성능 검토

- DB·SQL 작업 시 [postgres-patterns](.claude/skills/postgres-patterns/SKILL.md)를 읽고 적용한다.
- 스키마·데이터 마이그레이션 작업 시 [database-migrations](.claude/skills/database-migrations/SKILL.md)를 읽고 적용한다.
- DB 리뷰에는 [DB 리뷰 지침](.claude/agents/codex-astra-db-reviewer.md)을 참고한다. 이는 에이전트 지침 파일이며 스킬과 구분한다. 별도 에이전트 실행은 현재 세션의 위임 규칙을 따른다.
- 실제 조회 패턴과 데이터 규모를 기준으로 정규화, JSON 사용 범위, FK·UNIQUE·CHECK, 복합·부분 인덱스, 페이지네이션, N+1, 과도한 JOIN·응답 크기를 검토한다.
- 느리거나 복잡한 주요 조회는 실행 계획과 실제 소요 시간을 확인한다. `EXPLAIN ANALYZE`는 쿼리를 실행하므로 변경 쿼리나 무거운 운영 쿼리에 무심코 적용하지 않는다.
- 동시 수정·중복 요청·트랜잭션·잠금·권한 범위도 함께 검증한다. 성능을 위해 무결성이나 접근 통제를 제거하지 않는다.
- 적용된 마이그레이션은 수정하지 않는다. 새 파일로 변경하며 테스트 DB 검증과 복구 방안을 준비한다.
- DB 구조 때문에 응답이 느려지는 문제를 발견하면 원인을 해결하고 재측정한다. 측정하지 않은 속도나 성능 보장은 보고하지 않는다.

## 스킬 탐색 및 디자인

- 세션에 표시된 스킬 목록 외에도 프로젝트의 `.claude/skills/`와 관련 `.claude/agents/`를 확인한다. 숨김·무시 경로이므로 필요하면 `rg --files --hidden --no-ignore .claude/skills`로 찾는다.
- 스킬은 실제 파일을 읽은 경우에만 적용했다고 말한다. 찾지 못한 경우 정확한 경로를 먼저 확인한다.
- 디자인 작업은 작업에 맞는 디자인 스킬을 선택해 읽고 적용한다. 기존 화면 수정에서는 공통 레이아웃·좌우 여백·타이포그래피와 이미 확정된 사용자 선호를 우선한다.

## Graft 코드 관계 탐색 및 재사용

- 코드 수정 전에 [Graft 사용 지침](.claude/skills/graft/SKILL.md)을 읽고, `graft-changwon` MCP로 관련 정의·기존 공통 구현·호출자를 확인한다. 공통 기능을 추가하거나 옮길 때는 다른 페이지의 동일 기능을 먼저 찾는다.
- 재사용 가능한 로직은 기존 공통 모듈의 import를 우선한다. 실제로 같은 책임과 계약인 경우 공통 함수·훅·컴포넌트로 추출하고, 권한이나 업무 규칙이 다른 기능을 이름만 같다는 이유로 합치지 않는다.
- 그래프는 코드 탐색의 근거이며 규약·업무 결정의 정본이 아니다. 관련 원본 코드와 API 계약을 읽어 확인한다. HTTP 통신, 동적 호출, SQL/DB 관계는 그래프만으로 완전히 추적되지 않으므로 직접 검색·검증한다.
- MCP가 현재 세션에 노출되지 않으면 `powershell -NoProfile -File tools/graft/graft.ps1` 뒤에 `ask`, `callers`, `skeleton`, `check` 등의 명령을 붙여 사용한다. CLI 및 설정 복구 방법은 [설치 기록](docs/GRAFT_SETUP.md)을 따른다.
- Codex와 Claude는 동일한 체크아웃의 로컬 그래프를 사용한다. 자동 조회 갱신을 사용하고, 그래프 자체는 커밋하지 않는다. 작업 인계 문서와 DB/Chrome 검증 규칙은 계속 적용한다.
,
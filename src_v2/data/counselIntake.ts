// ─────────────────────────────────────────────────────────────────────────
// 상담 문진표 템플릿 (단일 소스)
//
// 신청 단계에서 학생에게 묻는 질문이다. 화면에 질문 문자열을 박지 않는다 —
// 상담사가 문진표를 편집하게 되면 이 배열이 그대로 저장소로 바뀐다.
//
// 답변은 StudentCounselRequest.intake 에 [질문, 답변] 쌍으로 붙는다.
// 질문 원문을 답변과 함께 저장하는 이유는 현행 EP_PRM_APP 과 같은 이유다 —
// 템플릿이 나중에 바뀌어도 그때 무엇을 물었는지가 남아야 한다(CLAUDE.md 규칙 2).
// ─────────────────────────────────────────────────────────────────────────

/** 신청 시 받은 문진표 답변 한 줄 */
export interface CounselIntakeAnswer {
  question: string
  answer: string
}

/** 진로취업 상담 문진표 — 심리·교수 상담은 아직 템플릿이 없다. */
export const CAREER_INTAKE_QUESTIONS: string[] = [
  '상반기 공채 자소서·면접 준비는 어느 정도 진행됐나요? 가장 막히는 지점은 어디인가요?',
  '직무의 변화가 생겼나요?',
]
